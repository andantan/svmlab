basePath: /
definitions:
  account.AccountAuthorityRequest:
    properties:
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  account.AccountAuthorityResponse:
    properties:
      close_authority:
        description: |-
          CloseAuthority reports who can actually close this token account,
          which is never blank once Type is token_account: the program checks
          close_authority.unwrap_or(owner), so this is TokenOwner whenever the
          account carries no close authority of its own, and the stored value
          otherwise. It is never left for the caller to resolve that fallback.
        type: string
      delegate:
        type: string
      exists:
        type: boolean
      freeze_authority:
        type: string
      m:
        description: M, N, and Signers are set only when Type is multisig.
        type: integer
      mint_authority:
        description: |-
          MintAuthority and FreezeAuthority are relevant only when Type is mint.
          Either can be permanently removed on a real mint, and empty here means
          exactly that: gone for good, not merely unset.
        type: string
      "n":
        type: integer
      nonce_authority:
        description: |-
          NonceAuthority is relevant only when Type is nonce_account, and even
          then only once initialized — it is always present in the response
          (empty when it does not apply) rather than silently omitted, so an
          empty string is never ambiguous between "not this kind of account" and
          "this account has none".
        type: string
      owner:
        type: string
      public_key:
        type: string
      signers:
        items:
          type: string
        type: array
      token_owner:
        description: |-
          TokenOwner and Delegate are relevant only when Type is token_account.
          Delegate empty means no delegate is currently approved.
        type: string
      type:
        type: string
    type: object
  account.AccountOwnerRequest:
    properties:
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  account.AccountOwnerResponse:
    properties:
      exists:
        type: boolean
      owner:
        type: string
      public_key:
        type: string
      system_owned:
        type: boolean
    type: object
  account.AccountRequest:
    properties:
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  account.AccountResponse:
    properties:
      data:
        type: string
      executable:
        type: boolean
      exists:
        type: boolean
      initialized:
        description: |-
          Initialized is true for anything without an uninitialized state of its
          own to report — a plain wallet, a program, an account this endpoint has
          no parser for. It is only ever false for the handful of layouts that
          actually carry an initialization flag: a durable nonce account, a mint,
          a token account, or a multisig, allocated and assigned to the right
          program but not yet initialized by InitializeMint*/InitializeAccount*/
          InitializeMultisig*.
        type: boolean
      lamports:
        type: string
      owner:
        type: string
      public_key:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  account.AirdropRequest:
    properties:
      public_key:
        type: string
    type: object
  account.AirdropResponse:
    properties:
      lamports:
        type: string
      signature:
        type: string
      sol:
        type: string
    type: object
  account.BalanceResponse:
    properties:
      lamports:
        type: string
      public_key:
        type: string
      sol:
        type: string
    type: object
  account.NonceRequest:
    properties:
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  account.NonceResponse:
    properties:
      authority:
        type: string
      exists:
        type: boolean
      initialized:
        type: boolean
      lamports_per_signature:
        type: string
      nonce:
        type: string
      public_key:
        type: string
      state:
        type: integer
      version:
        type: integer
    type: object
  account.TokenAccountResponse:
    properties:
      amount:
        type: string
      close_authority:
        type: string
      decimals:
        type: integer
      delegate:
        type: string
      delegated_amount:
        type: string
      exists:
        type: boolean
      mint:
        type: string
      native:
        type: boolean
      owner:
        type: string
      program:
        type: string
      public_key:
        type: string
      rent_reserve:
        type: string
      state:
        type: string
    type: object
  account.TokensRequest:
    properties:
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  account.TokensResponse:
    properties:
      accounts:
        items:
          $ref: '#/definitions/account.TokenAccountResponse'
        type: array
      count:
        type: integer
      public_key:
        type: string
    type: object
  misc.AeKeySeedMessageRequest:
    properties:
      token_account:
        description: |-
          TokenAccount is the account an AeKey will encrypt the confidential
          balance for (see extensions/confidential-transfer-account/configure-account).
        example: ""
        type: string
    type: object
  misc.AeKeySeedMessageResponse:
    properties:
      message:
        type: string
    type: object
  misc.BatchCall:
    properties:
      method:
        type: string
      params: {}
    type: object
  misc.BatchRequest:
    properties:
      calls:
        items:
          $ref: '#/definitions/misc.BatchCall'
        type: array
    type: object
  misc.BlockhashResponse:
    properties:
      blockhash:
        type: string
      last_valid_block_height:
        type: integer
    type: object
  misc.ConvertBase58To64Request:
    properties:
      value:
        type: string
    type: object
  misc.ConvertBase58To64Response:
    properties:
      base64:
        type: string
    type: object
  misc.ConvertBase64To58Request:
    properties:
      value:
        type: string
    type: object
  misc.ConvertBase64To58Response:
    properties:
      base58:
        type: string
    type: object
  misc.DeriveAeKeyRequest:
    properties:
      signature:
        description: Signature is base58, the same encoding sign/'s own response uses.
        example: ""
        type: string
    type: object
  misc.DeriveAeKeyResponse:
    properties:
      ae_key:
        type: string
    type: object
  misc.FeeRequest:
    properties:
      message:
        type: string
    type: object
  misc.FeeResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      valid:
        type: boolean
    type: object
  misc.GenerateElGamalKeypairResponse:
    properties:
      public_key:
        type: string
      secret_key:
        type: string
    type: object
  misc.GenerateKeypairResponse:
    properties:
      private_key:
        type: string
      public_key:
        type: string
    type: object
  misc.GenesisHashResponse:
    properties:
      configured:
        type: string
      genesis_hash:
        type: string
      matches:
        type: boolean
    type: object
  misc.HealthResponse:
    properties:
      health:
        type: string
    type: object
  misc.ProveConfidentialBurnRequest:
    properties:
      ae_key:
        description: |-
          AeKey decrypts the source's decryptable available balance and encrypts
          the new one, base58-encoded raw 16 bytes.
        example: ""
        type: string
      amount:
        description: |-
          Amount is the raw base-unit count to burn. It cannot exceed 2^48 - 1 nor
          the source's available balance.
        example: "400"
        type: string
      mint:
        description: Mint must carry ConfidentialMintBurn and ConfidentialTransferMint.
        example: ""
        type: string
      source:
        description: |-
          Source is the token account burned from, which must hold Mint and carry
          the ConfidentialTransferAccount extension.
        example: ""
        type: string
      source_elgamal_secret_key:
        description: |-
          SourceElgamalSecretKey is the secret key of the source account's own
          ElGamal public key, base58-encoded raw 32 bytes.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialBurnResponse:
    properties:
      auditor_ciphertext_hi:
        type: string
      auditor_ciphertext_lo:
        type: string
      auditor_elgamal_pubkey:
        type: string
      equality_proof_data:
        type: string
      new_decryptable_available_balance:
        type: string
      range_proof_data:
        type: string
      source_elgamal_pubkey:
        type: string
      supply_elgamal_pubkey:
        type: string
      validity_proof_data:
        type: string
    type: object
  misc.ProveConfidentialEmptyAccountRequest:
    properties:
      available_balance_ciphertext:
        description: |-
          AvailableBalanceCiphertext is the account's current available
          balance, base58-encoded raw 64-byte ElGamal ciphertext, read off
          chain. It must already encrypt zero (see
          extensions/confidential-transfer-account/withdraw) or the proof
          will not verify.
        example: ""
        type: string
      elgamal_secret_key:
        description: |-
          ElgamalSecretKey is the account's own ElGamal secret key,
          base58-encoded. The public key is derived from it here.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialEmptyAccountResponse:
    properties:
      elgamal_pubkey:
        type: string
      zero_ciphertext_proof_data:
        type: string
    type: object
  misc.ProveConfidentialMintRequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to mint. It cannot exceed 2^48
          - 1.
        example: "1000"
        type: string
      destination:
        description: |-
          Destination is the token account credited, which must hold Mint and
          carry the ConfidentialTransferAccount extension.
        example: ""
        type: string
      mint:
        description: Mint must carry ConfidentialMintBurn and ConfidentialTransferMint.
        example: ""
        type: string
      supply_ae_key:
        description: |-
          SupplyAeKey decrypts the mint's decryptable supply and encrypts the
          new one, base58-encoded raw 16 bytes.
        example: ""
        type: string
      supply_elgamal_secret_key:
        description: |-
          SupplyElgamalSecretKey is the secret key of the mint's supply ElGamal
          public key, base58-encoded raw 32 bytes.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialMintResponse:
    properties:
      auditor_ciphertext_hi:
        type: string
      auditor_ciphertext_lo:
        type: string
      auditor_elgamal_pubkey:
        type: string
      current_supply:
        type: string
      destination_elgamal_pubkey:
        type: string
      equality_proof_data:
        type: string
      new_decryptable_supply:
        type: string
      new_supply:
        type: string
      range_proof_data:
        type: string
      supply_elgamal_pubkey:
        type: string
      validity_proof_data:
        type: string
    type: object
  misc.ProveConfidentialRotateSupplyElGamalPubkeyRequest:
    properties:
      mint:
        description: Mint must carry the ConfidentialMintBurn extension.
        example: ""
        type: string
      new_supply_elgamal_pubkey:
        description: |-
          NewSupplyElgamalPubkey is the key the supply moves to, base58-encoded
          raw 32 bytes (see generate/elgamal-keypair).
        example: ""
        type: string
      supply_elgamal_secret_key:
        description: |-
          SupplyElgamalSecretKey is the secret key of the mint's current supply
          ElGamal public key, base58-encoded raw 32 bytes.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialRotateSupplyElGamalPubkeyResponse:
    properties:
      ciphertext_ciphertext_equality_proof_data:
        type: string
      current_supply:
        type: string
      current_supply_elgamal_pubkey:
        type: string
      new_supply_elgamal_pubkey:
        type: string
    type: object
  misc.ProveConfidentialTransferRequest:
    properties:
      ae_key:
        description: |-
          AeKey decrypts CurrentDecryptableAvailableBalance and encrypts the
          new one, base58-encoded raw 16-byte key.
        example: ""
        type: string
      amount:
        description: |-
          Amount is the raw base-unit count to move. It cannot exceed 2^48 - 1
          nor the source's current available balance.
        example: "250"
        type: string
      auditor_elgamal_pubkey:
        description: |-
          AuditorElgamalPubkey may be left empty for a mint with no auditor.
          Given, it must be the exact value that mint's
          ConfidentialTransferMint.auditor_elgamal_pubkey holds.
        example: ""
        type: string
      current_available_balance_ciphertext:
        description: |-
          CurrentAvailableBalanceCiphertext is the source's current available
          balance, base58-encoded raw 64-byte ElGamal ciphertext.
        example: ""
        type: string
      current_decryptable_available_balance:
        description: |-
          CurrentDecryptableAvailableBalance is the source's current available
          balance, base58-encoded raw 36-byte AE ciphertext.
        example: ""
        type: string
      destination_elgamal_pubkey:
        description: |-
          DestinationElgamalPubkey is the destination account's ElGamal public
          key, base58-encoded.
        example: ""
        type: string
      source_elgamal_secret_key:
        description: |-
          SourceElgamalSecretKey is the source account's own ElGamal secret
          key, base58-encoded. The public key is derived from it here.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialTransferResponse:
    properties:
      auditor_ciphertext_hi:
        type: string
      auditor_ciphertext_lo:
        type: string
      equality_proof_data:
        type: string
      new_source_decryptable_available_balance:
        type: string
      range_proof_data:
        type: string
      source_elgamal_pubkey:
        type: string
      validity_proof_data:
        type: string
    type: object
  misc.ProveConfidentialTransferWithFeeRequest:
    properties:
      ae_key:
        description: |-
          AeKey decrypts CurrentDecryptableAvailableBalance and encrypts the
          new one, base58-encoded raw 16-byte key.
        example: ""
        type: string
      amount:
        description: |-
          Amount is the raw base-unit count to move, before the fee. It cannot
          exceed 2^48 - 1 nor the source's current available balance.
        example: "1000"
        type: string
      current_available_balance_ciphertext:
        description: |-
          CurrentAvailableBalanceCiphertext is the source's current available
          balance, base58-encoded raw 64-byte ElGamal ciphertext.
        example: ""
        type: string
      current_decryptable_available_balance:
        description: |-
          CurrentDecryptableAvailableBalance is the source's current available
          balance, base58-encoded raw 36-byte AE ciphertext.
        example: ""
        type: string
      destination_elgamal_pubkey:
        description: |-
          DestinationElgamalPubkey is the destination account's ElGamal public
          key, base58-encoded.
        example: ""
        type: string
      mint:
        description: |-
          Mint must carry the TransferFeeConfig, ConfidentialTransferMint, and
          ConfidentialTransferFeeConfig extensions.
        example: ""
        type: string
      source_elgamal_secret_key:
        description: |-
          SourceElgamalSecretKey is the source account's own ElGamal secret
          key, base58-encoded. The public key is derived from it here.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialTransferWithFeeResponse:
    properties:
      auditor_ciphertext_hi:
        type: string
      auditor_ciphertext_lo:
        type: string
      auditor_elgamal_pubkey:
        type: string
      epoch:
        type: integer
      equality_proof_data:
        type: string
      fee:
        type: string
      fee_rate_basis_points:
        type: integer
      fee_validity_proof_data:
        type: string
      maximum_fee:
        type: string
      new_source_decryptable_available_balance:
        type: string
      percentage_with_cap_proof_data:
        type: string
      range_proof_data:
        type: string
      source_elgamal_pubkey:
        type: string
      transfer_amount_validity_proof_data:
        type: string
      withdraw_withheld_authority_elgamal_pubkey:
        type: string
    type: object
  misc.ProveConfidentialWithdrawRequest:
    properties:
      ae_key:
        description: |-
          AeKey decrypts CurrentDecryptableAvailableBalance and encrypts the
          new one, base58-encoded raw 16-byte key.
        example: ""
        type: string
      amount:
        description: |-
          Amount is the raw base-unit count to withdraw. It cannot exceed the
          account's current available balance.
        example: "5"
        type: string
      current_available_balance_ciphertext:
        description: |-
          CurrentAvailableBalanceCiphertext is the account's current available
          balance, base58-encoded raw 64-byte ElGamal ciphertext.
        example: ""
        type: string
      current_decryptable_available_balance:
        description: |-
          CurrentDecryptableAvailableBalance is the account's current available
          balance, base58-encoded raw 36-byte AE ciphertext.
        example: ""
        type: string
      elgamal_secret_key:
        description: |-
          ElgamalSecretKey is the account's own ElGamal secret key,
          base58-encoded. The public key is derived from it here.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialWithdrawResponse:
    properties:
      elgamal_pubkey:
        type: string
      equality_proof_data:
        type: string
      new_decryptable_available_balance:
        type: string
      range_proof_data:
        type: string
    type: object
  misc.ProveConfidentialWithdrawWithheldFromAccountsRequest:
    properties:
      ae_key:
        description: |-
          AeKey is the destination's own AE key, base58-encoded raw 16 bytes,
          needed to work out its new decryptable balance.
        example: ""
        type: string
      destination:
        description: |-
          Destination is the token account the fees are credited to, which must
          hold Mint and carry the ConfidentialTransferAccount extension.
        example: ""
        type: string
      mint:
        description: Mint must carry the ConfidentialTransferFeeConfig extension.
        example: ""
        type: string
      source_accounts:
        description: |-
          SourceAccounts are the token accounts to withdraw from, each holding
          Mint and carrying ConfidentialTransferFeeAmount. The same set has to
          go to the withdraw endpoint.
        items:
          type: string
        type: array
      withdraw_elgamal_secret_key:
        description: |-
          WithdrawElgamalSecretKey is the secret key of the ElGamal public key
          the mint encrypts withheld fees under, base58-encoded raw 32 bytes.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialWithdrawWithheldFromAccountsResponse:
    properties:
      ciphertext_ciphertext_equality_proof_data:
        type: string
      destination_elgamal_pubkey:
        type: string
      new_decryptable_available_balance:
        type: string
      withdraw_elgamal_pubkey:
        type: string
      withheld_amount:
        type: string
    type: object
  misc.ProveConfidentialWithdrawWithheldFromMintRequest:
    properties:
      ae_key:
        description: |-
          AeKey is the destination's own AE key, base58-encoded raw 16 bytes,
          needed to work out its new decryptable balance.
        example: ""
        type: string
      destination:
        description: |-
          Destination is the token account the fees are credited to, which must
          hold Mint and carry the ConfidentialTransferAccount extension.
        example: ""
        type: string
      mint:
        description: Mint must carry the ConfidentialTransferFeeConfig extension.
        example: ""
        type: string
      withdraw_elgamal_secret_key:
        description: |-
          WithdrawElgamalSecretKey is the secret key of the ElGamal public key
          the mint encrypts withheld fees under, base58-encoded raw 32 bytes.
        example: ""
        type: string
    type: object
  misc.ProveConfidentialWithdrawWithheldFromMintResponse:
    properties:
      ciphertext_ciphertext_equality_proof_data:
        type: string
      destination_elgamal_pubkey:
        type: string
      new_decryptable_available_balance:
        type: string
      withdraw_elgamal_pubkey:
        type: string
      withheld_amount:
        type: string
    type: object
  misc.ProvePubkeyValidityRequest:
    properties:
      secret_key:
        description: |-
          SecretKey is the ElGamal secret key, base58-encoded, exactly what
          generate/elgamal-keypair's secret_key returns.
        example: ""
        type: string
    type: object
  misc.ProvePubkeyValidityResponse:
    properties:
      proof:
        type: string
      public_key:
        type: string
    type: object
  misc.RawRequest:
    properties:
      method:
        example: getEpochInfo
        type: string
      params: {}
    type: object
  misc.RecordChunk:
    properties:
      data:
        description: Data is the chunk, base58-encoded.
        type: string
      length:
        type: integer
      offset:
        description: |-
          Offset is the record/write offset, as a string like every other
          64-bit value in this API.
        type: string
    type: object
  misc.RefreshBlockhashRequest:
    properties:
      transaction:
        type: string
    type: object
  misc.RefreshBlockhashResponse:
    properties:
      blockhash:
        type: string
      last_valid_block_height:
        type: integer
      transaction:
        type: string
    type: object
  misc.RentExemptionMintResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.RentExemptionNonceResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.RentExemptionPublicKeyRequest:
    properties:
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  misc.RentExemptionPublicKeyResponse:
    properties:
      lamports:
        type: string
      public_key:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.RentExemptionSpaceRequest:
    properties:
      space:
        example: "165"
        type: string
    type: object
  misc.RentExemptionSpaceResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.RentExemptionStakeResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.RentExemptionSystemResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.RentExemptionTokenResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.RentExemptionVoteResponse:
    properties:
      lamports:
        type: string
      sol:
        type: string
      space:
        type: integer
    type: object
  misc.ReplaceBlockhashWithNonceRequest:
    properties:
      nonce_account:
        type: string
      transaction:
        type: string
    type: object
  misc.ReplaceBlockhashWithNonceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        type: string
      message:
        type: string
      nonce_account:
        description: |-
          NonceAccount and NonceAuthority are always present here, unlike the v2
          responses where the authority doubles as the signal that a nonce was
          used at all: this endpoint does nothing else.
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      size:
        type: integer
      size_limit:
        type: integer
      transaction:
        type: string
    type: object
  misc.SendTransactionRequest:
    properties:
      transaction:
        type: string
    type: object
  misc.SendTransactionResponse:
    properties:
      signature:
        type: string
    type: object
  misc.SignRequest:
    properties:
      message:
        type: string
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  misc.SignResponse:
    properties:
      public_key:
        type: string
      signature:
        type: string
    type: object
  misc.SignTransactionRequest:
    properties:
      private_keys:
        items:
          type: string
        type: array
      public_keys:
        example:
        - EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  misc.SignTransactionResponse:
    properties:
      fully_signed:
        type: boolean
      signatures:
        items:
          type: string
        type: array
      transaction:
        type: string
      transaction_id:
        type: string
    type: object
  misc.SignatureStatusRequest:
    properties:
      signature:
        type: string
    type: object
  misc.SignatureStatusResponse:
    properties:
      confirmation_status:
        type: string
      confirmations:
        type: integer
      err:
        type: string
      failed:
        type: boolean
      found:
        type: boolean
      signature:
        type: string
      slot:
        type: integer
    type: object
  misc.SimulateTransactionRequest:
    properties:
      transaction:
        type: string
    type: object
  misc.SimulateTransactionResponse:
    properties:
      err:
        type: string
      failed:
        type: boolean
      logs:
        items:
          type: string
        type: array
      units_consumed:
        type: integer
    type: object
  misc.SlotResponse:
    properties:
      slot:
        type: integer
    type: object
  misc.SplitRecordChunksRequest:
    properties:
      chunk_size:
        description: |-
          ChunkSize is how many bytes each record/write carries. Left empty or
          zero it is 900, which fits a transaction with or without a durable
          nonce. It cannot exceed 1000.
        example: 900
        type: integer
      data:
        description: |-
          Data is the bytes to split, base58-encoded -- typically a proof_data
          blob from one of the tool/prove endpoints.
        example: ""
        type: string
    type: object
  misc.SplitRecordChunksResponse:
    properties:
      chunks:
        items:
          $ref: '#/definitions/misc.RecordChunk'
        type: array
      proof_offset:
        description: |-
          ProofOffset is where the data starts within the record account:
          33, the record header's length. It is the proof_offset a
          verify-from-account request needs when Data is a proof.
        type: integer
      total_length:
        description: |-
          TotalLength is len(Data) in bytes -- the data_length to create the
          record account with (record/create-account).
        type: integer
    type: object
  misc.VerifyRequest:
    properties:
      message:
        type: string
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      signature:
        type: string
    type: object
  misc.VerifyResponse:
    properties:
      valid:
        type: boolean
    type: object
  misc.VersionResponse:
    properties:
      version:
        additionalProperties: {}
        type: object
    type: object
  token.ATADeriveRequest:
    properties:
      mint:
        example: ""
        type: string
      owner:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
    type: object
  token.ATADeriveResponse:
    properties:
      associated_token_account:
        type: string
      bump:
        type: integer
      mint:
        type: string
      owner:
        type: string
      program:
        type: string
    type: object
  token.ATAValidateRequest:
    properties:
      associated_token_account:
        example: ""
        type: string
      mint:
        example: ""
        type: string
      owner:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
    type: object
  token.ATAValidateResponse:
    properties:
      associated_token_account:
        type: string
      bump:
        type: integer
      derived:
        type: string
      mint:
        type: string
      owner:
        type: string
      program:
        type: string
      valid:
        type: boolean
    type: object
  token.AccountRequest:
    properties:
      token_account:
        example: 4qRgcVrSqs43Jy9n8w7H5EJh8FnRiTFnABjE37Dd3Ae5
        type: string
    type: object
  token.AccountResponse:
    properties:
      amount:
        type: string
      amount_ui:
        type: string
      close_authority:
        type: string
      decimals:
        type: integer
      delegate:
        type: string
      delegated_amount:
        type: string
      exists:
        type: boolean
      frozen:
        type: boolean
      mint:
        type: string
      native:
        type: boolean
      owner:
        type: string
      program:
        type: string
      public_key:
        type: string
      rent_reserve:
        type: string
      state:
        type: string
    type: object
  token.AmountToUiRequest:
    properties:
      amount:
        example: "1000000"
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
    type: object
  token.AmountToUiResponse:
    properties:
      amount:
        type: string
      mint:
        type: string
      program:
        type: string
      ui_amount:
        type: string
    type: object
  token.GetAccountDataSizeRequest:
    properties:
      extension_types:
        description: |-
          ExtensionTypes may be empty, in which case the response is the bare
          size a Token-2022 account with no extensions needs. Each name is the
          same lowercase-with-underscores form reallocate takes. Unlike
          MintDataSizeRequest, nothing here is rejected client-side for being a
          mint-only type (e.g. "transfer_fee_config") -- the deployed program
          itself is what checks that, as ExtensionTypeMismatch.

          Valid names: "transfer_fee_config", "transfer_fee_amount",
          "mint_close_authority", "confidential_transfer_mint",
          "confidential_transfer_account", "default_account_state",
          "immutable_owner", "memo_transfer", "non_transferable",
          "interest_bearing_config", "cpi_guard", "permanent_delegate",
          "non_transferable_account", "transfer_hook", "transfer_hook_account",
          "confidential_transfer_fee_config", "confidential_transfer_fee_amount",
          "metadata_pointer", "token_metadata", "group_pointer", "token_group",
          "group_member_pointer", "token_group_member", "confidential_mint_burn",
          "scaled_ui_amount", "pausable", "pausable_account",
          "permissioned_burn".
        example:
        - immutable_owner
        items:
          type: string
        type: array
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      program:
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
    type: object
  token.GetAccountDataSizeResponse:
    properties:
      extension_types:
        items:
          type: string
        type: array
      mint:
        type: string
      program:
        type: string
      size:
        type: string
    type: object
  token.MintDataSizeRequest:
    properties:
      extension_types:
        description: |-
          ExtensionTypes must each be a mint extension, not a token-account
          one (e.g. "transfer_fee_amount" is rejected here the same way the
          deployed program itself would reject it on chain, just with no chain
          round trip needed to find out). "token_metadata" is also rejected:
          its size depends on actual name/symbol/uri content this endpoint
          never sees.

          Valid names: "transfer_fee_config", "mint_close_authority",
          "confidential_transfer_mint", "default_account_state",
          "non_transferable", "interest_bearing_config", "permanent_delegate",
          "transfer_hook", "confidential_transfer_fee_config",
          "metadata_pointer", "group_pointer", "token_group",
          "group_member_pointer", "token_group_member", "confidential_mint_burn",
          "scaled_ui_amount", "pausable".
        example:
        - transfer_fee_config
        items:
          type: string
        type: array
    type: object
  token.MintDataSizeResponse:
    properties:
      extension_types:
        items:
          type: string
        type: array
      size:
        type: string
    type: object
  token.MintRequest:
    properties:
      mint_account:
        example: EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
        type: string
    type: object
  token.MintResponse:
    properties:
      decimals:
        type: integer
      exists:
        type: boolean
      freezable:
        type: boolean
      freeze_authority:
        type: string
      initialized:
        type: boolean
      mint_authority:
        type: string
      mintable:
        type: boolean
      program:
        type: string
      public_key:
        type: string
      supply:
        type: string
      supply_ui:
        type: string
    type: object
  token.UiToAmountRequest:
    properties:
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      ui_amount:
        example: "1.5"
        type: string
    type: object
  token.UiToAmountResponse:
    properties:
      amount:
        type: string
      mint:
        type: string
      program:
        type: string
      ui_amount:
        type: string
    type: object
  v1.Account:
    properties:
      is_signer:
        example: true
        type: boolean
      is_writable:
        example: true
        type: boolean
      public_key:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v1.BuildTransactionRequest:
    properties:
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      instructions:
        items:
          $ref: '#/definitions/v1.Instruction'
        type: array
      recent_blockhash:
        description: |-
          RecentBlockhash may be left empty, in which case it is fetched from the
          cluster. Supplying it makes the call deterministic, which is what allows
          the same request to reproduce identical bytes.
        example: ""
        type: string
    type: object
  v1.BuildTransactionResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      header:
        $ref: '#/definitions/v1.Header'
      message:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      size:
        type: integer
      transaction:
        type: string
    type: object
  v1.Header:
    properties:
      num_readonly_signed_accounts:
        type: integer
      num_readonly_unsigned_accounts:
        type: integer
      num_required_signatures:
        type: integer
    type: object
  v1.Instruction:
    properties:
      accounts:
        items:
          $ref: '#/definitions/v1.Account'
        type: array
      data:
        example: AgAAAECcAAAAAAAA
        type: string
      program_id:
        example: "11111111111111111111111111111111"
        type: string
    type: object
  v2.ATARecoverNestedRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as Wallet.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      nested_mint:
        description: |-
          NestedMint is the mint of the nested account itself, the one being
          recovered and closed. It must differ from OwnerMint.
        example: ""
        type: string
      owner_mint:
        description: |-
          OwnerMint is the mint of the associated account that was mistakenly
          used as a wallet — deriving Wallet's associated account for OwnerMint
          is what produced the address the nested account was wrongly created
          under.
        example: ""
        type: string
      program:
        description: |-
          Program is a seed of every derived address, so one wallet has
          different owner, nested, and destination accounts for classic Token
          than for Token-2022 over the same mints.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      wallet:
        description: |-
          Wallet is the real owner, and the only one who signs: closing the
          nested account pays its reclaimed lamports back to Wallet's own
          associated account for OwnerMint, so only the real owner may authorize
          that.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ATARecoverNestedResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      destination:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nested_account:
        type: string
      nested_mint:
        type: string
      nonce_authority:
        type: string
      owner_account:
        type: string
      owner_mint:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      wallet:
        type: string
    type: object
  v2.ApplyPendingBalanceRequest:
    properties:
      account:
        description: |-
          Account is the token account to apply. It must already carry the
          ConfidentialTransferAccount extension (see configure-account).
        example: ""
        type: string
      ae_key:
        description: |-
          AeKey encrypts NewAvailableBalance, base58-encoded -- a raw 16-byte
          AES-128-GCM-SIV key, not a Solana address (see
          tool/derive/ae-key-seed-message and tool/derive/ae-key), the same
          key Account's own decryptable_zero_balance was set up under.
        example: ""
        type: string
      authority:
        description: |-
          Authority is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      expected_pending_balance_credit_counter:
        description: |-
          ExpectedPendingBalanceCreditCounter is how many pending-balance
          credits (deposits and incoming transfers) landed on Account since
          its last apply. The program rejects a mismatched count rather than
          silently applying a different set of credits than the caller
          believes it is catching up on.
        example: "1"
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_available_balance:
        description: |-
          NewAvailableBalance is the total available balance Account should
          hold once this apply lands, in raw base units -- not a delta, the
          full new total. AeKey encrypts it into the wire value this
          instruction actually carries.
        example: "10"
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ApplyPendingBalanceResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      expected_pending_balance_credit_counter:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      new_available_balance:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ApproveAccountRequest:
    properties:
      account:
        description: |-
          Account is the token account to approve. It must already carry the
          ConfidentialTransferAccount extension (see configure-account).
        example: ""
        type: string
      authority:
        description: |-
          Authority is the authority initialize (or update) named, or its
          multisig for a multisig-owned authority (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: Mint must already carry the ConfidentialTransferMint extension.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ApproveAccountResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ApproveCheckedMaxRequest:
    properties:
      delegate:
        description: |-
          Delegate is who may spend from TokenAccount going forward, up to the
          maximum representable amount, replacing any prior delegation entirely
          rather than adding to it.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: Mint is what TokenAccount must hold.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is debited if the delegation is ever spent. It must
          already exist and hold Mint.
        example: ""
        type: string
      token_account_owner:
        description: |-
          TokenAccountOwner must be TokenAccount's owner, never an existing
          delegate: re-delegating would let a delegate hand its own spending
          rights to a third party the owner never chose.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ApproveCheckedMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      decimals:
        type: integer
      delegate:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      max_amount:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_owner:
        type: string
      transaction:
        type: string
    type: object
  v2.ApproveCheckedRequest:
    properties:
      amount:
        description: |-
          Amount is the raw base-unit count Delegate may spend, not a UI decimal
          string.
        example: "500000"
        type: string
      decimals:
        description: |-
          Decimals is checked against Mint's own stored value rather than
          trusted, which is the whole point of the checked variant: catching a
          client that formatted Amount against the wrong decimals as a 400
          instead of an on-chain failure.
        example: 6
        type: integer
      delegate:
        description: |-
          Delegate is who may spend up to Amount from TokenAccount going
          forward, replacing any prior delegation entirely rather than adding to
          it.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what TokenAccount must hold, and is the source of the decimals
          checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is debited if the delegation is ever spent. It must
          already exist and hold Mint.
        example: ""
        type: string
      token_account_owner:
        description: |-
          TokenAccountOwner must be TokenAccount's owner, never an existing
          delegate: re-delegating would let a delegate hand its own spending
          rights to a third party the owner never chose.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ApproveCheckedResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      delegate:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_owner:
        type: string
      transaction:
        type: string
    type: object
  v2.ApproveMaxRequest:
    properties:
      delegate:
        example: ""
        type: string
      durable_nonce_account:
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        items:
          type: string
        type: array
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        example: ""
        type: string
      token_account:
        example: ""
        type: string
      token_account_owner:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ApproveMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      delegate:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      max_amount:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_owner:
        type: string
      transaction:
        type: string
    type: object
  v2.ApproveRequest:
    properties:
      amount:
        description: |-
          Amount is the raw base-unit count Delegate may spend, not a UI decimal
          string.
        example: "500000"
        type: string
      delegate:
        description: |-
          Delegate is who may spend up to Amount from TokenAccount going
          forward, replacing any prior delegation entirely rather than adding to
          it.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is debited if the delegation is ever spent. It must
          already exist.
        example: ""
        type: string
      token_account_owner:
        description: |-
          TokenAccountOwner must be TokenAccount's owner, never an existing
          delegate: re-delegating would let a delegate hand its own spending
          rights to a third party the owner never chose.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ApproveResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      delegate:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_owner:
        type: string
      transaction:
        type: string
    type: object
  v2.BurnCheckedMaxRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. Burning moves no lamports
          of its own, so this is the only balance this endpoint ever checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what TokenAccount must hold, and is the source of the decimals
          checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is debited and never credited elsewhere: burning destroys
          supply rather than moving it. It must already exist and hold Mint.
        example: ""
        type: string
      token_account_authority:
        description: |-
          TokenAccountAuthority is TokenAccount's owner, or its delegate for no
          more than what was delegated — checked against the balance actually
          swept, resolved at request time, not a caller-given amount.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.BurnCheckedMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.BurnCheckedRequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to destroy, not a UI decimal
          string.
        example: "1000"
        type: string
      decimals:
        description: |-
          Decimals is checked against Mint's own stored value rather than
          trusted, which is the whole point of the checked variant: catching a
          client that formatted Amount against the wrong decimals as a 400
          instead of an on-chain failure.
        example: 6
        type: integer
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. Burning moves no lamports
          of its own, so this is the only balance this endpoint ever checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what TokenAccount must hold, and is the source of the decimals
          checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is debited and never credited elsewhere: burning destroys
          supply rather than moving it. It must already exist and hold Mint.
        example: ""
        type: string
      token_account_authority:
        description: |-
          TokenAccountAuthority is TokenAccount's owner, or its delegate for no
          more than what was delegated. Burning spends a balance, so it is the
          holder's to authorize, not the mint's — unlike minting, which checks
          the mint's own authority instead.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.BurnCheckedResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.BurnMaxRequest:
    properties:
      durable_nonce_account:
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      multisig_signers:
        items:
          type: string
        type: array
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        example: ""
        type: string
      token_account:
        example: ""
        type: string
      token_account_authority:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.BurnMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.BurnRequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to destroy, not a UI decimal
          string.
        example: "1000"
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: Mint is what TokenAccount must hold.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is debited and never credited elsewhere: burning destroys
          supply rather than moving it. It must already exist and hold Mint.
        example: ""
        type: string
      token_account_authority:
        description: |-
          TokenAccountAuthority is TokenAccount's owner, or its delegate for no
          more than what was delegated.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.BurnResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.CloseAccountRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount receives TokenAccount's entire reclaimed lamport
          balance. It must already exist; this is not a way to bring a new
          account into existence.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      token_account:
        description: |-
          TokenAccount is closed. It must already hold no tokens (a wrapped SOL
          account is the one exception, closed to unwrap its lamport balance).
        example: ""
        type: string
      token_account_close_authority:
        description: |-
          TokenAccountCloseAuthority is checked against
          close_authority.unwrap_or(owner): once a close authority is set on
          TokenAccount, it alone may close it, and the owner who set it can no
          longer do so directly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.CloseAccountResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      recipient_account:
        type: string
      reclaimed:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Reclaimed reports TokenAccount's balance at the moment it was read,
          which is what closing hands to recipient_account. It can change
          between this response and the transaction landing if anything else
          touches the account first.
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_close_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.CloseMintRequest:
    properties:
      close_authority:
        description: |-
          CloseAuthority must be Mint's current MintCloseAuthority close
          authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is closed. It must already carry the MintCloseAuthority extension
          with an authority set, and its supply must already be zero.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount receives Mint's entire reclaimed lamport balance. It
          must already exist; this is not a way to bring a new account into
          existence.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.CloseMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      close_authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      recipient_account:
        type: string
      reclaimed:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Reclaimed reports Mint's balance at the moment it was read, which is
          what closing hands to recipient_account. It can change between this
          response and the transaction landing if anything else touches the
          account first.
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialApplyPendingBurnRequest:
    properties:
      authority:
        description: |-
          Authority is the mint's mint authority, or its multisig for a
          multisig-owned one (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that mint stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the mint, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: Mint must carry the ConfidentialMintBurn extension.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ConfidentialApplyPendingBurnResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialBurnRequest:
    properties:
      account:
        description: |-
          Account is debited (its available balance). It must already carry the
          ConfidentialTransferAccount extension and hold at least the amount.
        example: ""
        type: string
      auditor_ciphertext_hi:
        description: |-
          AuditorCiphertextHi is tool/prove/confidential-burn's own
          auditor_ciphertext_hi, base58-encoded (64 bytes).
        example: ""
        type: string
      auditor_ciphertext_lo:
        description: |-
          AuditorCiphertextLo is tool/prove/confidential-burn's own
          auditor_ciphertext_lo, base58-encoded (64 bytes).
        example: ""
        type: string
      authority:
        description: |-
          Authority is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners) -- not the mint authority.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      ciphertext_validity_context_state_account:
        description: |-
          CiphertextValidityContextStateAccount holds the verified
          BatchedGroupedCiphertext3HandlesValidity proof's context (see
          context-state/verify/batched-grouped-ciphertext-3-handles-validity).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCommitmentEquality proof's context (see
          context-state/verify/ciphertext-commitment-equality).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must carry ConfidentialBurnBurn and ConfidentialTransferMint, and
          is what Account holds.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_decryptable_available_balance:
        description: |-
          NewDecryptableAvailableBalance is tool/prove/confidential-burn's own
          new_decryptable_available_balance, base58-encoded (36 bytes).
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      range_proof_context_state_account:
        description: |-
          RangeProofContextStateAccount holds the verified BatchedRangeProofU128
          proof's context (see context-state/verify/batched-range-proof-u128).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ConfidentialBurnResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      ciphertext_validity_context_state_account:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      range_proof_context_state_account:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialConfigureAccountWithRegistryRequest:
    properties:
      account:
        description: |-
          Account must be a Token-2022 token account of a mint that carries the
          ConfidentialTransferMint extension, and must not already carry
          ConfidentialTransferAccount.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer is optional. Named, it signs and lets the program resize Account
          itself to fit the confidential extension (and the confidential fee
          extension on a fee mint), paying any rent shortfall. Left empty, Account
          must already have room for them (see reallocate).
        example: ""
        type: string
    type: object
  v2.ConfidentialConfigureAccountWithRegistryResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      registry_account:
        type: string
      rent_payer:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialEmptyAccountRequest:
    properties:
      account:
        description: |-
          Account must already carry the ConfidentialTransferAccount extension
          (see configure-account).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      zero_ciphertext_context_state_account:
        description: |-
          ZeroCiphertextContextStateAccount holds the verified ZeroCiphertext
          proof's context (see context-state/verify/zero-ciphertext).
        example: ""
        type: string
    type: object
  v2.ConfidentialEmptyAccountResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      zero_ciphertext_context_state_account:
        type: string
    type: object
  v2.ConfidentialHarvestWithheldTokensToMintRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee. Nothing else signs.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must carry the ConfidentialTransferFeeConfig extension with
          harvest_to_mint_enabled set (see enable-harvest-to-mint).
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_accounts:
        description: |-
          SourceAccounts are the token accounts to harvest from, each holding
          Mint. There is no limit here beyond what fits in one transaction, which
          this endpoint checks.
        items:
          type: string
        type: array
    type: object
  v2.ConfidentialHarvestWithheldTokensToMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      harvested_sources:
        description: |-
          HarvestedSources are the source accounts that carry both
          TransferFeeAmount and ConfidentialTransferAccount, so the program
          actually moves their withheld fees. SkippedSources do not, and the
          program passes over them silently.
        items:
          type: string
        type: array
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      skipped_sources:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialMintRequest:
    properties:
      account:
        description: |-
          Account is credited (its pending balance). It must already carry the
          ConfidentialTransferAccount extension.
        example: ""
        type: string
      auditor_ciphertext_hi:
        description: |-
          AuditorCiphertextHi is tool/prove/confidential-mint's own
          auditor_ciphertext_hi, base58-encoded (64 bytes).
        example: ""
        type: string
      auditor_ciphertext_lo:
        description: |-
          AuditorCiphertextLo is tool/prove/confidential-mint's own
          auditor_ciphertext_lo, base58-encoded (64 bytes).
        example: ""
        type: string
      authority:
        description: |-
          Authority is the mint's mint authority, or its multisig for a
          multisig-owned one (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      ciphertext_validity_context_state_account:
        description: |-
          CiphertextValidityContextStateAccount holds the verified
          BatchedGroupedCiphertext3HandlesValidity proof's context (see
          context-state/verify/batched-grouped-ciphertext-3-handles-validity).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCommitmentEquality proof's context (see
          context-state/verify/ciphertext-commitment-equality).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must carry ConfidentialMintBurn and ConfidentialTransferMint, and
          is what Account holds.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_decryptable_supply:
        description: |-
          NewDecryptableSupply is tool/prove/confidential-mint's own field of the
          same name, base58-encoded (36 bytes).
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      range_proof_context_state_account:
        description: |-
          RangeProofContextStateAccount holds the verified BatchedRangeProofU128
          proof's context (see context-state/verify/batched-range-proof-u128).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ConfidentialMintResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      ciphertext_validity_context_state_account:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      range_proof_context_state_account:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialRotateSupplyElGamalPubkeyRequest:
    properties:
      authority:
        description: |-
          Authority is the mint's mint authority, or its multisig for a
          multisig-owned one (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that mint stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the mint, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCiphertextEquality proof's context.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: Mint must carry the ConfidentialMintBurn extension.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_supply_elgamal_pubkey:
        description: |-
          NewSupplyElgamalPubkey is the key the supply moves to, base58-encoded raw
          32 bytes -- the same one the prove tool was given.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ConfidentialRotateSupplyElGamalPubkeyResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      new_supply_elgamal_pubkey:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialTransferRequest:
    properties:
      auditor_ciphertext_hi:
        description: |-
          AuditorCiphertextHi is tool/prove/confidential-transfer's own
          auditor_ciphertext_hi, base58-encoded (64 bytes).
        example: ""
        type: string
      auditor_ciphertext_lo:
        description: |-
          AuditorCiphertextLo is tool/prove/confidential-transfer's own
          auditor_ciphertext_lo, base58-encoded (64 bytes).
        example: ""
        type: string
      ciphertext_validity_context_state_account:
        description: |-
          CiphertextValidityContextStateAccount holds the verified
          BatchedGroupedCiphertext3HandlesValidity proof's context (see
          context-state/verify/batched-grouped-ciphertext-3-handles-validity).
        example: ""
        type: string
      destination:
        description: |-
          Destination is credited. It must already carry the
          ConfidentialTransferAccount extension.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCommitmentEquality proof's context (see
          context-state/verify/ciphertext-commitment-equality).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what both Source and Destination must hold, and must
          already carry the ConfidentialTransferMint extension.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty,
          Owner itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_source_decryptable_available_balance:
        description: |-
          NewSourceDecryptableAvailableBalance is tool/prove/confidential-transfer's
          own field of the same name, base58-encoded (36 bytes).
        example: ""
        type: string
      owner:
        description: |-
          Owner is Source's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      range_proof_context_state_account:
        description: |-
          RangeProofContextStateAccount holds the verified BatchedRangeProofU128
          proof's context (see context-state/verify/batched-range-proof-u128).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source:
        description: |-
          Source is debited. It must already carry the
          ConfidentialTransferAccount extension.
        example: ""
        type: string
    type: object
  v2.ConfidentialTransferResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      ciphertext_validity_context_state_account:
        type: string
      destination:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      range_proof_context_state_account:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source:
        type: string
      transaction:
        type: string
    type: object
  v2.ConfidentialTransferWithFeeRequest:
    properties:
      auditor_ciphertext_hi:
        description: |-
          AuditorCiphertextHi is tool/prove/confidential-transfer's own
          auditor_ciphertext_hi, base58-encoded (64 bytes).
        example: ""
        type: string
      auditor_ciphertext_lo:
        description: |-
          AuditorCiphertextLo is tool/prove/confidential-transfer's own
          auditor_ciphertext_lo, base58-encoded (64 bytes).
        example: ""
        type: string
      destination:
        description: |-
          Destination is credited. It must already carry the
          ConfidentialTransferAccount extension.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCommitmentEquality proof's context (see
          context-state/verify/ciphertext-commitment-equality).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      fee_sigma_context_state_account:
        description: |-
          FeeSigmaContextStateAccount holds the verified PercentageWithCap
          proof's context (see context-state/verify/percentage-with-cap).
        example: ""
        type: string
      fee_validity_context_state_account:
        description: |-
          FeeValidityContextStateAccount holds the verified
          BatchedGroupedCiphertext2HandlesValidity proof's context for the fee
          (see context-state/verify/batched-grouped-ciphertext-2-handles-validity).
        example: ""
        type: string
      mint:
        description: |-
          Mint is what both Source and Destination must hold, and must
          already carry the ConfidentialTransferMint extension.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty,
          Owner itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_source_decryptable_available_balance:
        description: |-
          NewSourceDecryptableAvailableBalance is tool/prove/confidential-transfer's
          own field of the same name, base58-encoded (36 bytes).
        example: ""
        type: string
      owner:
        description: |-
          Owner is Source's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      range_proof_context_state_account:
        description: |-
          RangeProofContextStateAccount holds the verified BatchedRangeProofU256
          proof's context (see context-state/verify/batched-range-proof-u256).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source:
        description: |-
          Source is debited. It must already carry the
          ConfidentialTransferAccount extension.
        example: ""
        type: string
      transfer_amount_validity_context_state_account:
        description: |-
          TransferAmountValidityContextStateAccount holds the verified
          BatchedGroupedCiphertext3HandlesValidity proof's context for the
          transfer amount (see
          context-state/verify/batched-grouped-ciphertext-3-handles-validity).
        example: ""
        type: string
    type: object
  v2.ConfidentialTransferWithFeeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      destination:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      fee_sigma_context_state_account:
        type: string
      fee_validity_context_state_account:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      range_proof_context_state_account:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source:
        type: string
      transaction:
        type: string
      transfer_amount_validity_context_state_account:
        type: string
    type: object
  v2.ConfidentialUpdateDecryptableSupplyRequest:
    properties:
      authority:
        description: |-
          Authority is the mint's mint authority, or its multisig for a
          multisig-owned one (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that mint stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the mint, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: Mint must carry the ConfidentialMintBurn extension.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_supply:
        description: |-
          NewSupply is the confidential supply the decryptable supply should now
          equal, in raw base units. The program cannot check it against the
          confidential supply, so it has to be the value the caller knows the
          supply to be (see the ElGamal decryption of the mint's
          confidential_supply).
        example: "600"
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      supply_ae_key:
        description: |-
          SupplyAeKey encrypts NewSupply into the decryptable supply the
          instruction carries, base58-encoded raw 16 bytes -- the key the mint was
          initialized with.
        example: ""
        type: string
    type: object
  v2.ConfidentialUpdateDecryptableSupplyResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      new_decryptable_supply:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialWithdrawRequest:
    properties:
      account:
        description: |-
          Account is debited from its confidential available balance and
          credited to its public balance. It must already carry the
          ConfidentialTransferAccount extension.
        example: ""
        type: string
      amount:
        description: |-
          Amount is the raw base-unit count to withdraw, not a UI decimal
          string. It must be the same amount tool/prove/confidential-withdraw
          built its proofs for.
        example: "5"
        type: string
      decimals:
        description: |-
          Decimals must equal Mint's own, the same check every other
          *Checked-shaped instruction in this API runs rather than trusting
          the caller's figure.
        example: 0
        type: integer
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCommitmentEquality proof's context (see
          context-state/verify/ciphertext-commitment-equality).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what Account must hold, and is the source of the decimals
          checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty,
          Owner itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_decryptable_available_balance:
        description: |-
          NewDecryptableAvailableBalance is tool/prove/confidential-withdraw's
          own new_decryptable_available_balance, base58-encoded (36 bytes).
        example: ""
        type: string
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      range_proof_context_state_account:
        description: |-
          RangeProofContextStateAccount holds the verified BatchedRangeProofU64
          proof's context (see context-state/verify/batched-range-proof-u64).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ConfidentialWithdrawResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      range_proof_context_state_account:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialWithdrawWithheldTokensFromAccountsRequest:
    properties:
      authority:
        description: |-
          Authority is the TransferFeeConfig's withdraw withheld authority, or
          its multisig for a multisig-owned one (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      destination:
        description: |-
          Destination is the token account credited, which must hold Mint and
          carry the ConfidentialTransferAccount extension. It may be any such
          account, including the sender's own.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCiphertextEquality proof's context (see
          context-state/verify/ciphertext-ciphertext-equality).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must carry the TransferFeeConfig and ConfidentialTransferFeeConfig
          extensions.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_decryptable_available_balance:
        description: |-
          NewDecryptableAvailableBalance is the destination's available balance
          after the credit, base58-encoded raw 36-byte AE ciphertext -- the
          tool/prove endpoint's new_decryptable_available_balance.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_accounts:
        description: |-
          SourceAccounts are the token accounts to withdraw from, each holding
          Mint and carrying ConfidentialTransferFeeAmount. Their order does not
          matter, but the proof was built from exactly this set, so it must be
          the same one given to the prove tool.
        items:
          type: string
        type: array
    type: object
  v2.ConfidentialWithdrawWithheldTokensFromAccountsResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      destination:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_accounts:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfidentialWithdrawWithheldTokensFromMintRequest:
    properties:
      authority:
        description: |-
          Authority is the TransferFeeConfig's withdraw withheld authority, or
          its multisig for a multisig-owned one (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      destination:
        description: |-
          Destination is the token account credited, which must hold Mint and
          carry the ConfidentialTransferAccount extension. It may be any such
          account, including the sender's own.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      equality_context_state_account:
        description: |-
          EqualityContextStateAccount holds the verified
          CiphertextCiphertextEquality proof's context (see
          context-state/verify/ciphertext-ciphertext-equality).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must carry the TransferFeeConfig and ConfidentialTransferFeeConfig
          extensions.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_decryptable_available_balance:
        description: |-
          NewDecryptableAvailableBalance is the destination's available balance
          after the credit, base58-encoded raw 36-byte AE ciphertext -- the
          tool/prove endpoint's new_decryptable_available_balance.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ConfidentialWithdrawWithheldTokensFromMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      destination:
        type: string
      equality_context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ConfigureAccountRequest:
    properties:
      account:
        description: |-
          Account is the token account this attaches to. It must already
          exist, be owned by Program, and already hold enough space (see
          extensions/confidential-transfer-account/reallocate).
        example: ""
        type: string
      ae_key:
        description: |-
          AeKey encrypts Account's decryptable_zero_balance (always zero -- a
          freshly configured account has no balance yet), base58-encoded --
          a raw 16-byte AES-128-GCM-SIV key, not a Solana address. Upstream
          derives this by signing a fixed message with the account owner's
          real wallet key, which this API never holds; the caller derives it
          themselves and supplies the raw bytes.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      elgamal_pubkey:
        description: |-
          ElgamalPubkey is the ElGamal public key Account's confidential
          balance will be encrypted under, base58-encoded -- a raw 32-byte
          ristretto255 value, not a Solana address (see
          tool/generate/elgamal-keypair).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      maximum_pending_balance_credit_counter:
        description: |-
          MaximumPendingBalanceCreditCounter caps how many deposits and
          transfers Account can receive before apply-pending-balance (not yet
          built) has to run before it can receive more.
        example: "65536"
        type: string
      mint:
        description: |-
          Mint must already carry the ConfidentialTransferMint extension (see
          extensions/confidential-transfer-mint/initialize).
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      pubkey_proof:
        description: |-
          PubkeyProof proves whoever built this request knows the secret key
          ElgamalPubkey was derived from, base58-encoded (see
          tool/prove/pubkey-validity).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ConfigureAccountResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      elgamal_pubkey:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      maximum_pending_balance_credit_counter:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateCloseRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is closed. It must be owned by the ZkElgamalProof
          program and its recorded authority must be ContextStateAccountOwner.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is the authority recorded when the account
          was verified into (see context-state/verify). It signs.
        example: ""
        type: string
      destination:
        description: Destination receives the account's lamports.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateCloseResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      destination:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      reclaimed_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateCreateBatchedGroupedCiphertext2HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a BatchedGroupedCiphertext2HandlesValidity context (289 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateBatchedGroupedCiphertext2HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateBatchedGroupedCiphertext3HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a BatchedGroupedCiphertext3HandlesValidity
          context (385 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateBatchedGroupedCiphertext3HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateBatchedRangeProofU64Request:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a BatchedRangeProofU64 context (297 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateBatchedRangeProofU64Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateBatchedRangeProofU128Request:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a BatchedRangeProofU128 context (297
          bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateBatchedRangeProofU128Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateBatchedRangeProofU256Request:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a BatchedRangeProofU256 context (297 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateBatchedRangeProofU256Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateCiphertextCiphertextEqualityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a CiphertextCiphertextEquality context (225 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateCiphertextCiphertextEqualityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateCiphertextCommitmentEqualityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a CiphertextCommitmentEquality context
          (161 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateCiphertextCommitmentEqualityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateGroupedCiphertext2HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a GroupedCiphertext2HandlesValidity context (193 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateGroupedCiphertext2HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateGroupedCiphertext3HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a GroupedCiphertext3HandlesValidity context (257 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateGroupedCiphertext3HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreatePercentageWithCapRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a PercentageWithCap context (137 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreatePercentageWithCapResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreatePubkeyValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a PubkeyValidity context (65 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreatePubkeyValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateCreateZeroCiphertextRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount is the account created. It signs alongside
          RentPayer, since an address does not exist until whoever holds its
          private key authorizes its creation. It must not already exist.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds ContextStateAccount's creation for exactly the
          rent-exemption minimum for a ZeroCiphertext context (129 bytes).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ContextStateCreateZeroCiphertextResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyBatchedGroupedCiphertext2HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/batched-grouped-ciphertext-2-handles-validity.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full BatchedGroupedCiphertext2HandlesValidityProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyBatchedGroupedCiphertext2HandlesValidity instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyBatchedGroupedCiphertext2HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyBatchedGroupedCiphertext3HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/batched-grouped-ciphertext-3-handles-validity.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is zkbridge.ProveBatchedGroupedCiphertext3HandlesValidity's own output (544 bytes),
          base58-encoded -- context then proof, packed exactly as the
          deployed program's VerifyBatchedGroupedCiphertext3HandlesValidity instruction expects.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyBatchedGroupedCiphertext3HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyBatchedRangeProofU64Request:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/batched-range-proof-u64.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full BatchedRangeProofU64ProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyBatchedRangeProofU64 instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyBatchedRangeProofU64Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyBatchedRangeProofU128Request:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/batched-range-proof-u128.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is zkbridge.ProveBatchedRangeProofU128's own output (1000 bytes),
          base58-encoded -- context then proof, packed exactly as the
          deployed program's VerifyBatchedRangeProofU128 instruction expects.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyBatchedRangeProofU128Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyBatchedRangeProofU256Request:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/batched-range-proof-u256.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full BatchedRangeProofU256ProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyBatchedRangeProofU256 instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyBatchedRangeProofU256Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyCiphertextCiphertextEqualityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/ciphertext-ciphertext-equality.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full CiphertextCiphertextEqualityProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyCiphertextCiphertextEquality instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyCiphertextCiphertextEqualityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyCiphertextCommitmentEqualityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/ciphertext-commitment-equality.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is zkbridge.ProveCiphertextCommitmentEquality's own output (320 bytes),
          base58-encoded -- context then proof, packed exactly as the
          deployed program's VerifyCiphertextCommitmentEquality instruction expects.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyCiphertextCommitmentEqualityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext2HandlesValidityRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext2HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext3HandlesValidityRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext3HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedRangeProofU64Request:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedRangeProofU64Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedRangeProofU128Request:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedRangeProofU128Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedRangeProofU256Request:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountBatchedRangeProofU256Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountCiphertextCiphertextEqualityRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountCiphertextCiphertextEqualityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountCiphertextCommitmentEqualityRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountCiphertextCommitmentEqualityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountGroupedCiphertext2HandlesValidityRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountGroupedCiphertext2HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountGroupedCiphertext3HandlesValidityRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountGroupedCiphertext3HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountPercentageWithCapRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountPercentageWithCapResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountPubkeyValidityRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountPubkeyValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyFromAccountZeroCiphertextRequest:
    properties:
      compute_unit_limit:
        description: |-
          ComputeUnitLimit is the compute-unit limit the transaction is given, in
          a ComputeBudget SetComputeUnitLimit instruction placed ahead of the
          verify. The ZkElgamalProof program charges a fixed cost per proof
          type before it verifies anything (range u128 costs the entire default
          200,000 and u256 costs 368,000), so without a raised limit the larger
          proofs fail with ComputationalBudgetExceeded. Left empty or zero it is
          this proof type's own cost plus a margin, rounded up to the next
          thousand. It cannot exceed 1,400,000, the most a transaction may have.
        example: 0
        type: integer
      context_state_account:
        description: ContextStateAccount already exists, created via context-state/create.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the key
          context-state/close will later require a signature from. It does not sign
          here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_account:
        description: |-
          ProofAccount holds the proof data, already written (for example with
          record/write). Nothing checks who owns it -- the program only reads the
          bytes -- but it has to be large enough to hold offset plus the proof.
        example: ""
        type: string
      proof_offset:
        description: |-
          ProofOffset is where the proof data starts within ProofAccount's data, up to
          4294967295. For a record account that is 33 plus wherever the proof was
          written, so 33 for a proof written from offset 0.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyFromAccountZeroCiphertextResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      proof_account:
        type: string
      proof_offset:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyGroupedCiphertext2HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/grouped-ciphertext-2-handles-validity.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full GroupedCiphertext2HandlesValidityProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyGroupedCiphertext2HandlesValidity instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyGroupedCiphertext2HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyGroupedCiphertext3HandlesValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/grouped-ciphertext-3-handles-validity.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full GroupedCiphertext3HandlesValidityProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyGroupedCiphertext3HandlesValidity instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyGroupedCiphertext3HandlesValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyPercentageWithCapRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/percentage-with-cap.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full PercentageWithCapProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyPercentageWithCap instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyPercentageWithCapResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyPubkeyValidityRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/pubkey-validity.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner -- the
          key context-state/close will later require a signature from to
          reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      elgamal_pubkey:
        description: |-
          ElgamalPubkey is the ElGamal public key the proof is about,
          base58-encoded (see tool/generate/elgamal-keypair).
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      pubkey_proof:
        description: |-
          PubkeyProof proves whoever built this request knows the secret key
          ElgamalPubkey was derived from, base58-encoded (see
          tool/prove/pubkey-validity).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyPubkeyValidityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ContextStateVerifyZeroCiphertextRequest:
    properties:
      context_state_account:
        description: |-
          ContextStateAccount already exists, created via
          context-state/create/zero-ciphertext.
        example: ""
        type: string
      context_state_account_owner:
        description: |-
          ContextStateAccountOwner is recorded as the context's owner --
          the key context-state/close will later require a signature from
          to reclaim this account's rent. It does not sign here.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      proof_data:
        description: |-
          ProofData is the full ZeroCiphertextProofData wire bytes (context then
          proof), base58-encoded, packed by the caller exactly as the
          deployed program's VerifyZeroCiphertext instruction expects -- no from-scratch
          prover exists in this codebase for this proof type yet.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.ContextStateVerifyZeroCiphertextResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      context_state_account:
        type: string
      context_state_account_owner:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.CreateATAIdempotentRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the token the associated account is derived and initialized to
          hold, and must already exist.
        example: ""
        type: string
      owner:
        description: |-
          Owner is who ends up owning the account, which need not be RentPayer.
          Funding somebody else's associated account is ordinary: the address is
          theirs either way.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      program:
        description: |-
          Program is a seed of the derived address, not only the program the
          account belongs to, so one owner has a different associated account
          for classic Token than for Token-2022 over the same mint.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer covers the rent-exemption deposit, distinct from FeePayer:
          the two are separate balances to check, and a caller funding somebody
          else's associated account pays this one without necessarily paying the
          transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.CreateATAIdempotentResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      associated_token_account:
        type: string
      bump:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the associated account's creation. Its
          lamports are always exactly the rent-exemption minimum for a 165-byte
          account, never more or less.
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.CreateATARequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the token the associated account is derived and initialized to
          hold, and must already exist.
        example: ""
        type: string
      owner:
        description: |-
          Owner is who ends up owning the account, which need not be RentPayer.
          Funding somebody else's associated account is ordinary: the address is
          theirs either way.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      program:
        description: |-
          Program is a seed of the derived address, not only the program the
          account belongs to, so one owner has a different associated account
          for classic Token than for Token-2022 over the same mint.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer covers the rent-exemption deposit, distinct from FeePayer:
          the two are separate balances to check, and a caller funding somebody
          else's associated account pays this one without necessarily paying the
          transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.CreateATAResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      associated_token_account:
        type: string
      bump:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the associated account's creation. Its
          lamports are always exactly the rent-exemption minimum for a 165-byte
          account, never more or less.
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.CreateKTARequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token
          or Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds TokenAccount's creation for exactly the rent-exemption
          minimum for a 165-byte account, and is a separate balance from
          FeePayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      token_account:
        description: |-
          TokenAccount is created. It signs alongside RentPayer, since an address
          does not exist until whoever holds its private key authorizes its
          creation. It must not already exist.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.CreateKTAResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds CreateAccount itself. Its lamports are always
          exactly the rent-exemption minimum for a 165-byte account, never more
          or less.
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.CreateMintRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the account created. It signs alongside RentPayer, since an
          address does not exist until whoever holds its private key authorizes
          its creation. It must not already exist.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token
          or Token-2022. It is required rather than defaulted, since a mint
          belongs to exactly one of the two forever and defaulting would make
          picking wrong silent. It is an address rather than a name because that
          is what actually selects the program on chain: Token-2022 is not an
          enum value, it is a different account, and a third Token
          implementation would need no change here to be reachable.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds Mint's creation for exactly the rent-exemption minimum
          for an 82-byte account, and is a separate balance from FeePayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.CreateMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries
          a stored value rather than a fetched blockhash.
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds CreateMint itself. Its lamports are always
          exactly the rent-exemption minimum for an 82-byte account, never more
          or less.
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.CreateMultisigRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_account:
        description: |-
          MultisigAccount is the account created. It signs alongside RentPayer,
          since an address does not exist until whoever holds its private key
          authorizes its creation. It must not already exist.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds MultisigAccount's creation for exactly the
          rent-exemption minimum for a 355-byte account, and is a separate
          balance from FeePayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.CreateMultisigResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      multisig_account:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds CreateMultisig itself. Its lamports are always
          exactly the rent-exemption minimum for a 355-byte account, never more
          or less.
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.CreateNativeMintRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: Program must be Token-2022. Classic Token has no CreateNativeMint.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer signs and pays the native mint's rent, which the program takes
          from it.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.CreateNativeMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent_payer:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.DepositRequest:
    properties:
      account:
        description: |-
          Account is debited from its public balance and credited to its own
          confidential pending balance -- the same account both ends.
        example: ""
        type: string
      amount:
        description: Amount is the raw base-unit count to move, not a UI decimal string.
        example: "1000"
        type: string
      authority:
        description: |-
          Authority is Account's owner, or its delegate for no more than what
          was delegated -- the same role a plain transfer or burn checks,
          since this spends from the public balance like either of those.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      decimals:
        description: |-
          Decimals must equal Mint's own, the same check every other
          *Checked-shaped instruction in this API runs rather than trusting
          the caller's figure.
        example: 0
        type: integer
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what Account must hold, and is the source of the decimals
          checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.DepositResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      authority:
        type: string
      decimals:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.DisableConfidentialCreditsRequest:
    properties:
      account:
        description: |-
          Account must already carry the ConfidentialTransferAccount extension
          (see configure-account).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.DisableConfidentialCreditsResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.DisableCpiGuardRequest:
    properties:
      account:
        description: |-
          Account is a Token-2022 token account. If it does not carry the
          CpiGuard extension yet, the instruction adds it, which needs room for it
          already in the account (see cpi-guard/reallocate).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.DisableCpiGuardResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.DisableHarvestToMintRequest:
    properties:
      authority:
        description: |-
          Authority is the authority the ConfidentialTransferFeeConfig names
          (its authority field, not the TransferFeeConfig's withdraw withheld
          authority), or its multisig for a multisig-owned one (see
          MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that mint stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the mint, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must already carry the ConfidentialTransferFeeConfig extension
          (see confidential-transfer-fee-config/initialize).
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.DisableHarvestToMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.DisableNonConfidentialCreditsRequest:
    properties:
      account:
        description: |-
          Account must already carry the ConfidentialTransferAccount extension
          (see configure-account).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.DisableNonConfidentialCreditsResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.DisableRequiredMemoTransfersRequest:
    properties:
      account:
        description: |-
          Account is a Token-2022 token account. If it does not carry the
          MemoTransfer extension yet, the instruction adds it, which needs room for it
          already in the account (see memo-transfer/reallocate).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.DisableRequiredMemoTransfersResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.ElGamalRegistryCreateRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      pubkey_validity_context_state_account:
        description: |-
          PubkeyValidityContextStateAccount holds the verified PubkeyValidity
          proof for the ElGamal public key being registered (see
          tool/prove/pubkey-validity and
          zk-elgamal-proof/context-state/{create,verify}/pubkey-validity).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds the registry account's rent-exempt minimum. The
          registry program does not fund the account it creates -- it only
          allocates and assigns it, and refuses to if the address does not
          already hold enough lamports -- so this transaction first transfers
          the shortfall from RentPayer to the registry address. It may be left
          empty only when the address is already funded. It signs when named
          and is not the wallet's business: anyone can fund the address.
        example: ""
        type: string
      wallet:
        description: |-
          Wallet owns the registry and signs. The registry account itself is not
          a field: it is the PDA derived from the wallet (seeds
          ["elgamal-registry", wallet]) and the response reports it.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ElGamalRegistryCreateResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      pubkey_validity_context_state_account:
        type: string
      recent_blockhash:
        type: string
      registry_account:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      wallet:
        type: string
    type: object
  v2.ElGamalRegistryUpdateRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      pubkey_validity_context_state_account:
        description: |-
          PubkeyValidityContextStateAccount holds the verified PubkeyValidity
          proof for the ElGamal public key being registered (see
          tool/prove/pubkey-validity and
          zk-elgamal-proof/context-state/{create,verify}/pubkey-validity).
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      wallet:
        description: |-
          Wallet owns the registry and signs. The registry account itself is not
          a field: it is the PDA derived from the wallet (seeds
          ["elgamal-registry", wallet]) and the response reports it.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ElGamalRegistryUpdateResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      pubkey_validity_context_state_account:
        type: string
      recent_blockhash:
        type: string
      registry_account:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      wallet:
        type: string
    type: object
  v2.EnableConfidentialCreditsRequest:
    properties:
      account:
        description: |-
          Account must already carry the ConfidentialTransferAccount extension
          (see configure-account).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.EnableConfidentialCreditsResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.EnableCpiGuardRequest:
    properties:
      account:
        description: |-
          Account is a Token-2022 token account. If it does not carry the
          CpiGuard extension yet, the instruction adds it, which needs room for it
          already in the account (see cpi-guard/reallocate).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.EnableCpiGuardResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.EnableHarvestToMintRequest:
    properties:
      authority:
        description: |-
          Authority is the authority the ConfidentialTransferFeeConfig names
          (its authority field, not the TransferFeeConfig's withdraw withheld
          authority), or its multisig for a multisig-owned one (see
          MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that mint stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the mint, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must already carry the ConfidentialTransferFeeConfig extension
          (see confidential-transfer-fee-config/initialize).
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.EnableHarvestToMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.EnableNonConfidentialCreditsRequest:
    properties:
      account:
        description: |-
          Account must already carry the ConfidentialTransferAccount extension
          (see configure-account).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.EnableNonConfidentialCreditsResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.EnableRequiredMemoTransfersRequest:
    properties:
      account:
        description: |-
          Account is a Token-2022 token account. If it does not carry the
          MemoTransfer extension yet, the instruction adds it, which needs room for it
          already in the account (see memo-transfer/reallocate).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token account can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.EnableRequiredMemoTransfersResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.FreezeAccountRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      freeze_authority:
        description: FreezeAuthority must be Mint's freeze authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what TokenAccount must hold, and is the source of the freeze
          authority checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is suspended. It must already exist, hold Mint, and not
          already be frozen.
        example: ""
        type: string
    type: object
  v2.FreezeAccountResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      freeze_authority:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.HarvestWithheldTokensToMintRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what every one of SourceTokenAccounts must hold, and where
          the harvested total ends up.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold
          TransferFeeConfig.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_accounts:
        description: |-
          SourceTokenAccounts is swept in full: this instruction always sweeps
          each named account's own withheld_amount entirely, there is no
          partial-amount form.
        example:
        - ""
        items:
          type: string
        type: array
    type: object
  v2.HarvestWithheldTokensToMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_accounts:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeAccount2Request:
    properties:
      durable_nonce_account:
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      owner:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        example: ""
        type: string
      token_account:
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.InitializeAccount2Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.InitializeAccount3Request:
    properties:
      durable_nonce_account:
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      owner:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        example: ""
        type: string
      token_account:
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.InitializeAccount3Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.InitializeAccountRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the token TokenAccount is initialized to hold, and must already
          exist.
        example: ""
        type: string
      owner:
        description: |-
          Owner is who can transfer, burn, or otherwise authorize spending from
          TokenAccount. It is not required to sign this transaction:
          InitializeAccount only records it, it does not check it against a
          signer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It must match the program that already owns TokenAccount
          and Mint.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is the account initialized as a holder account for Mint.
          It must already exist, must be owned by Program, and must be exactly
          165 bytes and uninitialized. It does not sign.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.InitializeAccountResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.InitializeConfidentialMintBurnRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the account this attaches to. It must already exist (see
          create-mint) and not yet be initialized -- initialize-mint2 has to
          run after this, never before.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      supply_ae_key:
        description: |-
          SupplyAeKey encrypts the initial supply of zero into the decryptable
          supply the instruction carries, base58-encoded raw 16 bytes -- the same
          kind of key an account's decryptable balance uses. Keep it: every later
          mint and burn needs it to keep the decryptable supply in step.
        example: ""
        type: string
      supply_elgamal_pubkey:
        description: |-
          SupplyElgamalPubkey is the ElGamal public key the mint's confidential
          supply is encrypted under, base58-encoded raw 32 bytes (see
          tool/generate/elgamal-keypair). Its secret key is what later mint and
          burn proofs are built with, and it can be rotated (see
          rotate-supply-elgamal-pubkey).
        example: ""
        type: string
    type: object
  v2.InitializeConfidentialMintBurnResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      decryptable_supply:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      supply_elgamal_pubkey:
        type: string
      transaction:
        type: string
    type: object
  v2.InitializeConfidentialTransferFeeConfigRequest:
    properties:
      authority:
        description: |-
          Authority may later reconfigure this extension. Left empty, it can
          never be reconfigured.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the account this attaches to. It must already exist (see
          create-mint) and not yet be initialized -- initialize-mint2 has to
          run after this, never before.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      withdraw_withheld_authority_elgamal_pubkey:
        description: |-
          WithdrawWithheldAuthorityElgamalPubkey is required: the ElGamal
          public key withheld fees are encrypted under, base58-encoded (a
          32-byte compressed Ristretto point, not a Solana address -- see
          tool/generate/elgamal-keypair). Whoever holds its secret key can
          decrypt every withheld fee, and with the fee parameters that can
          reveal information about transfer amounts.
        example: ""
        type: string
    type: object
  v2.InitializeConfidentialTransferFeeConfigResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      withdraw_withheld_authority_elgamal_pubkey:
        type: string
    type: object
  v2.InitializeConfidentialTransferMintRequest:
    properties:
      auditor_elgamal_pubkey:
        description: |-
          AuditorElgamalPubkey, left empty, means no auditor can ever decrypt a
          confidential transfer amount on this mint. Given, it is a raw ElGamal
          public key encoded as base58 -- a 32-byte compressed Ristretto point,
          not a Solana address, and this API does not derive one from a
          keypair; the caller supplies the raw bytes themselves.
        example: ""
        type: string
      authority:
        description: |-
          Authority may later call whatever reconfigures this extension and
          approve new confidential accounts when AutoApproveNewAccounts is
          false. Left empty, this capability is given up permanently.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      auto_approve_new_accounts:
        description: |-
          AutoApproveNewAccounts determines whether a newly configured
          confidential account may be used immediately, or needs Authority to
          approve it first.
        example: true
        type: boolean
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the account this attaches to. It must already exist (see
          create-mint) and not yet be initialized -- initialize-mint2 has to
          run after this, never before.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.InitializeConfidentialTransferMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      auditor_elgamal_pubkey:
        type: string
      authority:
        type: string
      auto_approve_new_accounts:
        type: boolean
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeImmutableOwnerRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token
          or Token-2022. On Token-2022 this attaches the real extension; on
          classic Token it is a documented no-op, kept for compatibility with
          the Associated Token Account program's own create flow.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is the account whose owner field is locked. It must
          already exist, be owned by Program, and — on Token-2022 — carry the
          extension space this instruction writes to. This endpoint does not
          verify that itself, since parsing Token-2022 extension layouts is its
          own separate undertaking; a mismatch fails on chain instead.
        example: ""
        type: string
    type: object
  v2.InitializeImmutableOwnerResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.InitializeMint2Request:
    properties:
      decimals:
        description: |-
          Decimals fixes how the raw integer amount this mint moves is displayed
          as a UI amount, and cannot be changed after initialization.
        example: 6
        type: integer
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      freeze_authority:
        description: |-
          FreezeAuthority may be left empty, in which case the mint is
          initialized with no freeze authority at all, permanently.
        example: ""
        type: string
      mint:
        description: |-
          Mint is the account initialized. It must already exist, must be owned
          by Program, and must be exactly 82 bytes and uninitialized. It does not
          sign: it has already been created by the time this runs, and nothing
          about initializing it needs its authority.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      mint_authority:
        description: |-
          MintAuthority is who can mint new supply going forward. It is not
          required to sign this transaction: InitializeMint2 only records it, it
          does not check it against a signer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It must match the program that already owns Mint.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.InitializeMint2Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      decimals:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      freeze_authority:
        type: string
      message:
        type: string
      mint:
        type: string
      mint_authority:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries
          a stored value rather than a fetched blockhash.
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeMintCloseAuthorityRequest:
    properties:
      close_authority:
        description: |-
          CloseAuthority may sign close-account against Mint once initialized.
          Left empty, the extension is not attached at all. Unlike
          transfer_fee_config_authority, upstream also exposes
          AuthorityType::CloseMint through the plain set-authority instruction,
          so a close authority can still be granted or replaced later even if
          none is set here.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the account this attaches to. It must already exist (see
          create-mint) and not yet be initialized -- initialize-mint2 has to
          run after this, never before.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.InitializeMintCloseAuthorityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      close_authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeMintRequest:
    properties:
      decimals:
        description: |-
          Decimals fixes how the raw integer amount this mint moves is displayed
          as a UI amount, and cannot be changed after initialization.
        example: 6
        type: integer
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      freeze_authority:
        description: |-
          FreezeAuthority may be left empty, in which case the mint is
          initialized with no freeze authority at all, permanently.
        example: ""
        type: string
      mint:
        description: |-
          Mint is the account initialized. It must already exist, must be owned
          by Program, and must be exactly 82 bytes and uninitialized. It does not
          sign: it has already been created by the time this runs, and nothing
          about initializing it needs its authority.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      mint_authority:
        description: |-
          MintAuthority is who can mint new supply going forward. It is not
          required to sign this transaction: InitializeMint only records it, it
          does not check it against a signer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It must match the program that already owns Mint.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.InitializeMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      decimals:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      freeze_authority:
        type: string
      message:
        type: string
      mint:
        type: string
      mint_authority:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries
          a stored value rather than a fetched blockhash.
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeMultisig2Request:
    properties:
      durable_nonce_account:
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      m:
        example: 2
        type: integer
      multisig_account:
        example: ""
        type: string
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        example: ""
        type: string
      signers:
        items:
          type: string
        type: array
    type: object
  v2.InitializeMultisig2Response:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      m:
        type: integer
      message:
        type: string
      multisig_account:
        type: string
      multisig_signers:
        items:
          type: string
        type: array
      "n":
        type: integer
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeMultisigRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      m:
        description: |-
          M is how many of Signers must sign in the multisig's place, wherever
          it is later named as an authority. The program enforces
          1 <= m <= len(signers) <= 11.
        example: 2
        type: integer
      multisig_account:
        description: |-
          MultisigAccount is the account initialized. It must already exist,
          must be owned by Program, and must be exactly 355 bytes and
          uninitialized. It does not sign: nothing about becoming a registered
          signer needs proving here, only once the multisig is actually used as
          an authority.
        example: ""
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It must match the program that already owns
          MultisigAccount.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      signers:
        description: |-
          Signers is who is enrolled. Order is preserved in the response, but the
          program itself treats membership as a set: any m of them may sign.
        items:
          type: string
        type: array
    type: object
  v2.InitializeMultisigResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      m:
        type: integer
      message:
        type: string
      multisig_account:
        type: string
      multisig_signers:
        items:
          type: string
        type: array
      "n":
        type: integer
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeNonTransferableMintRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the account this attaches to. It must already exist (see
          create-mint) and not yet be initialized -- initialize-mint2 has to
          run after this, never before.
        example: ""
        type: string
      program:
        description: Program must be Token-2022. A classic Token mint can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.InitializeNonTransferableMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializePermanentDelegateRequest:
    properties:
      delegate:
        description: |-
          Delegate may transfer or burn any holder's tokens of Mint, with no approval,
          for as long as the mint exists. Required: there is no such thing as a
          permanent delegate that is nobody.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the account this attaches to. It must already exist (see
          create-mint) and not yet be initialized -- initialize-mint2 has to
          run after this, never before.
        example: ""
        type: string
      program:
        description: Program must be Token-2022. A classic Token mint can never hold
          this extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.InitializePermanentDelegateResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      delegate:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.InitializeTransferFeeConfigRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      maximum_fee:
        description: |-
          MaximumFee caps what a single transfer-checked-with-fee ever
          withholds, in raw base units, regardless of how large the transfer
          is or what the rate above would otherwise compute.
        example: "1000000"
        type: string
      mint:
        description: |-
          Mint is the account this attaches to. It must already exist (see
          create-mint) and not yet be initialized -- initialize-mint2 has to
          run after this, never before.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      transfer_fee_basis_points:
        description: |-
          TransferFeeBasisPoints is the fee rate, out of 10,000 (500 = 5%). The
          program itself rejects anything over 10,000, so this is checked here
          too rather than left to come back as an on-chain rejection.
        example: 500
        type: integer
      transfer_fee_config_authority:
        description: |-
          TransferFeeConfigAuthority may call set-transfer-fee later to change
          the rate. Left empty, this capability is given up permanently:
          unlike a mint or freeze authority, there is no later instruction that
          grants one where none was set.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      withdraw_withheld_authority:
        description: |-
          WithdrawWithheldAuthority may withdraw what
          withdraw-withheld-tokens-from-mint/from-accounts moves. Left empty,
          whatever accumulates as withheld fees can never be withdrawn from the
          mint again -- harvest-withheld-tokens-to-mint still works from
          accounts into the mint, but nothing can move it out from there.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.InitializeTransferFeeConfigResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      maximum_fee:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      transfer_fee_basis_points:
        type: integer
      transfer_fee_config_authority:
        type: string
      withdraw_withheld_authority:
        type: string
    type: object
  v2.InitializeWrappedSolRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      owner:
        description: |-
          Owner is who can transfer, unwrap, or otherwise authorize spending
          from TokenAccount. It is not required to sign this transaction:
          InitializeAccount3 only records it, it does not check it against a
          signer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token
          or Token-2022. It also selects which native mint TokenAccount is
          initialized against — the two programs never share one.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is the account initialized. It must already exist, must
          be owned by Program, and must be at least 165 bytes and uninitialized.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.InitializeWrappedSolResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.MintToCheckedRequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to mint, not a UI decimal string.
        example: "1000000"
        type: string
      decimals:
        description: |-
          Decimals is checked against Mint's own stored value rather than
          trusted, which is the whole point of the checked variant: catching a
          client that formatted Amount against the wrong decimals as a 400
          instead of an on-chain failure.
        example: 6
        type: integer
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. Minting moves no lamports
          of its own, so this is the only balance this endpoint ever checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the token whose supply grows. It must already exist, and its
          own MintAuthority is what this request has to name to be honored.
        example: ""
        type: string
      mint_authority:
        description: |-
          MintAuthority is Mint's mint authority, not TokenAccount's owner or
          delegate: minting checks who is allowed to create new supply, not who
          is allowed to move what already exists.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is credited with the newly minted supply. It must already
          exist and hold Mint.
        example: ""
        type: string
    type: object
  v2.MintToCheckedResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      mint_authority:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.MintToRequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to mint, not a UI decimal string.
        example: "1000000"
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is the token whose supply grows. It must already exist, and its
          own MintAuthority is what this request has to name to be honored.
        example: ""
        type: string
      mint_authority:
        description: |-
          MintAuthority is Mint's mint authority, not TokenAccount's owner or
          delegate.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is credited with the newly minted supply. It must already
          exist and hold Mint.
        example: ""
        type: string
    type: object
  v2.MintToResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      mint_authority:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.ReallocateConfidentialTransferAccountRequest:
    properties:
      account:
        description: |-
          Account is the token account to check and, if needed, grow. It must
          already exist and be owned by Program.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      include_confidential_transfer_fee_amount:
        description: |-
          IncludeConfidentialTransferFeeAmount also reserves room for the
          ConfidentialTransferFeeAmount extension, in the same Reallocate
          instruction. Set it when Account's mint charges a transfer fee: on
          such a mint, configure-account itself initializes
          ConfidentialTransferFeeAmount alongside ConfidentialTransferAccount,
          so the account needs room for both before it runs, and the two have
          to be reserved together -- reallocating for one and then the other
          leaves room for only one, since neither exists yet for the second
          call to count.
        example: false
        type: boolean
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. Unlike every other Token endpoint, this
          is not the usual either-program field: a classic Token account's
          layout is fixed at 165 bytes forever, with no TLV region to grow
          into, so classic Token is rejected here rather than left to fail on
          chain.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds whatever the resize costs, a distinct role from
          Owner: Owner authorizes the account being touched, RentPayer covers
          what that costs, and they need not be the same key.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ReallocateConfidentialTransferAccountResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the resize: the shortfall between
          TargetSize's rent-exemption minimum and Account's actual current
          lamports, zero when Account already holds enough.
      signers:
        items:
          type: string
        type: array
      target_size:
        description: |-
          TargetSize is the total account size GetAccountDataSize reported for
          Account's existing extensions plus ConfidentialTransferAccount, asked of
          the deployed program rather than recomputed here.
        type: string
      transaction:
        type: string
    type: object
  v2.ReallocateConfidentialTransferMintRequest:
    properties:
      account:
        description: |-
          Account is the token account to check and, if needed, grow. It must
          already exist and be owned by Program.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. Unlike every other Token endpoint, this
          is not the usual either-program field: a classic Token account's
          layout is fixed at 165 bytes forever, with no TLV region to grow
          into, so classic Token is rejected here rather than left to fail on
          chain.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds whatever the resize costs, a distinct role from
          Owner: Owner authorizes the account being touched, RentPayer covers
          what that costs, and they need not be the same key.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ReallocateConfidentialTransferMintResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the resize: the shortfall between
          TargetSize's rent-exemption minimum and Account's actual current
          lamports, zero when Account already holds enough.
      signers:
        items:
          type: string
        type: array
      target_size:
        description: |-
          TargetSize is the total account size GetAccountDataSize reported for
          Account's existing extensions plus ConfidentialTransferMint, asked of
          the deployed program rather than recomputed here.
        type: string
      transaction:
        type: string
    type: object
  v2.ReallocateCpiGuardRequest:
    properties:
      account:
        description: |-
          Account is the token account to check and, if needed, grow. It must
          already exist and be owned by Program.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. Unlike every other Token endpoint, this
          is not the usual either-program field: a classic Token account's
          layout is fixed at 165 bytes forever, with no TLV region to grow
          into, so classic Token is rejected here rather than left to fail on
          chain.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds whatever the resize costs, a distinct role from
          Owner: Owner authorizes the account being touched, RentPayer covers
          what that costs, and they need not be the same key.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ReallocateCpiGuardResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the resize: the shortfall between
          TargetSize's rent-exemption minimum and Account's actual current
          lamports, zero when Account already holds enough.
      signers:
        items:
          type: string
        type: array
      target_size:
        description: |-
          TargetSize is the total account size GetAccountDataSize reported for
          Account's existing extensions plus CpiGuard, asked of the
          deployed program rather than recomputed here.
        type: string
      transaction:
        type: string
    type: object
  v2.ReallocateMemoTransferRequest:
    properties:
      account:
        description: |-
          Account is the token account to check and, if needed, grow. It must
          already exist and be owned by Program.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. Unlike every other Token endpoint, this
          is not the usual either-program field: a classic Token account's
          layout is fixed at 165 bytes forever, with no TLV region to grow
          into, so classic Token is rejected here rather than left to fail on
          chain.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds whatever the resize costs, a distinct role from
          Owner: Owner authorizes the account being touched, RentPayer covers
          what that costs, and they need not be the same key.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ReallocateMemoTransferResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the resize: the shortfall between
          TargetSize's rent-exemption minimum and Account's actual current
          lamports, zero when Account already holds enough.
      signers:
        items:
          type: string
        type: array
      target_size:
        description: |-
          TargetSize is the total account size GetAccountDataSize reported for
          Account's existing extensions plus MemoTransfer, asked of the
          deployed program rather than recomputed here.
        type: string
      transaction:
        type: string
    type: object
  v2.ReallocateMintCloseAuthorityRequest:
    properties:
      account:
        description: |-
          Account is the token account to check and, if needed, grow. It must
          already exist and be owned by Program.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. Unlike every other Token endpoint, this
          is not the usual either-program field: a classic Token account's
          layout is fixed at 165 bytes forever, with no TLV region to grow
          into, so classic Token is rejected here rather than left to fail on
          chain.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds whatever the resize costs, a distinct role from
          Owner: Owner authorizes the account being touched, RentPayer covers
          what that costs, and they need not be the same key.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ReallocateMintCloseAuthorityResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the resize: the shortfall between
          TargetSize's rent-exemption minimum and Account's actual current
          lamports, zero when Account already holds enough.
      signers:
        items:
          type: string
        type: array
      target_size:
        description: |-
          TargetSize is the total account size GetAccountDataSize reported for
          Account's existing extensions plus MintCloseAuthority, asked of the
          deployed program rather than recomputed here.
        type: string
      transaction:
        type: string
    type: object
  v2.ReallocateTransferFeeAmountRequest:
    properties:
      account:
        description: |-
          Account is the token account to check and, if needed, grow. It must
          already exist and be owned by Program.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. Unlike every other Token endpoint, this
          is not the usual either-program field: a classic Token account's
          layout is fixed at 165 bytes forever, with no TLV region to grow
          into, so classic Token is rejected here rather than left to fail on
          chain.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds whatever the resize costs, a distinct role from
          Owner: Owner authorizes the account being touched, RentPayer covers
          what that costs, and they need not be the same key.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ReallocateTransferFeeAmountResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the resize: the shortfall between
          TargetSize's rent-exemption minimum and Account's actual current
          lamports, zero when Account already holds enough.
      signers:
        items:
          type: string
        type: array
      target_size:
        description: |-
          TargetSize is the total account size GetAccountDataSize reported for
          Account's existing extensions plus TransferFeeAmount, asked of the
          deployed program rather than recomputed here.
        type: string
      transaction:
        type: string
    type: object
  v2.ReallocateTransferFeeConfigRequest:
    properties:
      account:
        description: |-
          Account is the token account to check and, if needed, grow. It must
          already exist and be owned by Program.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer owner. Non-empty, Owner
          itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is Account's owner, or its multisig for a multisig-owned
          account (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program must be Token-2022. Unlike every other Token endpoint, this
          is not the usual either-program field: a classic Token account's
          layout is fixed at 165 bytes forever, with no TLV region to grow
          into, so classic Token is rejected here rather than left to fail on
          chain.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds whatever the resize costs, a distinct role from
          Owner: Owner authorizes the account being touched, RentPayer covers
          what that costs, and they need not be the same key.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.ReallocateTransferFeeConfigResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the resize: the shortfall between
          TargetSize's rent-exemption minimum and Account's actual current
          lamports, zero when Account already holds enough.
      signers:
        items:
          type: string
        type: array
      target_size:
        description: |-
          TargetSize is the total account size GetAccountDataSize reported for
          Account's existing extensions plus TransferFeeConfig, asked of the
          deployed program rather than recomputed here.
        type: string
      transaction:
        type: string
    type: object
  v2.RecordCloseRequest:
    properties:
      authority:
        description: Authority is the record's recorded authority. It signs.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      receiver:
        description: Receiver gains the record's lamports.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      record_account:
        description: RecordAccount must already be initialized.
        example: ""
        type: string
    type: object
  v2.RecordCloseResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      receiver:
        type: string
      recent_blockhash:
        type: string
      reclaimed_lamports:
        type: integer
      record_account:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.RecordCreateAccountRequest:
    properties:
      data_length:
        description: |-
          DataLength is how many bytes of data (excluding the 33-byte header) the
          record should hold. For a 256-bit range proof that is 1064; a proof is
          verified from record offset 33.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      record_account:
        description: |-
          RecordAccount is the account created. It signs alongside RentPayer, since an
          address does not exist until whoever holds its private key authorizes its
          creation. It must not already exist.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds RecordAccount's creation for exactly the rent-exemption
          minimum for its size (33 + data_length).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.RecordCreateAccountResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      data_length:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      record_account:
        type: string
      rent_exempt_lamports:
        type: integer
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.RecordInitializeRequest:
    properties:
      authority:
        description: |-
          Authority is who may later write to, reassign, reallocate, or close the
          record. It does not sign here, which is why this should land in the same
          transaction as record/create-account: an uninitialized record account can
          be initialized by anyone, with any authority they like.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      record_account:
        description: |-
          RecordAccount must already exist, owned by the Record program and at least
          33 bytes (see record/create-account), and not yet initialized.
        example: ""
        type: string
    type: object
  v2.RecordInitializeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      record_account:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.RecordReallocateRequest:
    properties:
      authority:
        description: Authority is the record's recorded authority. It signs.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      data_length:
        description: |-
          DataLength is how many bytes of data (excluding the 33-byte header) the
          record should hold afterwards. It does nothing if the account is already
          that large.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      record_account:
        description: |-
          RecordAccount must already be initialized. It must already hold enough
          lamports to be rent exempt at the new size: this instruction does not fund
          it, so top it up with a system transfer first.
        example: ""
        type: string
    type: object
  v2.RecordReallocateResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      data_length:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      record_account:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.RecordSetAuthorityRequest:
    properties:
      authority:
        description: Authority is the record's current authority. It signs.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      new_authority:
        description: NewAuthority becomes the record's authority. It does not sign.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      record_account:
        description: RecordAccount must already be initialized.
        example: ""
        type: string
    type: object
  v2.RecordSetAuthorityResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      new_authority:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      record_account:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.RecordWriteRequest:
    properties:
      authority:
        description: Authority is the record's recorded authority. It signs.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      data:
        description: |-
          Data is the bytes to write, base58-encoded. It has to fit in one
          transaction alongside everything else: about 1000 bytes without a durable
          nonce, about 900 with one. A larger proof is written in several calls,
          each at its own offset.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      offset:
        description: |-
          Offset counts from the end of the record's 33-byte header, not from the
          start of the account: writing at offset 0 puts the first byte at byte 33.
          A proof written from offset 0 is verified with proof_offset 33.
        example: ""
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      record_account:
        description: RecordAccount must already be initialized (see record/initialize).
        example: ""
        type: string
    type: object
  v2.RecordWriteResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      length:
        type: integer
      message:
        type: string
      nonce_authority:
        type: string
      offset:
        type: string
      recent_blockhash:
        type: string
      record_account:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.RequestHeapFrameRequest:
    properties:
      bytes:
        description: |-
          Bytes must be a multiple of 1024 in [32768, 262144] (32KiB to 256KiB);
          the runtime rejects anything else as InvalidInstructionData, so this
          is checked here rather than left to come back as an on-chain
          rejection.
        example: "65536"
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.RequestHeapFrameResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      bytes:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.RevokeRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount has its delegate and delegated amount cleared, whatever
          they currently are.
        example: ""
        type: string
      token_account_owner:
        description: |-
          TokenAccountOwner must be TokenAccount's owner, matching
          approve-checked's rule that only the owner may grant one: a delegate
          holds no authority over the delegation itself, only over what it was
          allowed to spend.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.RevokeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_owner:
        type: string
      transaction:
        type: string
    type: object
  v2.SetAccountOwnerReplaceRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_token_account_owner:
        description: NewTokenAccountOwner replaces TokenAccountOwner entirely.
        example: ""
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: TokenAccount is handed to NewTokenAccountOwner. It must already
          exist.
        example: ""
        type: string
      token_account_owner:
        description: |-
          TokenAccountOwner must be TokenAccount's current owner exactly, never
          a delegate: a delegate has no authority over the account itself, only
          over what it was allowed to spend.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.SetAccountOwnerReplaceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      new_token_account_owner:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_owner:
        type: string
      transaction:
        type: string
    type: object
  v2.SetCloseAuthorityClearRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: TokenAccount has its close authority cleared. It must already
          exist.
        example: ""
        type: string
      token_account_close_authority:
        description: |-
          TokenAccountCloseAuthority must be TokenAccount's current close
          authority exactly: its owner, unless a close authority is already set,
          in which case it must be that instead.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.SetCloseAuthorityClearResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      cleared:
        type: boolean
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_close_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.SetCloseAuthorityReplaceRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_token_account_close_authority:
        description: |-
          NewTokenAccountCloseAuthority replaces TokenAccountCloseAuthority
          entirely.
        example: ""
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: TokenAccount has its close authority replaced. It must already
          exist.
        example: ""
        type: string
      token_account_close_authority:
        description: |-
          TokenAccountCloseAuthority must be TokenAccount's current close
          authority exactly: its owner, unless a close authority is already set,
          in which case it must be that instead.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.SetCloseAuthorityReplaceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      new_token_account_close_authority:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      token_account_close_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.SetCloseMintAuthorityClearRequest:
    properties:
      close_authority:
        description: |-
          CloseAuthority must be Mint's current MintCloseAuthority close
          authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetCloseMintAuthorityClearResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      cleared:
        type: boolean
      close_authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetCloseMintAuthorityReplaceRequest:
    properties:
      close_authority:
        description: |-
          CloseAuthority must be Mint's current MintCloseAuthority close
          authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_close_authority:
        description: |-
          NewCloseAuthority replaces CloseAuthority entirely; it is not required
          to sign, since SetAuthority-style authority changes only record the
          new value rather than checking it against a signer.
        example: ""
        type: string
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetCloseMintAuthorityReplaceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      close_authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      new_close_authority:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetComputeUnitLimitRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      units:
        description: |-
          Units is the raw compute unit ceiling, not a multiplier or a
          percentage. Exceeding it during execution fails the transaction with
          a compute-budget-exceeded error.
        example: "200000"
        type: string
    type: object
  v2.SetComputeUnitLimitResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      units:
        type: string
    type: object
  v2.SetComputeUnitPriceRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      micro_lamports:
        description: |-
          MicroLamports is the raw priority-fee rate: the runtime multiplies
          this by whatever compute the transaction actually consumes to get the
          priority fee on top of the base signature fee.
        example: "1000"
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetComputeUnitPriceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      micro_lamports:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetFreezeAuthorityClearRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      freeze_authority:
        description: FreezeAuthority must be Mint's current freeze authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a mint
          belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetFreezeAuthorityClearResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      cleared:
        type: boolean
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      freeze_authority:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetFreezeAuthorityReplaceRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      freeze_authority:
        description: FreezeAuthority must be Mint's current freeze authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_freeze_authority:
        description: NewFreezeAuthority replaces FreezeAuthority entirely.
        example: ""
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a mint
          belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetFreezeAuthorityReplaceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      freeze_authority:
        type: string
      message:
        type: string
      mint:
        type: string
      new_freeze_authority:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetLoadedAccountsDataSizeLimitRequest:
    properties:
      bytes:
        description: |-
          Bytes must be non-zero — the runtime rejects zero outright as
          InvalidLoadedAccountsDataSizeLimit, so this is checked here rather
          than left to come back as an on-chain rejection. Left unset, legacy
          and v0 transactions default to 64MiB; this only ever lowers that
          ceiling, since anything larger is clamped back down to it.
        example: "131072"
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetLoadedAccountsDataSizeLimitResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      bytes:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetMintAuthorityClearRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      mint_authority:
        description: MintAuthority must be Mint's current mint authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a mint
          belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetMintAuthorityClearResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      cleared:
        type: boolean
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      mint_authority:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetMintAuthorityReplaceRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        example: ""
        type: string
      mint_authority:
        description: MintAuthority must be Mint's current mint authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      new_mint_authority:
        description: |-
          NewMintAuthority replaces MintAuthority entirely; it is not required to
          sign, since InitializeMint2-style authority changes only record the
          new value rather than checking it against a signer.
        example: ""
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a mint
          belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SetMintAuthorityReplaceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      mint_authority:
        type: string
      new_mint_authority:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SetTransferFeeRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      maximum_fee:
        description: |-
          MaximumFee caps what a single transfer-checked-with-fee ever
          withholds, in raw base units, regardless of how large the transfer
          is or what the rate above would otherwise compute.
        example: "1000000"
        type: string
      mint:
        description: |-
          Mint must already have the TransferFeeConfig extension (see
          initialize-transfer-fee-config).
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          TransferFeeConfigAuthority itself does not sign; the named members
          do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      transfer_fee_basis_points:
        description: |-
          TransferFeeBasisPoints is the new fee rate, out of 10,000 (500 = 5%).
          The program itself rejects anything over 10,000, so this is checked
          here too rather than left to come back as an on-chain rejection.
        example: 500
        type: integer
      transfer_fee_config_authority:
        description: |-
          TransferFeeConfigAuthority is the authority
          initialize-transfer-fee-config named, or its multisig for a
          multisig-owned authority (see MultisigSigners). Unlike that endpoint,
          this one cannot be left empty: an authority that was never set has no
          way to sign this instruction in the first place, permanently.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.SetTransferFeeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      maximum_fee:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      transfer_fee_basis_points:
        type: integer
      transfer_fee_config_authority:
        type: string
    type: object
  v2.SyncNativeRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount must already exist, be owned by Program, and actually be
          a wrapped-SOL account — the program rejects one that is not.
        example: ""
        type: string
    type: object
  v2.SyncNativeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      estimated_amount:
        $ref: '#/definitions/v2.SystemPayer'
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.SystemAllocateRequest:
    properties:
      account:
        description: |-
          Account must already exist, be owned by the System Program, and not
          already be allocated space — Allocate only ever sets a size once, on
          an account that does not yet have one.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer covers the shortfall, if any, between Account's current
          balance and the rent-exemption minimum for Space: a transfer from
          RentPayer for exactly that shortfall is prepended ahead of the
          allocation. Required even when Account already holds enough and no
          such transfer ends up being built. It may be the same account as
          Account, but only when Account already holds enough on its own — a
          shortfall has nowhere to come from in that case.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      space:
        example: "128"
        type: string
    type: object
  v2.SystemAllocateResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports RentPayer and the shortfall it covered. Its lamports are
          zero, and no transfer was actually prepended, when Account's balance
          already reached the rent-exemption minimum for Space.
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.SystemAssignRequest:
    properties:
      account:
        description: |-
          Account must already exist and already be owned by the System Program:
          only the current owner may reassign an account, and the System Program
          is the only owner exposing this as a callable instruction.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      owner:
        description: |-
          Owner must be executable: only the owning program may debit an account
          or write its data, so an account handed to a plain address is locked
          permanently. Pass the System Program to leave it as an ordinary account.
        example: "11111111111111111111111111111111"
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SystemAssignResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      owner:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemCreateAccountRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      new_account:
        description: |-
          NewAccount is the account created. It signs alongside RentPayer, since
          an address does not exist until whoever holds its private key
          authorizes its creation. It must not already exist.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds NewAccount's creation for exactly the rent-exemption
          minimum for a zero-byte account — always, and only that amount. There
          is no way to fund it beyond that minimum here: this endpoint only ever
          brings an account into existence, empty. Reserving space is a separate
          call to allocate, and topping up its balance further is a separate
          transfer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.SystemCreateAccountResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds CreateAccount itself. Its lamports are always
          exactly the rent-exemption minimum for a zero-byte account, never more
          or less.
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemNonceAdvanceRequest:
    properties:
      authority:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice. It has to be an account other than NonceAccount: the
          runtime refuses a transaction that advances the same nonce twice.
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      nonce_account:
        description: |-
          NonceAccount must already exist, be owned by the System Program, be
          exactly NonceAccountSpace bytes, and already be initialized — Advance
          rotates a stored value, it does not create one. It is named apart from
          DurableNonceAccount because both name a nonce account, and only their
          roles differ: this one is what the instruction acts on, that one is
          only what prices and expiry-proofs the transaction.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SystemNonceAdvanceResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      current_nonce:
        description: |-
          CurrentNonce is what NonceAccount holds now, before this transaction
          lands. Any transaction already built against it stops being valid once
          this one executes.
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_account:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash. It belongs to
          durable_nonce_account, not to NonceAccount — Authority above is
          NonceAccount's.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemNonceAuthorizeRequest:
    properties:
      authority:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice. It has to be an account other than NonceAccount: the
          runtime refuses a transaction that advances the same nonce twice.
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      new_authority:
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      nonce_account:
        description: |-
          NonceAccount must already exist, be owned by the System Program, be
          exactly NonceAccountSpace bytes, and already be initialized — Authorize
          hands off control of a nonce account, it does not create one. It is
          named apart from DurableNonceAccount because both name a nonce
          account, and only their roles differ: this one is what the instruction
          acts on, that one is only what prices and expiry-proofs the transaction.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SystemNonceAuthorizeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      new_authority:
        type: string
      nonce_account:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash. It belongs to
          durable_nonce_account, not to NonceAccount — Authority above is
          NonceAccount's, before this transaction lands.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemNonceCreateRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice. It has to be an account other than NewNonceAccount, which
          holds no nonce yet.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      new_nonce_account:
        description: |-
          NewNonceAccount is the account created. It signs alongside RentPayer,
          since an address does not exist until whoever holds its private key
          authorizes its creation. It must not already exist.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds NewNonceAccount's creation for exactly the
          rent-exemption minimum for a nonce account's fixed size — always, and
          only that amount. Neither the size nor the funding is a choice: a
          nonce account is always exactly NonceAccountSpace bytes, and the
          amount that has to sit in it follows from that.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.SystemNonceCreateResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      new_nonce_account:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries
          a stored value rather than a fetched blockhash. It belongs to
          durable_nonce_account, not to NewNonceAccount: this endpoint never
          initializes NewNonceAccount, so it gains no authority of its own here.
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds the creation. Its lamports are always exactly
          the rent-exemption minimum for a nonce account's fixed size, never more
          or less.
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.SystemNonceInitializeRequest:
    properties:
      authority:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice. It has to be an account other than NonceAccount, which
          holds no nonce to build against yet.
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      nonce_account:
        description: |-
          NonceAccount must already exist, be owned by the System Program, and be
          exactly NonceAccountSpace bytes — this endpoint doesn't create it, only
          writes the nonce state into a slot that's already there, and it does
          not sign: no authority of its own is needed to accept one. It is named
          apart from DurableNonceAccount because both name a nonce account, and
          only their roles differ: this one is what the instruction acts on, that
          one is only what prices and expiry-proofs the transaction.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SystemNonceInitializeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_account:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash. It belongs to
          durable_nonce_account, not to NonceAccount — Authority above is what
          NonceAccount gains.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemNonceUpgradeRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice. It has to be an account other than NonceAccount, which is
          Legacy and so not something to build against.
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      nonce_account:
        description: |-
          NonceAccount is the Legacy account being migrated. It is named apart
          from DurableNonceAccount because both name a nonce account, and only
          their roles differ: this one is what the instruction acts on, that one
          is only what prices and expiry-proofs the transaction.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.SystemNonceUpgradeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_account:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries
          a stored value rather than a fetched blockhash. NonceAccount has an
          authority too, but nothing signs for an upgrade, so it is not reported.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      version:
        type: integer
    type: object
  v2.SystemNonceWithdrawMaxRequest:
    properties:
      authority:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice. It has to be an account other than NonceAccount, which
          could not be closed anyway once this transaction has just advanced it.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It cannot be NonceAccount
          itself, since the balance being withdrawn is the same balance the fee
          would come from.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      nonce_account:
        description: |-
          NonceAccount is emptied and thereby closed. It is named apart from
          DurableNonceAccount because both name a nonce account, and only their
          roles differ: this one is what the instruction acts on, that one is
          only what prices and expiry-proofs the transaction.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited. It must already exist: this
          is a plain transfer between two accounts, not a way to bring a new one
          into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.SystemNonceWithdrawMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_account:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash. It belongs to
          durable_nonce_account, not to NonceAccount — Authority above is
          NonceAccount's.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      withdrawal:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Withdrawal reports NonceAccount and its entire balance, which this
          closes the account by taking.
    type: object
  v2.SystemNonceWithdrawRequest:
    properties:
      authority:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice. It has to be an account other than NonceAccount: the
          runtime refuses a transaction that advances the same nonce twice.
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      lamports:
        description: |-
          Lamports is the raw amount moved from NonceAccount to RecipientAccount.
          Taking everything closes the account and the runtime applies a further
          rule to that, so it has its own endpoint: what stays here has to keep
          NonceAccount rent exempt at its size.
        example: "1000000"
        type: string
      nonce_account:
        description: |-
          NonceAccount is withdrawn from. It is named apart from
          DurableNonceAccount because both name a nonce account, and only their
          roles differ: this one is what the instruction acts on, that one is
          only what prices and expiry-proofs the transaction.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited. It must already exist: this
          is a plain transfer between two accounts, not a way to bring a new one
          into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.SystemNonceWithdrawResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_account:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash. It belongs to
          durable_nonce_account, not to NonceAccount — Authority above is
          NonceAccount's.
        type: string
      recent_blockhash:
        type: string
      remaining:
        description: |-
          Remaining is what NonceAccount keeps, which has to stay at or above the
          rent-exempt minimum for its size.
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      withdrawal:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: Withdrawal reports NonceAccount and how much was taken from it.
    type: object
  v2.SystemPayer:
    properties:
      lamports:
        type: string
      payer:
        type: string
      sol:
        type: string
    type: object
  v2.SystemSeedAllocateRequest:
    properties:
      base:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer covers the shortfall, if any, between the derived account's
          current balance and the rent-exemption minimum for Space: a transfer
          from RentPayer for exactly that shortfall is prepended ahead of the
          allocation. Required even when the derived account already holds
          enough and no such transfer ends up being built. It may be the same
          account as the derived address, but only when the derived address
          already holds enough on its own — a shortfall has nowhere to come from
          in that case.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      seed:
        example: vault-1
        type: string
      space:
        example: "128"
        type: string
    type: object
  v2.SystemSeedAllocateResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      derived_address:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports RentPayer and the shortfall it covered. Its lamports are
          zero, and no transfer was actually prepended, when the derived
          account's balance already reached the rent-exemption minimum for
          Space.
      signers:
        items:
          type: string
        type: array
      space:
        type: integer
      transaction:
        type: string
    type: object
  v2.SystemSeedAssignRequest:
    properties:
      base:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      owner:
        description: |-
          Owner is both the program the derived account is handed to and the
          value base, seed, and this field must have been combined with from the
          start to name that account at all. It must be executable: only the
          owning program may debit an account or write its data, so assigning to
          a plain address locks the account and its lamports permanently.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      seed:
        example: vault-1
        type: string
    type: object
  v2.SystemSeedAssignResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      derived_address:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      owner:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemSeedCreateAccountRequest:
    properties:
      base:
        description: |-
          Base signs in the derived account's place: nobody holds a secret for
          SHA256(base || seed || owner), so whoever controls base controls every
          address derived from it.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as RentPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      rent_payer:
        description: |-
          RentPayer funds the derived account's creation for exactly the
          rent-exemption minimum for a zero-byte account — always, and only that
          amount. There is no way to fund it beyond that minimum here: this
          endpoint only ever brings a plain account into existence, empty, and
          topping up its balance further is a separate transfer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      seed:
        example: vault-1
        type: string
    type: object
  v2.SystemSeedCreateAccountResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      derived_address:
        description: |-
          DerivedAddress is what base, seed, and the System Program produce. It
          is the account being created, and it is absent from signers because
          nobody holds a secret for it.
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      rent:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Rent reports what funds CreateAccountWithSeed itself. Its lamports are
          always exactly the rent-exemption minimum for a zero-byte account,
          never more or less.
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemSeedTransferMaxRequest:
    properties:
      base:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as Base, but not the same account as the derived address,
          which has no key to sign with.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited. It must already exist: this
          is a plain transfer between two accounts, not a way to bring a new one
          into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      seed:
        example: vault-1
        type: string
    type: object
  v2.SystemSeedTransferMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      funding:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Funding reports the transfer itself: the derived account drained, and
          how much that turned out to be.
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemSeedTransferRequest:
    properties:
      base:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as Base, but not the same account as the derived address,
          which has no key to sign with.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      lamports:
        description: |-
          Lamports is the raw amount moved from the derived account to
          RecipientAccount.
        example: "1000000"
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited. It must already exist: this
          is a plain transfer between two accounts, not a way to bring a new one
          into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      seed:
        example: vault-1
        type: string
    type: object
  v2.SystemSeedTransferResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      funding:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Funding reports the transfer itself: the derived account debited, and
          how much.
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemTransferCollectRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as any one source, in which case the fee is deducted from its
          balance alongside its own transfer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited by every source. It must
          already exist: this is a plain transfer between accounts, not a way to
          bring a new one into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      transfers:
        description: |-
          Transfers lists the funding payers and their amounts, in the order the
          instructions are executed and the response echoes them back. There is
          no upper bound here beyond what fits in one transaction.
        items:
          $ref: '#/definitions/v2.SystemTransferCollectSource'
        type: array
    type: object
  v2.SystemTransferCollectResponse:
    properties:
      account_keys:
        description: |-
          AccountKeys is shorter than the source list plus two. The recipient
          appears in every instruction and the System Program in all of them, yet
          each is one key here: compiling a message deduplicates account keys and
          the instructions address them by index.
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      recipient:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: |-
          Recipient reports the account credited and the total collected into it
          across every transfer.
      signers:
        description: |-
          Signers lists every source alongside fee_payer: unlike transfer/spread,
          this is a multi-party transaction, and each source signs only for its
          own instruction.
        items:
          type: string
        type: array
      size:
        description: |-
          Size and SizeLimit are what bounds this endpoint. A transaction travels
          in one packet and cannot be split, so the transfer count is really a
          byte count, and reporting both lets a caller work out how many more
          would fit rather than discovering it by being refused.
        type: integer
      size_limit:
        type: integer
      transaction:
        type: string
      transfers:
        items:
          $ref: '#/definitions/v2.SystemTransferCollectSourceResponse'
        type: array
    type: object
  v2.SystemTransferCollectSource:
    properties:
      funding_payer:
        description: |-
          FundingPayer is debited. It signs the transaction as the transfer
          authority for its own instruction, whether or not it also pays the fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      lamports:
        description: |-
          Lamports is the raw amount moved from FundingPayer to the request's
          RecipientAccount. It must be left unset when Max is true: the two name
          the amount in mutually exclusive ways, and naming both leaves no
          reading of which one is meant.
        example: "100000000"
        type: string
      max:
        description: |-
          Max sweeps FundingPayer's entire balance instead of a chosen amount.
          When it is also FeePayer, the fee comes out of that same balance first;
          every other source's Lamports is untouched by any fee.
        example: false
        type: boolean
    type: object
  v2.SystemTransferCollectSourceResponse:
    properties:
      funding_payer:
        type: string
      lamports:
        type: string
      max:
        type: boolean
      sol:
        type: string
    type: object
  v2.SystemTransferMaxRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as FundingPayer, in which case the fee is deducted from what
          is sent.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      funding_payer:
        description: |-
          FundingPayer is the account drained. It signs the transaction as the
          transfer authority, whether or not it also pays the fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited. It must already exist: this
          is a plain transfer between two accounts, not a way to bring a new
          one into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.SystemTransferMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      funding:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemTransferRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as FundingPayer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      funding_payer:
        description: |-
          FundingPayer is the account debited. It signs the transaction as the
          transfer authority, whether or not it also pays the fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      lamports:
        description: Lamports is the raw amount moved from FundingPayer to RecipientAccount.
        example: "1000000"
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited. It must already exist: this
          is a plain transfer between two accounts, not a way to bring a new one
          into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.SystemTransferResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      funding:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: 'Funding reports the transfer itself: who sent it and how much.'
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries
          a stored value rather than a fetched blockhash and the transaction does
          not expire. It is reported because the request never named it: advancing
          the nonce is the first instruction and that key has to sign, and the
          server read it off the account.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.SystemTransferSpreadRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. It may be the same
          account as FundingPayer, in which case the fee is deducted from its
          balance alongside every transfer.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      funding_payer:
        description: |-
          FundingPayer is the account debited for every transfer. It signs the
          transaction as the transfer authority, whether or not it also pays the
          fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      transfers:
        description: |-
          Transfers lists the recipients and their amounts, in the order the
          instructions are executed and the response echoes them back. There is
          no upper bound here beyond what fits in one transaction.
        items:
          $ref: '#/definitions/v2.SystemTransferSpreadTarget'
        type: array
    type: object
  v2.SystemTransferSpreadResponse:
    properties:
      account_keys:
        description: |-
          AccountKeys is shorter than the transfer list plus two. The sender
          appears in every instruction and the System Program in all of them, yet
          each is one key here: compiling a message deduplicates account keys and
          the instructions address them by index. This is the first endpoint whose
          response shows that.
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      funding:
        allOf:
        - $ref: '#/definitions/v2.SystemPayer'
        description: Funding is the funding_payer's total spend across every transfer.
      message:
        type: string
      nonce_authority:
        description: |-
          NonceAuthority is present only when the transaction was built against a
          durable nonce, so it doubles as the signal that RecentBlockhash carries a
          stored value rather than a fetched blockhash.
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      size:
        description: |-
          Size and SizeLimit are what bounds this endpoint. A transaction travels
          in one packet and cannot be split, so the recipient count is really a
          byte count, and reporting both lets a caller work out how many more
          would fit rather than discovering it by being refused.
        type: integer
      size_limit:
        type: integer
      transaction:
        type: string
      transfers:
        items:
          $ref: '#/definitions/v2.SystemTransferSpreadTargetResponse'
        type: array
    type: object
  v2.SystemTransferSpreadTarget:
    properties:
      lamports:
        description: |-
          Lamports is the raw amount moved from the request's FundingPayer to
          RecipientAccount.
        example: "100000000"
        type: string
      recipient_account:
        description: |-
          RecipientAccount is the account credited. It must already exist: this
          is a plain transfer between accounts, not a way to bring a new one
          into existence.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
    type: object
  v2.SystemTransferSpreadTargetResponse:
    properties:
      lamports:
        type: string
      recipient_account:
        type: string
      sol:
        type: string
    type: object
  v2.ThawAccountRequest:
    properties:
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      freeze_authority:
        description: FreezeAuthority must be Mint's freeze authority exactly.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what TokenAccount must hold, and is the source of the freeze
          authority checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      token_account:
        description: |-
          TokenAccount is resumed. It must already exist, hold Mint, and
          currently be frozen.
        example: ""
        type: string
    type: object
  v2.ThawAccountResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      freeze_authority:
        type: string
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      token_account:
        type: string
      transaction:
        type: string
    type: object
  v2.TransferCheckedMaxRequest:
    properties:
      destination_token_account:
        description: |-
          DestinationTokenAccount is credited. It must already exist, hold Mint,
          and not be frozen: a transfer moves between *token accounts*, never to
          a wallet address directly.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. A transfer moves no
          lamports of its own, so this is the only balance this endpoint ever
          checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what both token accounts must hold, and is the source of the
          decimals checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account:
        description: |-
          SourceTokenAccount is swept to zero. It must already exist, hold Mint,
          and not be frozen.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is SourceTokenAccount's owner, or its
          delegate for no more than what was delegated — checked against the
          balance actually swept, resolved at request time, not a caller-given
          amount.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.TransferCheckedMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      destination_token_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.TransferCheckedRequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to move, not a UI decimal string.
        example: "250000"
        type: string
      decimals:
        description: |-
          Decimals is checked against Mint's own stored value rather than
          trusted, which is the whole point of the checked variant: catching a
          client that formatted Amount against the wrong decimals as a 400
          instead of an on-chain failure.
        example: 6
        type: integer
      destination_token_account:
        description: |-
          DestinationTokenAccount is credited. It must already exist, hold Mint,
          and not be frozen: a transfer moves between *token accounts*, never to
          a wallet address directly, and a caller with only a wallet address
          needs the recipient's associated token account created first.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. A transfer moves no
          lamports of its own, so this is the only balance this endpoint ever
          checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what both token accounts must hold, and is the source of the
          decimals checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022. It is required rather than defaulted, since a token
          account belongs to exactly one of the two forever, and it must agree
          with the mint's own owning program or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account:
        description: |-
          SourceTokenAccount is debited. It must already exist, hold Mint, and
          not be frozen.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is SourceTokenAccount's owner, or its
          delegate for no more than what was delegated. A transfer spends a
          balance, so it is the holder's to authorize, not the mint's — unlike
          minting, which checks the mint's own authority instead.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.TransferCheckedResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      destination_token_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.TransferCheckedWithFeeRequest:
    properties:
      amount:
        description: |-
          Amount is the raw base-unit count to move, not a UI decimal string.
          DestinationTokenAccount is credited Amount minus Fee.
        example: "250000"
        type: string
      decimals:
        description: |-
          Decimals is checked against Mint's own stored value rather than
          trusted, the same catch every checked variant makes.
        example: 6
        type: integer
      destination_token_account:
        description: |-
          DestinationTokenAccount is credited Amount minus Fee, and is where
          Fee itself accumulates as withheld -- it must already have the
          TransferFeeAmount extension (see reallocate/transfer-fee-amount).
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what both token accounts must hold, and is the source of the
          decimals checked against; it must already have the TransferFeeConfig
          extension (see initialize-transfer-fee-config).
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          the authority itself does not sign; the named members do, in its
          place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold
          TransferFeeConfig.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account:
        description: |-
          SourceTokenAccount is debited. It must already exist, hold Mint, and
          not be frozen.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is SourceTokenAccount's owner, or its
          delegate for no more than what was delegated.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.TransferCheckedWithFeeResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      destination_token_account:
        type: string
      epoch:
        description: |-
          Epoch is which epoch's rate TransferFee was computed against: Mint's
          newer_transfer_fee if this epoch has reached the one it is stamped
          with, its older_transfer_fee otherwise. A transaction built here and
          sent after the epoch rolls over no longer matches what the program
          will demand and has to be rebuilt.
        type: integer
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
      transfer_fee:
        description: |-
          TransferFee is the amount withheld into DestinationTokenAccount's
          TransferFeeAmount extension, computed from Mint's TransferFeeConfig
          at Epoch -- not a value this request ever supplies.
        type: string
    type: object
  v2.TransferFromATAMaxRequest:
    properties:
      destination_token_account:
        description: |-
          DestinationTokenAccount is credited. It must already exist, hold Mint,
          and not be frozen — an exact address, not a wallet to derive from.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. A transfer moves no
          lamports of its own, so this is the only balance this endpoint ever
          checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what the derived source and DestinationTokenAccount must both
          hold.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is the sender's wallet. Its associated token account is derived
          from Owner and Mint rather than accepted directly, and is swept to
          zero.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program is a seed of the derived source address, not only the program
          both token accounts belong to, so one owner has a different
          associated account for classic Token than for Token-2022 over the
          same mint.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is the derived source account's owner, or
          its delegate for no more than what was delegated — checked against the
          balance actually swept, resolved at request time, not a caller-given
          amount.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.TransferFromATAMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      destination_token_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.TransferFromATARequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to move, not a UI decimal string.
        example: "250000"
        type: string
      decimals:
        description: |-
          Decimals is checked against Mint's own stored value rather than
          trusted, which is the whole point of the checked variant: catching a
          client that formatted Amount against the wrong decimals as a 400
          instead of an on-chain failure.
        example: 6
        type: integer
      destination_token_account:
        description: |-
          DestinationTokenAccount is credited. It must already exist, hold Mint,
          and not be frozen — an exact address, not a wallet to derive from.
        example: Cc81es6UdN5EwjE27Pv4ZFaQhd6yh4XG5n11SNd8pmxo
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. A transfer moves no
          lamports of its own, so this is the only balance this endpoint ever
          checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what the derived source and DestinationTokenAccount must both
          hold, and is the source of the decimals checked against.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      owner:
        description: |-
          Owner is the sender's wallet. Its associated token account is derived
          from Owner and Mint rather than accepted directly, and is never
          created if absent.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      program:
        description: |-
          Program is a seed of the derived source address, not only the program
          both token accounts belong to, so one owner has a different
          associated account for classic Token than for Token-2022 over the
          same mint.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is the derived source account's owner, or
          its delegate for no more than what was delegated. A transfer spends a
          balance, so it is the holder's to authorize, not the mint's.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.TransferFromATAResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      decimals:
        type: integer
      destination_token_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      owner:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.TransferMaxRequest:
    properties:
      destination_token_account:
        example: ""
        type: string
      durable_nonce_account:
        example: ""
        type: string
      fee_payer:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        items:
          type: string
        type: array
      program:
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        example: ""
        type: string
      source_token_account:
        example: ""
        type: string
      source_token_account_authority:
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.TransferMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      destination_token_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.TransferRequest:
    properties:
      amount:
        description: Amount is the raw base-unit count to move, not a UI decimal string.
        example: "250000"
        type: string
      destination_token_account:
        description: |-
          DestinationTokenAccount is credited. It must already exist, hold the
          same mint as SourceTokenAccount, and not be frozen: a transfer moves
          between *token accounts*, never to a wallet address directly.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: |-
          FeePayer signs and pays the transaction fee. A transfer moves no
          lamports of its own, so this is the only balance this endpoint ever
          checks.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token
          or Token-2022. It must agree with both accounts' own owning program
          or the instruction fails on chain.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account:
        description: |-
          SourceTokenAccount is debited. It must already exist and not be
          frozen.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is SourceTokenAccount's owner, or its
          delegate for no more than what was delegated.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.TransferResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        type: string
      destination_token_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.UnwrapLamportsMaxRequest:
    properties:
      destination_token_account:
        description: |-
          DestinationTokenAccount receives the unwrapped lamports directly, as
          plain SOL rather than tokens — it need not be a token account at all.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token
          or Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account:
        description: |-
          SourceTokenAccount is debited and never closed. It must already
          exist, be owned by Program, and actually be a wrapped-SOL account.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is SourceTokenAccount's owner, or its
          delegate for no more than what was delegated.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.UnwrapLamportsMaxResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      destination_token_account:
        type: string
      estimated_amount:
        $ref: '#/definitions/v2.SystemPayer'
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.UnwrapLamportsRequest:
    properties:
      amount:
        description: |-
          Amount is the raw base-unit (lamport) count to unwrap, not a UI
          decimal string.
        example: "250000"
        type: string
      destination_token_account:
        description: |-
          DestinationTokenAccount receives the unwrapped lamports directly, as
          plain SOL rather than tokens — it need not be a token account at all.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token
          or Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_account:
        description: |-
          SourceTokenAccount is debited and never closed. It must already
          exist, be owned by Program, and actually be a wrapped-SOL account.
        example: ""
        type: string
      source_token_account_authority:
        description: |-
          SourceTokenAccountAuthority is SourceTokenAccount's owner, or its
          delegate for no more than what was delegated.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.UnwrapLamportsResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      amount:
        $ref: '#/definitions/v2.SystemPayer'
      destination_token_account:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_account:
        type: string
      source_token_account_authority:
        type: string
      transaction:
        type: string
    type: object
  v2.UpdateConfidentialTransferMintRequest:
    properties:
      auditor_elgamal_pubkey:
        description: |-
          AuditorElgamalPubkey replaces whatever initialize set entirely. Left
          empty, no auditor can decrypt a confidential transfer amount on this
          mint from this point on. Given, it is a raw ElGamal public key
          encoded as base58 -- a 32-byte compressed Ristretto point, not a
          Solana address.
        example: ""
        type: string
      authority:
        description: |-
          Authority is the authority initialize named, or its multisig for a
          multisig-owned authority (see MultisigSigners). This cannot be left
          empty: an authority that was never set has no way to sign this
          instruction in the first place, permanently.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      auto_approve_new_accounts:
        description: AutoApproveNewAccounts replaces whatever initialize set entirely.
        example: true
        type: boolean
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must already have the ConfidentialTransferMint extension (see
          initialize).
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          Authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold this
          extension.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.UpdateConfidentialTransferMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      auditor_elgamal_pubkey:
        type: string
      authority:
        type: string
      auto_approve_new_accounts:
        type: boolean
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.WithdrawExcessLamportsRequest:
    properties:
      account:
        description: |-
          Account is read for its excess, never closed or resized. It must
          already exist and be owned by Program; it may be a mint, a token
          account, or a multisig.
        example: ""
        type: string
      authority:
        description: |-
          Authority is whichever role Account's actual type requires — see the
          type doc. It is not verified against Account here.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      destination:
        description: |-
          Destination receives the recovered lamports. It must already exist;
          this is not a way to bring a new account into existence.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty, the
          authority itself does not sign; the named members do, in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program names the account to send the instruction to: classic Token or
          Token-2022.
        example: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
    type: object
  v2.WithdrawExcessLamportsResponse:
    properties:
      account:
        type: string
      account_keys:
        items:
          type: string
        type: array
      authority:
        type: string
      destination:
        type: string
      estimated_recovered:
        $ref: '#/definitions/v2.SystemPayer'
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
    type: object
  v2.WithdrawWithheldTokensFromAccountsRequest:
    properties:
      destination:
        description: |-
          Destination receives everything withdrawn, summed across every
          source. It must already exist and hold Mint.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint is what every one of SourceTokenAccounts must hold. It is never
          written to by this instruction.
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          WithdrawWithheldAuthority itself does not sign; the named members do,
          in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold
          TransferFeeConfig.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      source_token_accounts:
        description: |-
          SourceTokenAccounts is swept in full: this instruction always
          withdraws each named account's own withheld_amount entirely, there
          is no partial-amount form. At most 255, since the count travels the
          wire as a single byte.
        example:
        - ""
        items:
          type: string
        type: array
      withdraw_withheld_authority:
        description: |-
          WithdrawWithheldAuthority is the authority
          initialize-transfer-fee-config named, or its multisig for a
          multisig-owned authority (see MultisigSigners).
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.WithdrawWithheldTokensFromAccountsResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      destination:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      source_token_accounts:
        items:
          type: string
        type: array
      transaction:
        type: string
      withdraw_withheld_authority:
        type: string
    type: object
  v2.WithdrawWithheldTokensFromMintRequest:
    properties:
      destination:
        description: |-
          Destination receives the withdrawn tokens. It must already exist and
          hold Mint.
        example: ""
        type: string
      durable_nonce_account:
        description: |-
          DurableNonceAccount may be left empty, in which case the message is
          built against RecentBlockhash directly and expires with it. Naming one
          builds the message against the value that account stores instead, so it
          never expires, and prepends the advance that consumes it; RecentBlockhash
          is then used only to price the transaction. The authority is not a
          field: it is read from the account, since it is a fact about it rather
          than a choice.
        example: ""
        type: string
      fee_payer:
        description: FeePayer signs and pays the transaction fee.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
      mint:
        description: |-
          Mint must already have the TransferFeeConfig extension, with some
          amount already harvested into it (see harvest-withheld-tokens-to-mint).
        example: ""
        type: string
      multisig_signers:
        description: |-
          MultisigSigners is empty for a single-signer authority. Non-empty,
          WithdrawWithheldAuthority itself does not sign; the named members do,
          in its place.
        items:
          type: string
        type: array
      program:
        description: |-
          Program must be Token-2022. A classic Token mint can never hold
          TransferFeeConfig.
        example: TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb
        type: string
      recent_blockhash:
        description: |-
          RecentBlockhash is always required, and there is no server-side fetch
          behind it: this builds the message against exactly the value given,
          which expires whenever the runtime says it does. When
          DurableNonceAccount is also named, this is not what the message is
          built against — it is only what prices it, since a nonce is never among
          the cluster's recent blockhashes and pricing against one directly comes
          back expired.
        example: ""
        type: string
      withdraw_withheld_authority:
        description: |-
          WithdrawWithheldAuthority is the authority
          initialize-transfer-fee-config named, or its multisig for a
          multisig-owned authority (see MultisigSigners). Unlike
          TransferFeeConfigAuthority, this cannot be left empty when set up:
          an authority that was never named has no way to sign this
          instruction, permanently.
        example: EodYvwsT22JTdNmvCeC974WjPiVYcxvfGpYLJxnB3JqK
        type: string
    type: object
  v2.WithdrawWithheldTokensFromMintResponse:
    properties:
      account_keys:
        items:
          type: string
        type: array
      destination:
        type: string
      fee:
        $ref: '#/definitions/v2.SystemPayer'
      message:
        type: string
      mint:
        type: string
      nonce_authority:
        type: string
      program:
        type: string
      recent_blockhash:
        type: string
      signers:
        items:
          type: string
        type: array
      transaction:
        type: string
      withdraw_withheld_authority:
        type: string
    type: object
host: localhost:33153
info:
  contact: {}
  description: Solana transaction API
  title: SVMlab API
  version: "1.0"
paths:
  /svm/account/airdrop:
    post:
      consumes:
      - application/json
      description: Requests a fixed half a SOL. The amount is not a field because
        the faucet enforces its own limit, and asking above it fails the call rather
        than handing out less.
      parameters:
      - description: Account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/account.AirdropRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/account.AirdropResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund an account on devnet or testnet
      tags:
      - account
  /svm/account/authority:
    post:
      consumes:
      - application/json
      description: 'Reports the authority for whichever kind of account public_key
        names: a durable nonce account''s authority, a mint''s mint_authority and
        freeze_authority, a token account''s token_owner, delegate, and close_authority,
        or a multisig''s m, n, and signers. type says which of these applies, and
        is system_account for a plain account with none of them, or unknown for a
        program-owned account this endpoint has no parser for.'
      parameters:
      - description: Account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/account.AccountAuthorityRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/account.AccountAuthorityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read the authority-bearing fields of an account
      tags:
      - account
  /svm/account/balance:
    post:
      consumes:
      - application/json
      parameters:
      - description: Account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/account.AccountRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/account.BalanceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read an account's balance
      tags:
      - account
  /svm/account/nonce:
    post:
      consumes:
      - application/json
      description: 'Parses the 80 bytes a nonce account holds. The stored nonce is
        what a transaction may carry in place of a recent blockhash so that it never
        expires, which is the opposite of an EVM nonce: there a per-account counter
        prevents replay, here expiry is what is being removed. An account sized by
        create-account but never initialized is a normal intermediate state and reports
        initialized=false with all-zero fields.'
      parameters:
      - description: Nonce account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/account.NonceRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/account.NonceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read a durable nonce account
      tags:
      - account
  /svm/account/owner:
    post:
      consumes:
      - application/json
      description: Only the owning program may debit an account or write its data.
        An account owned by anything other than the System Program cannot be moved
        with a system transfer, and one owned by a non-executable address cannot be
        moved at all, so system_owned answers whether the balance is still reachable.
      parameters:
      - description: Account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/account.AccountOwnerRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/account.AccountOwnerResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read who owns an account
      tags:
      - account
  /svm/account/state:
    post:
      consumes:
      - application/json
      description: Returns the owner, executable flag, data size, balance, and data.
        An account that does not exist is reported with exists=false rather than as
        an error, since most 32-byte values name one nobody has created.
      parameters:
      - description: Account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/account.AccountRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/account.AccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read an account's on-chain state
      tags:
      - account
  /svm/account/tokens:
    post:
      consumes:
      - application/json
      description: Lists token accounts owned by a wallet under both the classic Token
        Program and Token-2022. The response keeps the program on each account because
        the two programs own separate accounts and no single token instruction can
        touch both at once.
      parameters:
      - description: Wallet
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/account.TokensRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/account.TokensResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: List the token accounts a wallet owns
      tags:
      - account
  /svm/cluster/blockhash:
    post:
      description: Returns the blockhash to build against and the block height past
        which it is rejected, so expiry can be checked rather than guessed. Fetched
        at finalized commitment, since a blockhash from a less settled view risks
        belonging to a fork.
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.BlockhashResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read a recent blockhash
      tags:
      - cluster
  /svm/cluster/genesis-hash:
    post:
      description: A genesis hash is not folded into a signature the way EIP-155 binds
        a chain id, so nothing on chain prevents a transaction from replaying on another
        cluster. Comparing what the endpoint reports against what config names is
        the substitute check.
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.GenesisHashResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read the cluster's genesis hash and compare it to config
      tags:
      - cluster
  /svm/cluster/health:
    post:
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.HealthResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read the node's health
      tags:
      - cluster
  /svm/cluster/slot:
    post:
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.SlotResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read the current slot
      tags:
      - cluster
  /svm/cluster/transaction/fee:
    post:
      consumes:
      - application/json
      description: The fee follows from the signature count and any compute budget
        instructions, both of which live in the message, so it is known before signing
        and cannot be exceeded by execution the way a gas estimate can.
      parameters:
      - description: Message
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.FeeRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.FeeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Price a message
      tags:
      - cluster
  /svm/cluster/transaction/refresh-blockhash:
    post:
      consumes:
      - application/json
      description: Swaps in a fresh blockhash at finalized commitment, so a transaction
        built too long ago can still be signed and sent instead of failing "blockhash
        not found". transaction is base64. The transaction must be unsigned — refreshing
        after signing would leave every signature invalid without anything here able
        to tell, so it is refused rather than silently returned broken.
      parameters:
      - description: Transaction
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.RefreshBlockhashRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RefreshBlockhashResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Replace an unsigned transaction's recent blockhash
      tags:
      - cluster
  /svm/cluster/transaction/replace-blockhash-with-nonce:
    post:
      consumes:
      - application/json
      description: 'Recompiles a transaction so it draws its message blockhash from
        a durable nonce account and advances that nonce as its first instruction,
        which is what makes it stop expiring. transaction is base64 and must be unsigned:
        this rebuilds the account list rather than editing it, so any existing signature
        covers a message that no longer exists. The nonce authority is read off the
        account rather than taken from the request, and it becomes a required signer,
        so read signers in the response rather than assuming it matches what went
        in. A legacy transaction and a versioned (v0) one that declares no address
        lookup tables are both accepted; the result is legacy either way. A v0 transaction
        that does use lookup tables is refused, since the accounts its instructions
        point at are not in the message.'
      parameters:
      - description: Transaction and nonce account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ReplaceBlockhashWithNonceRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ReplaceBlockhashWithNonceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Rebuild an unsigned transaction against a durable nonce
      tags:
      - cluster
  /svm/cluster/transaction/send:
    post:
      consumes:
      - application/json
      description: Submits a fully signed transaction to the cluster and returns its
        signature. transaction is base64. Never parses the message inside, only checks
        that every signature slot is filled, so a versioned (v0) transaction is accepted
        the same as a legacy one. Acceptance is not execution; the transaction still
        has to land in a block.
      parameters:
      - description: Signed transaction
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.SendTransactionRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.SendTransactionResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Broadcast a signed transaction
      tags:
      - cluster
  /svm/cluster/transaction/simulate:
    post:
      consumes:
      - application/json
      description: Runs a fully signed transaction against the node's state without
        broadcasting it, and returns the program logs either way. transaction is base64.
        Never parses the message inside, only checks that every signature slot is
        filled, so a versioned (v0) transaction is accepted the same as a legacy one.
        The logs are the only account of why execution stopped; nothing here corresponds
        to a revert string.
      parameters:
      - description: Transaction
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.SimulateTransactionRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.SimulateTransactionResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Execute a transaction without submitting it
      tags:
      - cluster
  /svm/cluster/transaction/status:
    post:
      consumes:
      - application/json
      description: 'Stands in for an EVM receipt, but found can stay false forever:
        a transaction whose blockhash expired is forgotten and never lands.'
      parameters:
      - description: Signature
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.SignatureStatusRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.SignatureStatusResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read a transaction's status
      tags:
      - cluster
  /svm/cluster/version:
    post:
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.VersionResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read the node's software version
      tags:
      - cluster
  /svm/protocol/rent-exemption/mint:
    post:
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for an SPL Token mint
      tags:
      - protocol
  /svm/protocol/rent-exemption/nonce:
    post:
      description: A nonce account has to stay rent exempt to keep holding its nonce,
        so this is what one must be funded with. It is also the floor a partial withdrawal
        has to leave behind.
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionNonceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for a durable nonce account
      tags:
      - protocol
  /svm/protocol/rent-exemption/public-key:
    post:
      consumes:
      - application/json
      description: Reads the size from a live account, so the caller does not have
        to know the layout. Pass a byte count to the space endpoint instead when the
        account does not exist yet.
      parameters:
      - description: Account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.RentExemptionPublicKeyRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionPublicKeyResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for an account of the same size as an existing one
      tags:
      - protocol
  /svm/protocol/rent-exemption/space:
    post:
      consumes:
      - application/json
      description: Takes a byte count rather than an account kind, which covers layouts
        none of the named endpoints describe and sizes no live account holds yet.
      parameters:
      - description: Space
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.RentExemptionSpaceRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionSpaceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for a size given directly
      tags:
      - protocol
  /svm/protocol/rent-exemption/stake:
    post:
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionStakeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for a stake account
      tags:
      - protocol
  /svm/protocol/rent-exemption/system:
    post:
      description: A wallet holds lamports and no data, so this is the floor any account
        must clear. It is also what a transfer to a previously unused address has
        to meet, since the transfer creates the account.
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionSystemResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for a plain wallet
      tags:
      - protocol
  /svm/protocol/rent-exemption/token:
    post:
      description: Every token balance lives in an account of its own, so opening
        a position in a new token costs this much before any tokens move.
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionTokenResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for an SPL Token account
      tags:
      - protocol
  /svm/protocol/rent-exemption/vote:
    post:
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.RentExemptionVoteResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Minimum balance for a validator vote account
      tags:
      - protocol
  /svm/rpc/:
    post:
      consumes:
      - application/json
      description: Passes a method straight through, so anything this API has not
        wrapped stays reachable.
      parameters:
      - description: Method and params
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.RawRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Call any JSON-RPC method
      tags:
      - rpc
  /svm/rpc/batch:
    post:
      consumes:
      - application/json
      parameters:
      - description: Calls
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.BatchRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Call several JSON-RPC methods in one round trip
      tags:
      - rpc
  /svm/sign:
    post:
      consumes:
      - application/json
      description: 'Signs the given bytes with a key resolved from config.yaml and
        returns the bare signature, assembling nothing around it. The input is the
        message itself, not a digest: ed25519 hashes internally, so a pre-hashed input
        yields a signature no verifier holding the real message can check. Pass the
        message field a build returns to reproduce what the transaction signer does.'
      parameters:
      - description: Signer public key and message
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.SignRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.SignResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Sign arbitrary bytes
      tags:
      - sign
  /svm/sign/transaction:
    post:
      consumes:
      - application/json
      description: 'Signs the message inside a serialized transaction and writes each
        signature into its signer''s slot, found from the key rather than from the
        order given. transaction is base64. The message is never rebuilt, so the bytes
        a caller signs are exactly the bytes they were given. A versioned (v0) message
        is accepted and signed without being parsed: a signer must be one of its leading
        static keys, since an address lookup table entry can never sign, so locating
        a slot needs no address table lookups. The response is thinner for one, since
        nothing here reads what the transaction does. public_keys are resolved from
        config.yaml and private_keys carry the secret directly, for a signer such
        as a newly created account that is not registered; the two may be mixed in
        one call. Keys may also be named across several calls, so co-signers can complete
        a transaction one at a time.'
      parameters:
      - description: Transaction and its signers
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.SignTransactionRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.SignTransactionResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Sign a serialized transaction
      tags:
      - sign
  /svm/sign/verify:
    post:
      consumes:
      - application/json
      description: Checks a signature against a message and a public key. The key
        is required and cannot be recovered from the signature, since ed25519 offers
        no equivalent of ecrecover. Any key may be given, not only one from config.
      parameters:
      - description: Public key, message, and signature
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.VerifyRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.VerifyResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a signature
      tags:
      - sign
  /svm/token/account:
    post:
      consumes:
      - application/json
      description: 'Parses the 165 bytes a token account holds. owner is not the runtime
        owner: that is the token program, which is what may write the data, while
        owner here is the wallet whose signature the program accepts. amount is base
        units with no scale of its own, so the mint is read as well to report amount_ui;
        when the mint cannot be read the base units are still returned and decimals
        is omitted rather than guessed. Works for both classic Token and Token-2022,
        whose extensions sit past the base layout.'
      parameters:
      - description: Token account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.AccountRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.AccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read an SPL Token holder account
      tags:
      - token
  /svm/token/amount-to-ui:
    post:
      consumes:
      - application/json
      description: 'Builds AmountToUiAmount and simulates it — never sends it, since
        it writes nothing. In this version of the program a mint can only specify
        decimals, so against a plain mint this is exactly amount / 10^decimals and
        could be computed without the network at all; it earns its keep only against
        a Token-2022 mint carrying the interest-bearing extension, where the true
        UI string includes interest accrued since the mint''s last update and only
        the program can compute that. fee_payer is required even though nothing is
        ever sent: the node still checks the simulated payer can afford the fee before
        running the instruction.'
      parameters:
      - description: Mint, raw amount, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.AmountToUiRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.AmountToUiResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Format a raw token amount as a UI string, using the mint's own program
      tags:
      - token
  /svm/token/ata/derive:
    post:
      consumes:
      - application/json
      description: 'Returns the address create-ata and create-ata-idempotent will
        create and everything else in this API that takes owner + mint derives internally,
        plus the canonical bump: PDA.Find always searches from 255 downward and stops
        at the first off-curve point, which is not a choice — the Associated Token
        Account program recomputes and validates that same bump internally and accepts
        no other. This is a pure computation, not a chain read: it never checks whether
        the derived address actually exists.'
      parameters:
      - description: Owner, mint, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.ATADeriveRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.ATADeriveResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Derive the canonical associated token account for a wallet and mint
      tags:
      - token
  /svm/token/ata/validate:
    post:
      consumes:
      - application/json
      description: Recomputes the associated token account for owner, mint, and program
        the same way ata/derive does, and compares it against associated_token_account.
        valid is true only when the two match exactly; derived is always returned
        so a caller who gets false back knows the right address without a second call.
        Like ata/derive, this never checks whether either address actually exists
        on chain — it is purely a derivation check.
      parameters:
      - description: Owner, mint, program, and the address to check
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.ATAValidateRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.ATAValidateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Check a supplied address against the canonical associated token account
      tags:
      - token
  /svm/token/extensions/get-account-data-size:
    post:
      consumes:
      - application/json
      description: 'Builds GetAccountDataSize and simulates it — never sends it, since
        it writes nothing. Returns the same authority Reallocate itself defers to,
        asked directly rather than recomputed client-side: a hardcoded copy of the
        program''s own extension-size table would drift the moment the program changes
        a layout, the same reason a rent-exemption minimum is always asked of the
        cluster rather than assumed. extension_types may be empty, in which case the
        response is the bare size a Token-2022 account with no extensions needs. fee_payer
        is required even though nothing is ever sent: the node still checks the simulated
        payer can afford the fee before running the instruction.'
      parameters:
      - description: Mint, extension types, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.GetAccountDataSizeRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.GetAccountDataSizeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Ask the program for the exact byte size an account needs for a mint
        and a set of extensions
      tags:
      - token
  /svm/token/extensions/mint/data-size:
    post:
      consumes:
      - application/json
      description: 'Computed client-side, never asked of the chain: GetAccountDataSize
        only ever answers "how big does an account need to be to hold this mint",
        never "how big does this mint itself need to be" -- a different question the
        interface crate exposes no return-data instruction for at all. create-mint''s
        own System.CreateAccount always sizes for a bare 82-byte mint; a mint that
        will carry any extension has to be created at this size instead, with create-mint
        bypassed in favor of system/create-account directly. Each named type must
        be a mint extension, not a token-account one (rejected here the same way the
        deployed program would reject it on chain, without the round trip). token_metadata
        is also rejected: its size depends on actual name/symbol/uri content this
        endpoint never sees.'
      parameters:
      - description: Extension types the new mint will carry
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.MintDataSizeRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.MintDataSizeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Compute the byte size a new mint needs for a set of extensions
      tags:
      - token
  /svm/token/mint:
    post:
      consumes:
      - application/json
      description: 'Parses the 82 bytes a mint account holds. A mint is the closest
        thing Solana has to an ERC-20 contract, and what it lacks is the point: no
        balances and no allowances, since those live in separate token accounts owned
        by each holder. mint_authority and freeze_authority are omitted when absent,
        which is different from being the zero address; a mint whose mint authority
        was removed has a permanently fixed supply, and one initialized without a
        freeze authority can never gain one. Works for both classic Token and Token-2022,
        whose extensions sit past the base layout.'
      parameters:
      - description: Mint account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.MintRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.MintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Read an SPL Token mint
      tags:
      - token
  /svm/token/ui-to-amount:
    post:
      consumes:
      - application/json
      description: 'Builds UiAmountToAmount and simulates it — never sends it, since
        it writes nothing. AmountToUi''s inverse, for the same reason: against a plain
        mint this is a decimals multiply a client could do itself, and it earns its
        keep only against a Token-2022 mint carrying the interest-bearing extension.
        fee_payer is required even though nothing is ever sent: the node still checks
        the simulated payer can afford the fee before running the instruction.'
      parameters:
      - description: Mint, UI amount string, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/token.UiToAmountRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/token.UiToAmountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Parse a UI amount string back into a raw token amount, using the mint's
        own program
      tags:
      - token
  /svm/tool/convert/base58264:
    post:
      consumes:
      - application/json
      description: Decodes value as base58 and re-encodes the same bytes as base64.
        Works on any base58 value — a public key, a signature, a hash — since it never
        interprets what the bytes mean.
      parameters:
      - description: Value
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ConvertBase58To64Request'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ConvertBase58To64Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Convert a base58 string to base64
      tags:
      - tool
  /svm/tool/convert/base64258:
    post:
      consumes:
      - application/json
      description: Decodes value as base64 and re-encodes the same bytes as base58.
        Works on any base64 value — a transaction, a message, arbitrary bytes — since
        it never interprets what the bytes mean.
      parameters:
      - description: Value
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ConvertBase64To58Request'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ConvertBase64To58Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Convert a base64 string to base58
      tags:
      - tool
  /svm/tool/derive/ae-key:
    post:
      consumes:
      - application/json
      description: 'Reproduces AeKey::new_from_signer''s second half: given the Ed25519
        signature over ae-key-seed-message''s own output, returns the AeKey that signature
        determines (two rounds of SHA3-512, keeping the first 16 bytes of the second).
        A real wallet rederiving the same key needs the same signature every time,
        so this never generates one itself.'
      parameters:
      - description: Signature over ae-key-seed-message's output
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.DeriveAeKeyRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.DeriveAeKeyResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Derive an AeKey from a signature
      tags:
      - tool
  /svm/tool/derive/ae-key-seed-message:
    post:
      consumes:
      - application/json
      description: Returns the exact bytes upstream's AeKey::seed_from_signer signs
        to derive an account's AeKey deterministically -- b"AeKey" followed by token_account's
        own bytes, the public seed convention real tooling (solana-foundation's Confidential-Balances-Sample)
        uses. Sign the returned message (base64) with the account owner's own key
        via sign/, then pass the resulting signature to derive/ae-key.
      parameters:
      - description: Token account
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.AeKeySeedMessageRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.AeKeySeedMessageResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the message to sign for an account's AeKey
      tags:
      - tool
  /svm/tool/generate/ed25519-keypair:
    post:
      description: 'Returns a new key pair in the base58 form config.yaml expects,
        so it can be added as a keys entry and used as a signer. Nothing is written
        or funded: the address does not exist on any cluster until a transaction creates
        it, which is what makes a fresh pair usable as the new_account of a create-account
        build.'
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.GenerateKeypairResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Generate an ed25519 key pair
      tags:
      - tool
  /svm/tool/generate/elgamal-keypair:
    post:
      description: 'Returns a new ElGamal key pair for Token-2022''s ConfidentialTransfer
        family -- auditor_elgamal_pubkey on extensions/confidential-transfer-mint/initialize
        is one consumer. Neither value is a Solana address: both are base58-encoded
        raw 32-byte ristretto255 values (a scalar and a group element), not ed25519
        keys, and cannot sign a transaction or hold lamports.'
      parameters:
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.GenerateElGamalKeypairResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Generate a ristretto255 ElGamal key pair
      tags:
      - tool
  /svm/tool/prove/confidential-burn:
    post:
      consumes:
      - application/json
      description: 'Builds the commitment-equality, batched grouped 3-handle validity
        and batched u128 range proofs a single confidential burn requires, in one
        call, because they are built from the same fresh randomness and only agree
        with each other if drawn together. The mint and the source account are named
        rather than their contents passed in: the source''s available balance ciphertext,
        decryptable balance and ElGamal key, and the mint''s supply and auditor keys
        are read from chain, since the deployed program compares the proofs against
        exactly those. source_elgamal_secret_key has to match the source''s ElGamal
        key and ae_key has to decrypt its decryptable balance, and both are checked,
        as is the amount against that balance. Each *_proof_data blob goes to the
        matching zk-elgamal-proof/context-state/verify endpoint, and auditor_ciphertext_lo/hi
        and new_decryptable_available_balance are what the burn instruction itself
        carries. The response cannot be rebuilt: a second call draws new randomness
        and produces proofs that no longer match any context-state account already
        verified from the first. The source''s available balance must not change between
        building these proofs and the burn landing.'
      parameters:
      - description: Mint, source, source ElGamal secret, AE key, amount
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialBurnRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialBurnResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the three proofs one confidential Burn needs
      tags:
      - tool
  /svm/tool/prove/confidential-empty-account:
    post:
      consumes:
      - application/json
      description: Proves that the account's available balance ciphertext encrypts
        zero under its ElGamal key. zero_ciphertext_proof_data goes to zk-elgamal-proof/context-state/verify/zero-ciphertext.
        The ciphertext must already encrypt zero -- withdraw the whole balance first
        -- or the proof will not verify against the deployed program. This does not
        check that itself, since a confidential balance cannot be read without its
        AE key.
      parameters:
      - description: EmptyAccount inputs
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialEmptyAccountRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialEmptyAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the zero-ciphertext proof one confidential EmptyAccount needs
      tags:
      - tool
  /svm/tool/prove/confidential-mint:
    post:
      consumes:
      - application/json
      description: 'Builds the commitment-equality, batched grouped 3-handle validity
        and batched u128 range proofs a single confidential mint requires, in one
        call, because they are built from the same fresh randomness and only agree
        with each other if drawn together. The mint and the destination account are
        named rather than their contents passed in: the mint''s current confidential
        supply and decryptable supply, the supply and auditor ElGamal keys, and the
        destination''s ElGamal key are read from chain, since the deployed program
        compares the proofs against exactly those. supply_elgamal_secret_key has to
        match the mint''s supply key and supply_ae_key has to decrypt its decryptable
        supply, and both are checked. Each *_proof_data blob goes to the matching
        zk-elgamal-proof/context-state/verify endpoint, and auditor_ciphertext_lo/hi
        and new_decryptable_supply are what the mint instruction itself carries. The
        response cannot be rebuilt: a second call draws new randomness and produces
        proofs that no longer match any context-state account already verified from
        the first. The mint''s supply must not change between building these proofs
        and the mint landing.'
      parameters:
      - description: Mint, destination, supply keys, amount
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialMintRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the three proofs one confidential Mint needs
      tags:
      - tool
  /svm/tool/prove/confidential-rotate-supply-elgamal-pubkey:
    post:
      consumes:
      - application/json
      description: Decrypts the mint's confidential supply with the current supply
        ElGamal secret key, re-encrypts it under new_supply_elgamal_pubkey, and proves
        the two ciphertexts encrypt the same value. The mint's supply ciphertext and
        key are read from chain, since the deployed program compares the proof against
        exactly those, and the given secret is checked against the mint's supply key.
        ciphertext_ciphertext_equality_proof_data goes to zk-elgamal-proof/context-state/verify/ciphertext-ciphertext-equality.
        The supply must fit in 32 bits. The response goes stale if a mint or burn
        changes the supply before the rotation lands, and the mint's pending burn
        must be zero when it does.
      parameters:
      - description: Mint, current supply secret, new supply key
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialRotateSupplyElGamalPubkeyRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialRotateSupplyElGamalPubkeyResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the proof one RotateSupplyElGamalPubkey needs
      tags:
      - tool
  /svm/tool/prove/confidential-transfer:
    post:
      consumes:
      - application/json
      description: 'Builds the equality, ciphertext-validity, and range proofs a single
        confidential transfer requires, in one call, because they are built from the
        same fresh randomness and only agree with each other if drawn together. Each
        proof_data blob goes to the matching zk-elgamal-proof/context-state/verify
        endpoint (equality_proof_data to verify/ciphertext-commitment-equality, validity_proof_data
        to verify/batched-grouped-ciphertext-3-handles-validity, range_proof_data
        to verify/batched-range-proof-u128), and auditor_ciphertext_lo/hi and new_source_decryptable_available_balance
        are what the transfer instruction itself carries. The response cannot be rebuilt:
        a second call draws new randomness and produces proofs that no longer match
        any context-state account already verified from the first. The source balance
        must not change between building these proofs and the transfer landing, or
        the transfer''s own checks against the stored balance fail.'
      parameters:
      - description: Transfer inputs
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialTransferRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialTransferResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the three proofs one ConfidentialTransfer needs
      tags:
      - tool
  /svm/tool/prove/confidential-transfer-with-fee:
    post:
      consumes:
      - application/json
      description: 'Builds the equality, transfer-amount validity, percentage-with-cap,
        fee validity, and 256-bit range proofs a single confidential transfer on a
        fee-charging mint requires, in one call, because they are built from the same
        fresh randomness and only agree with each other if drawn together. The mint
        is named rather than its parameters being passed in: the transfer fee rate
        and cap in effect this epoch (TransferFeeConfig), the auditor key (ConfidentialTransferMint),
        and the withdraw withheld authority key (ConfidentialTransferFeeConfig) are
        read from it, since the deployed program recomputes them from the same place
        and rejects proofs built for different values. Each *_proof_data blob goes
        to the matching zk-elgamal-proof/context-state/verify endpoint, and auditor_ciphertext_lo/hi
        and new_source_decryptable_available_balance are what the transfer-with-fee
        instruction itself carries. The response cannot be rebuilt: a second call
        draws new randomness and produces proofs that no longer match any context-state
        account already verified from the first. The source balance must not change,
        and the epoch must not roll over into a different fee, between building these
        proofs and the transfer landing.'
      parameters:
      - description: Transfer-with-fee inputs
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialTransferWithFeeRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialTransferWithFeeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the five proofs one confidential TransferWithFee needs
      tags:
      - tool
  /svm/tool/prove/confidential-withdraw:
    post:
      consumes:
      - application/json
      description: 'Builds the equality and 64-bit range proofs a single confidential
        withdrawal requires, in one call, because they are built from the same fresh
        randomness and only agree with each other if drawn together. equality_proof_data
        goes to zk-elgamal-proof/context-state/verify/ciphertext-commitment-equality
        and range_proof_data to verify/batched-range-proof-u64, and new_decryptable_available_balance
        is what the withdraw instruction itself carries. The response cannot be rebuilt:
        a second call draws new randomness and produces proofs that no longer match
        any context-state account already verified from the first. The account''s
        balance must not change between building these proofs and the withdrawal landing.'
      parameters:
      - description: Withdraw inputs
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialWithdrawRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialWithdrawResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the two proofs one confidential Withdraw needs
      tags:
      - tool
  /svm/tool/prove/confidential-withdraw-withheld-from-accounts:
    post:
      consumes:
      - application/json
      description: Sums the withheld confidential fee ciphertexts of source_accounts,
        decrypts the total with the withdraw authority's ElGamal secret key, re-encrypts
        it under the destination account's ElGamal key, and proves the two ciphertexts
        encrypt the same value. The sources' withheld amounts, the withdraw authority's
        public key (from the mint), and the destination's ElGamal key and current
        decryptable balance are read from chain, since the deployed program compares
        the proof against exactly those. ciphertext_ciphertext_equality_proof_data
        goes to zk-elgamal-proof/context-state/verify/ciphertext-ciphertext-equality,
        and new_decryptable_available_balance is what the withdraw instruction itself
        carries. The total must fit in 32 bits. The response goes stale if any source's
        withheld amount, or the destination's decryptable balance, changes before
        the withdraw lands.
      parameters:
      - description: Mint, sources, destination, withdraw authority secret, AE key
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialWithdrawWithheldFromAccountsRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialWithdrawWithheldFromAccountsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the proof one confidential WithdrawWithheldTokensFromAccounts
        needs
      tags:
      - tool
  /svm/tool/prove/confidential-withdraw-withheld-from-mint:
    post:
      consumes:
      - application/json
      description: Decrypts the mint's withheld confidential fee amount with the withdraw
        authority's ElGamal secret key, re-encrypts that amount under the destination
        account's ElGamal key, and proves the two ciphertexts encrypt the same value.
        The mint's withheld ciphertext, the withdraw authority's public key, and the
        destination's ElGamal key and current decryptable balance are read from chain,
        since the deployed program compares the proof against exactly those. ciphertext_ciphertext_equality_proof_data
        goes to zk-elgamal-proof/context-state/verify/ciphertext-ciphertext-equality,
        and new_decryptable_available_balance is what the withdraw instruction itself
        carries. The withheld amount must fit in 32 bits. The response cannot be rebuilt
        against a context-state account already verified from an earlier call, and
        it goes stale if more fees are harvested onto the mint, or the destination's
        decryptable balance changes, before the withdraw lands.
      parameters:
      - description: Mint, destination, withdraw authority secret, AE key
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProveConfidentialWithdrawWithheldFromMintRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProveConfidentialWithdrawWithheldFromMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build the proof one confidential WithdrawWithheldTokensFromMint needs
      tags:
      - tool
  /svm/tool/prove/pubkey-validity:
    post:
      consumes:
      - application/json
      description: 'Derives the public key secret_key determines and builds a sigma-protocol
        proof that whoever holds secret_key knows it -- what extensions/confidential-transfer-account/configure-account
        requires alongside the public key it names, since nothing else lets the deployed
        program tell a real ElGamal public key from 32 arbitrary bytes. The proof
        is zero-knowledge: public_key and proof in the response reveal nothing about
        secret_key beyond what configure-account already needs to see.'
      parameters:
      - description: ElGamal secret key
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.ProvePubkeyValidityRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.ProvePubkeyValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a PubkeyValidityProof for an ElGamal secret key
      tags:
      - tool
  /svm/tool/split/record-chunks:
    post:
      consumes:
      - application/json
      description: Splits data (base58-encoded, typically a proof_data blob from a
        tool/prove endpoint) into the writes that put all of it into a SPL Record
        account. A proof too large for one transaction -- a 256-bit range proof is
        1064 bytes -- is written into a record account first and then verified from
        there (zk-elgamal-proof/context-state/verify-from-account), and a single record/write
        carries at most about 1000 bytes. Each chunk's offset and data go straight
        into a record/write request; total_length is the data_length for record/create-account;
        proof_offset (33, the record header) is the proof_offset the verify-from-account
        request needs. chunk_size defaults to 900, which fits a transaction with or
        without a durable nonce, and cannot exceed 1000.
      parameters:
      - description: Bytes to split
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/misc.SplitRecordChunksRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/misc.SplitRecordChunksResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Split bytes into record/write chunks
      tags:
      - tool
  /svm/v1/transaction/build:
    post:
      consumes:
      - application/json
      description: Compiles instructions into a message and returns the unsigned transaction,
        the message bytes every signer signs, and the account ordering the compilation
        produced
      parameters:
      - description: Fee payer, blockhash, and instructions
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v1.BuildTransactionRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v1.BuildTransactionResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build an unsigned transaction
      tags:
      - transaction
  /svm/v2/transaction/compute-budget/request-heap-frame:
    post:
      consumes:
      - application/json
      description: Resizes the heap available to the transaction carrying this instruction,
        in bytes. bytes must be a multiple of 1024 in [32768, 262144] (32KiB to 256KiB);
        anything else is rejected here rather than left to come back as an on-chain
        InvalidInstructionData. The default heap is 32KiB per instruction invocation,
        and applies to every program the transaction invokes, CPIs included; a program
        that allocates more fails with an out-of-memory error unless this runs first
        in the same transaction. It takes no accounts at all — there is nobody to
        name as an authority, since the frame only ever sizes the transaction it rides
        in, never any other. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Heap frame parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.RequestHeapFrameRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RequestHeapFrameResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a heap-frame-resize instruction
      tags:
      - v2-transaction-compute-budget
  /svm/v2/transaction/compute-budget/set-compute-unit-limit:
    post:
      consumes:
      - application/json
      description: Caps how many compute units the transaction carrying this instruction
        may consume. It takes no accounts at all — there is nobody to name as an authority,
        since the limit only ever binds the transaction it rides in, never any other.
        Exceeding it during execution fails that same transaction with a compute-budget-exceeded
        error. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Compute unit limit parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetComputeUnitLimitRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetComputeUnitLimitResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a compute-unit-limit instruction
      tags:
      - v2-transaction-compute-budget
  /svm/v2/transaction/compute-budget/set-compute-unit-price:
    post:
      consumes:
      - application/json
      description: 'Sets the priority fee for the transaction carrying this instruction,
        in micro-lamports per compute unit: the runtime multiplies this by whatever
        compute the transaction actually consumes to get the priority fee on top of
        the base signature fee. It takes no accounts at all — there is nobody to name
        as an authority, since the price only ever binds the transaction it rides
        in, never any other. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Compute unit price parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetComputeUnitPriceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetComputeUnitPriceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a compute-unit-price instruction
      tags:
      - v2-transaction-compute-budget
  /svm/v2/transaction/compute-budget/set-loaded-accounts-data-size-limit:
    post:
      consumes:
      - application/json
      description: Caps the total byte size of every account the transaction carrying
        this instruction loads, across all its instructions and any CPIs they make.
        bytes must be non-zero — the runtime rejects zero outright as InvalidLoadedAccountsDataSizeLimit,
        so this is checked here rather than left to come back as an on-chain rejection.
        Left unset, legacy and v0 transactions default to 64MiB; naming a value here
        only ever lowers that ceiling, since anything larger is clamped back down
        to it. It takes no accounts at all — there is nobody to name as an authority,
        since the limit only ever bounds the transaction it rides in, never any other.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Loaded accounts data size limit parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetLoadedAccountsDataSizeLimitRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetLoadedAccountsDataSizeLimitResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a loaded-accounts-data-size-limit instruction
      tags:
      - v2-transaction-compute-budget
  /svm/v2/transaction/elgamal-registry/create:
    post:
      consumes:
      - application/json
      description: 'ElGamal registry CreateRegistry: creates the wallet''s registry
        account -- a 64-byte PDA of the registry program with seeds [\"elgamal-registry\",
        wallet], created by the program itself -- and records the ElGamal public key
        that a PubkeyValidity proof certifies. The proof must already be verified
        into a context-state account (see tool/prove/pubkey-validity and zk-elgamal-proof/context-state/{create,verify}/pubkey-validity).
        The wallet signs, and one wallet has exactly one registry. With it, any of
        the wallet''s token accounts can be set up for confidential transfers without
        a proof or the owner''s signature (see confidential-transfer-account/configure-account-with-registry),
        and they all share this one ElGamal key. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Registry request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ElGamalRegistryCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ElGamalRegistryCreateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create a wallet's ElGamal registry
      tags:
      - v2-transaction-elgamal-registry
  /svm/v2/transaction/elgamal-registry/update:
    post:
      consumes:
      - application/json
      description: 'ElGamal registry UpdateRegistry: replaces the ElGamal public key
        in the wallet''s existing registry account with the one a verified PubkeyValidity
        proof certifies. Token accounts already configured keep the key they were
        configured with; only later configure-account-with-registry calls see the
        new one. The wallet signs. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Registry request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ElGamalRegistryUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ElGamalRegistryUpdateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Replace the ElGamal public key in a wallet's registry
      tags:
      - v2-transaction-elgamal-registry
  /svm/v2/transaction/record/close:
    post:
      consumes:
      - application/json
      description: 'SPL Record CloseAccount: drains record_account''s lamports into
        receiver, which removes the account once the transaction ends. authority signs.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Record request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.RecordCloseRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RecordCloseResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Close a record account and reclaim its rent
      tags:
      - v2-transaction-record
  /svm/v2/transaction/record/create-account:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, owned by the Record program and sized
        for a header plus data_length bytes of data (33 + data_length), but not yet
        initialized. Initialize it in the same transaction as this (see record/initialize):
        an uninitialized record account can be initialized by anyone, with any authority
        they like. This is what holds a proof too large to carry in one transaction,
        such as a 256-bit range proof (1064 bytes, so data_length 1064). recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Record create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.RecordCreateAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RecordCreateAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for the SPL Record program
      tags:
      - v2-transaction-record
  /svm/v2/transaction/record/initialize:
    post:
      consumes:
      - application/json
      description: 'SPL Record Initialize: marks record_account as a record and names
        its authority. record_account must already exist, owned by the Record program
        (see record/create-account), and not yet be initialized. The authority does
        not sign here, which is why this should land in the same transaction as record/create-account
        -- an uninitialized record account can be initialized by anyone, with any
        authority they like. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Record request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.RecordInitializeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RecordInitializeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize a record account
      tags:
      - v2-transaction-record
  /svm/v2/transaction/record/reallocate:
    post:
      consumes:
      - application/json
      description: 'SPL Record Reallocate: grows record_account to hold data_length
        bytes (excluding the 33-byte header); it does nothing if the account is already
        that large. The account must already hold enough lamports for the larger size,
        since this instruction does not fund it -- top it up with a system transfer
        first. authority signs. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Record request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.RecordReallocateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RecordReallocateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a record account
      tags:
      - v2-transaction-record
  /svm/v2/transaction/record/set-authority:
    post:
      consumes:
      - application/json
      description: 'SPL Record SetAuthority: makes new_authority the record''s authority.
        authority, the current one, signs. recent_blockhash is always required and
        is never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Record request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.RecordSetAuthorityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RecordSetAuthorityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Hand a record account to a new authority
      tags:
      - v2-transaction-record
  /svm/v2/transaction/record/write:
    post:
      consumes:
      - application/json
      description: 'SPL Record Write: copies data into record_account at offset (counted
        from the end of the 33-byte header). It fails if that would run past the end
        of the account, so create or reallocate the record large enough first. data
        is base58-encoded and has to fit in one transaction: about 1000 bytes without
        a durable nonce, about 900 with one, so a larger proof is written in several
        calls, each at its own offset. authority signs. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Record request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.RecordWriteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RecordWriteResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Write bytes into a record account
      tags:
      - v2-transaction-record
  /svm/v2/transaction/system/allocate:
    post:
      consumes:
      - application/json
      description: 'Reserves data space on an existing System-owned account that is
        not already sized: Allocate only ever sets a size once, so an account already
        allocated is rejected regardless of the new size requested, and one that does
        not exist yet is rejected too — this endpoint sizes an account, it does not
        create one. Growing an account raises its rent-exemption floor, so the balance
        is checked against the minimum for the new size rather than the old one; rent_payer
        is always required and covers exactly that shortfall with a transfer prepended
        ahead of the allocation, even though nothing is built and rent reports zero
        when Account already holds enough. recent_blockhash is always required and
        is never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Account, space, fee payer, and rent payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemAllocateRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemAllocateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a System Program allocation
      tags:
      - v2-transaction-system-account
  /svm/v2/transaction/system/assign:
    post:
      consumes:
      - application/json
      description: 'Hands a System-owned account to another program, which is the
        step that puts an account under a program''s control. Ownership is a field
        on the account rather than a mapping the program keeps, which is the inverse
        of an EVM contract holding balances for its users in its own storage. Account
        must already exist and already be owned by the System Program: only the current
        owner may reassign an account. The owner must be executable: only the owning
        program may debit an account or write its data, so assigning to a plain address
        locks the account and its lamports permanently. Assign itself has no notion
        of size: whether account''s byte count matches what the new owner expects
        is that program''s own concern, checked only when its initialization instruction
        runs, not here. Assign moves no lamports of its own, so fee_payer is the only
        role this endpoint charges. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Account, new owner, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemAssignRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemAssignResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a System Program ownership assignment
      tags:
      - v2-transaction-system-account
  /svm/v2/transaction/system/create-account:
    post:
      consumes:
      - application/json
      description: 'Funds a new, zero-byte account owned by the System Program itself.
        The new account signs alongside rent_payer, which is what has no EVM counterpart:
        an address does not exist until whoever holds its private key authorizes its
        creation, and it must not already exist. rent_payer funds the creation for
        exactly the rent-exemption minimum for zero bytes — always, and only that
        amount. This endpoint only ever brings a plain account into existence, empty;
        reserving space is a separate call to allocate, which requires the account
        to already exist, handing it to another program is a separate call to assign,
        and funding it further is a separate transfer. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: New account, fee payer, and rent payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemCreateAccountRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemCreateAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a System Program account creation
      tags:
      - v2-transaction-system-account
  /svm/v2/transaction/system/nonce/advance:
    post:
      consumes:
      - application/json
      description: 'Replaces the stored nonce with the current blockhash. Advancing
        is what consumes a nonce: a transaction built against one carries it in place
        of a recent blockhash and runs this as its first instruction, so the value
        it was built for is gone by the time it finishes and it cannot land twice.
        Run on its own, this simply invalidates anything already built against the
        account. The authority signs, and the stored authority is checked here rather
        than left to fail on chain. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well. It has to be an account other than nonce_account: the
        runtime refuses a transaction that advances the same nonce twice.'
      parameters:
      - description: Nonce account, authority, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemNonceAdvanceRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemNonceAdvanceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a durable nonce advance
      tags:
      - v2-transaction-system-nonce
  /svm/v2/transaction/system/nonce/authorize:
    post:
      consumes:
      - application/json
      description: 'Hands control of a nonce account to another key. The stored nonce
        and the balance are untouched, so only who may advance and withdraw changes.
        That also invalidates anything the old authority signed but never submitted,
        since such a transaction advances the nonce as its first instruction and that
        now needs a signature the old authority cannot give. If the reason for changing
        is a leaked key, the old authority''s pending transaction and this one race,
        so pair it with an advance. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well. It has to be an account other than nonce_account: the
        runtime refuses a transaction that advances the same nonce twice.'
      parameters:
      - description: Nonce account, current authority, new authority, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemNonceAuthorizeRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemNonceAuthorizeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a durable nonce authority change
      tags:
      - v2-transaction-system-nonce
  /svm/v2/transaction/system/nonce/create-account:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized correctly for a nonce account
        but not initialized. This is deliberately the low-level half: nothing stops
        somebody else from initializing the account first, with their own authority,
        in between this transaction and the next; a caller who wants that race closed
        should build create+initialize as two instructions in one transaction themselves.
        Initializing is a separate call: nonce/initialize. Size and funding are not
        choices: a nonce account is always exactly NonceAccountSpace bytes, and rent_payer
        funds exactly the rent-exemption minimum for that size, never more or less.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        It has to be an account other than new_nonce_account, which holds no nonce
        yet.'
      parameters:
      - description: New nonce account, fee payer, and rent payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemNonceCreateRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemNonceCreateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a durable nonce account creation
      tags:
      - v2-transaction-system-nonce
  /svm/v2/transaction/system/nonce/initialize:
    post:
      consumes:
      - application/json
      description: 'Initializes an account that already exists, is System-owned, and
        is already the right size. This is the natural pairing for nonce/create-account,
        which only creates the account and never initializes it; nothing stops somebody
        else from initializing it first in between with their own authority, so a
        caller who wants that race closed has to build create+initialize as two instructions
        in one transaction themselves. It is also the only path left for an address
        that can no longer be created: CreateAccount refuses one that already holds
        lamports, which is what happens when someone funds the address first. The
        account is writable but does not sign, since initializing it needs no authority
        of its own; it gains the authority named here. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well. It has to be an account other than nonce_account,
        which holds no nonce to build against yet.'
      parameters:
      - description: Nonce account, authority, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemNonceInitializeRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemNonceInitializeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a durable nonce initialization on an existing account
      tags:
      - v2-transaction-system-nonce
  /svm/v2/transaction/system/nonce/upgrade:
    post:
      consumes:
      - application/json
      description: 'Rewrites a Legacy nonce account as the current version. Legacy
        accounts stored the blockhash itself, which could collide with a real one;
        the current version stores a value derived from it that cannot. Nothing signs,
        since this is not a privileged operation, so anyone willing to pay the fee
        may upgrade anyone''s account. No account this project creates can be upgraded:
        initialize has written the current version for a long time, only accounts
        predating that change are Legacy, and no instruction can produce one now.
        The check below rejects a current account before it reaches the chain. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well. It has to be an account other than nonce_account,
        which is Legacy and so not something to build against.'
      parameters:
      - description: Nonce account and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemNonceUpgradeRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemNonceUpgradeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a Legacy nonce account migration
      tags:
      - v2-transaction-system-nonce
  /svm/v2/transaction/system/nonce/withdraw:
    post:
      consumes:
      - application/json
      description: 'Moves part of a nonce account''s balance out. What stays has to
        keep the account rent exempt at its size, since an account below that floor
        is subject to removal while still holding a nonce something may have been
        built against. Taking the whole balance closes the account and carries a further
        rule, so that has its own endpoint. recipient_account must already exist:
        this is a plain transfer between two accounts, not a way to bring a new one
        into existence. The authority signs, and the stored authority is checked here
        rather than left to fail on chain. recent_blockhash is always required and
        is never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well. It has to be an account other than nonce_account:
        the runtime refuses a transaction that advances the same nonce twice.'
      parameters:
      - description: Nonce account, authority, recipient, lamports, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemNonceWithdrawRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemNonceWithdrawResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a partial withdrawal from a durable nonce account
      tags:
      - v2-transaction-system-nonce
  /svm/v2/transaction/system/nonce/withdraw/max:
    post:
      consumes:
      - application/json
      description: 'Takes the whole balance, which closes the account. recipient_account
        must already exist: this is a plain transfer between two accounts, not a way
        to bring a new one into existence. The rent-exempt floor that constrains a
        partial withdrawal does not apply, since nothing is left to keep exempt. One
        rule replaces it and is not checked here: the runtime refuses to close an
        account whose stored nonce is still the blockhash the transaction executes
        against, so closing in the same block the nonce was last advanced or initialized
        fails with NonceBlockhashNotExpired. That cannot be decided before submitting,
        because the blockhash it is compared against is the one at execution rather
        than any this build could see. Waiting a block and rebuilding is the fix.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        It has to be an account other than nonce_account.'
      parameters:
      - description: Nonce account, authority, recipient, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemNonceWithdrawMaxRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemNonceWithdrawMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a full withdrawal that closes a durable nonce account
      tags:
      - v2-transaction-system-nonce
  /svm/v2/transaction/system/seed/allocate:
    post:
      consumes:
      - application/json
      description: Reserves data space on SHA256(base || seed || System Program) with
        base signing in the account's place. The account must already exist, be System-owned,
        and not already be allocated space — Allocate only ever sets a size once.
        Growing an account raises its rent-exemption floor, so the balance is checked
        against the minimum for the new size rather than the old one; rent_payer is
        always required and covers exactly that shortfall with a transfer prepended
        ahead of the allocation, even though nothing is built and rent reports zero
        when the derived account already holds enough. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Base, seed, space, fee payer, and rent payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemSeedAllocateRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemSeedAllocateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build an allocation on a seed-derived address
      tags:
      - v2-transaction-system-seed
  /svm/v2/transaction/system/seed/assign:
    post:
      consumes:
      - application/json
      description: 'Hands SHA256(base || seed || owner) to that same owner. There
        is no separate new-owner field, because one owner does both jobs: it is what
        the address is derived from and what the account is assigned to, so an account
        can only be handed to the program its own address already encodes. This is
        a narrow, rarely-needed tool: it only works on a derived account whose address
        was chosen for owner from the very start, brought into existence some other
        way — an account made through seed/create-account never qualifies, since that
        always derives against the System Program, and naming that here is either
        a no-op or a mismatch pointing at an unrelated address. The account must already
        exist and already be owned by the System Program: only the current owner may
        reassign an account. owner must be executable, since assigning to a plain
        address locks the account and its lamports permanently. recent_blockhash is
        always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Base, seed, owner, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemSeedAssignRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemSeedAssignResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build an ownership assignment on a seed-derived address
      tags:
      - v2-transaction-system-seed
  /svm/v2/transaction/system/seed/create-account:
    post:
      consumes:
      - application/json
      description: 'Creates a zero-byte account at SHA256(base || seed || System Program)
        and funds it for exactly the rent-exemption minimum, the same split as create-account,
        allocate, and assign for a keypair account: this endpoint only ever brings
        the derived account into existence, empty; reserving space is a separate call
        to seed/allocate, and handing it to another program is a separate call to
        seed/assign. The derived account never signs, which is the difference from
        create-account: nobody holds a secret for it, so base signs in its place and
        whoever controls base controls every address derived from it. The address
        is derived rather than accepted, since the runtime recomputes it and rejects
        a mismatch. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Base, seed, fee payer, and rent payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemSeedCreateAccountRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemSeedCreateAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build an account creation at a seed-derived address
      tags:
      - v2-transaction-system-seed
  /svm/v2/transaction/system/seed/transfer:
    post:
      consumes:
      - application/json
      description: 'Debits SHA256(base || seed || System Program) without that account
        signing, since base signs for it. That is what makes a derived address usable
        as a holding account: anyone can fund it, and only the holder of base can
        spend it. The account must still be System-owned for a system transfer to
        debit it, and recipient_account must already exist: this is a plain transfer
        between two accounts, not a way to bring a new one into existence. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Base, seed, recipient, lamports, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemSeedTransferRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemSeedTransferResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a transfer out of a seed-derived address
      tags:
      - v2-transaction-system-seed
  /svm/v2/transaction/system/seed/transfer/max:
    post:
      consumes:
      - application/json
      description: 'Sends everything SHA256(base || seed || System Program) holds.
        The amount is the whole balance with nothing held back, because a derived
        address can never pay the fee: a fee payer has to sign, and this account cannot.
        That also means no probe is needed to price the message first, since the amount
        does not depend on the fee here the way it does for a plain transfer. Emptying
        the account lets the runtime reclaim it. recipient_account must already exist:
        this is a plain transfer between two accounts, not a way to bring a new one
        into existence. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Base, seed, recipient, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemSeedTransferMaxRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemSeedTransferMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a transfer of a seed-derived address's entire balance
      tags:
      - v2-transaction-system-seed
  /svm/v2/transaction/system/transfer:
    post:
      consumes:
      - application/json
      description: 'Assembles a System Program transfer between two existing accounts
        and returns the same shape as a v1 build, so sign and send accept it unchanged.
        recipient_account must already exist: this is a plain balance transfer, not
        a way to bring a new account into existence. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it as the first instruction;
        recent_blockhash then only prices the transaction, since a nonce is never
        among the cluster''s recent blockhashes. The response reports nonce_authority
        in that case, which has to sign as well. To send the sender''s entire balance,
        use transfer/max instead.'
      parameters:
      - description: Funding payer, recipient, and lamports
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemTransferRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemTransferResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a native SOL transfer
      tags:
      - v2-transaction-system-transfer
  /svm/v2/transaction/system/transfer/collect:
    post:
      consumes:
      - application/json
      description: 'Assembles one Transfer instruction per source, all landing on
        the same account that already exists, in a single transaction — the inverse
        of transfer/spread. Unlike spread, this is a multi-party transaction: each
        source signs only for its own instruction, so as many signatures are required
        as sources named, plus fee_payer. A source may set max instead of lamports
        to sweep its entire balance; if that source is also fee_payer, the fee comes
        out of the same sweep first, exactly as transfer/max handles the same overlap.
        This is bounded by transaction size rather than by anything it checks: a transaction
        travels in one 1232-byte packet and cannot be split, which caps the source
        list somewhere around twenty and is reported as size and size_limit. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it as the first instruction;
        recent_blockhash then only prices the transaction. The response reports nonce_authority
        in that case, which has to sign as well.'
      parameters:
      - description: Recipient, transfers with amounts or max, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemTransferCollectRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemTransferCollectResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build one transaction collecting from several senders
      tags:
      - v2-transaction-system-transfer
  /svm/v2/transaction/system/transfer/max:
    post:
      consumes:
      - application/json
      description: 'Assembles a System Program transfer moving everything the sender
        can send, between two existing accounts. Resolving that amount needs the sender''s
        balance and the fee, both fetched from the chain. The fee only comes out of
        the sender''s balance when the sender is also the fee payer; with a separate
        fee payer the whole balance can go, which empties the account and lets the
        runtime reclaim it. recipient_account must already exist: this is a plain
        balance transfer, not a way to bring a new account into existence. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it as the first instruction;
        recent_blockhash then only prices the transaction, since a nonce is never
        among the cluster''s recent blockhashes. The response reports nonce_authority
        in that case, which has to sign as well.'
      parameters:
      - description: Funding payer and recipient
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemTransferMaxRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemTransferMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build a native SOL transfer of the sender's entire balance
      tags:
      - v2-transaction-system-transfer
  /svm/v2/transaction/system/transfer/spread:
    post:
      consumes:
      - application/json
      description: 'Assembles one Transfer instruction per recipient, all leaving
        the same account and all landing on accounts that already exist, in a single
        transaction. This is the first endpoint to carry an arbitrary number of instructions,
        so it is the first bounded by transaction size rather than by anything it
        checks: a transaction travels in one 1232-byte packet and cannot be split,
        which caps the list somewhere around twenty and is reported as size and size_limit.
        The account keys show fewer entries than instructions, since the sender and
        the System Program appear in every one and a compiled message lists each key
        once. There is no max variant, because sending everything one account holds
        does not say how to divide it. recent_blockhash is always required and is
        never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it as the first instruction;
        recent_blockhash then only prices the transaction, since a nonce is never
        among the cluster''s recent blockhashes. The response reports nonce_authority
        in that case, which has to sign as well.'
      parameters:
      - description: Funding payer, recipients with amounts, and fee payer
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SystemTransferSpreadRequest'
      - description: Chain name, e.g. solana
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Chain network, e.g. testnet
        in: header
        name: X-Chain-Network
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SystemTransferSpreadResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build one transaction paying several recipients
      tags:
      - v2-transaction-system-transfer
  /svm/v2/transaction/token/approve:
    post:
      consumes:
      - application/json
      description: 'Raw Approve, the original opcode: neither the mint nor its decimals
        is named or verified against the account. mint is still resolved server-side
        from token_account''s own stored value and reported in the response. token_account_owner
        must be token_account''s owner, never an existing delegate. This is for compatibility
        with the original opcode; approve-checked remains the normal public path.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Approve parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ApproveRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ApproveResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grant a delegate spending rights over a token account (original opcode)
      tags:
      - v2-transaction-token-delegation
  /svm/v2/transaction/token/approve-checked:
    post:
      consumes:
      - application/json
      description: Authorizes delegate to move up to amount from token_account, on
        token_account_owner's behalf. A second approve replaces the delegation entirely
        rather than adding to it, since the program stores one delegate and one amount,
        not a list; the owner may still move the whole balance regardless of what
        a delegate holds. Only token_account's owner may approve, never an existing
        delegate, so re-delegating is not possible through this endpoint. decimals
        is checked against the mint the same way every other checked endpoint checks
        it. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Delegation parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ApproveCheckedRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ApproveCheckedResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grant a delegate limited spending rights over a token account
      tags:
      - v2-transaction-token-delegation
  /svm/v2/transaction/token/approve-checked/max:
    post:
      consumes:
      - application/json
      description: Same as approve-checked, except the amount granted is always the
        maximum representable base-unit value rather than a caller-given one — the
        standard effectively-unlimited approval, since that value is far beyond any
        real balance and never needs re-approving as the balance changes. The delegate
        can still only ever move what token_account actually holds; the amount here
        is a ceiling, not a balance. decimals is never a request field, since there
        is no client-computed amount to protect against a wrong decimals assumption;
        the mint's own value is used to build the instruction directly. Only token_account_owner
        may approve, never an existing delegate, so re-delegating is not possible
        through this endpoint. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Delegation parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ApproveCheckedMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ApproveCheckedMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grant a delegate effectively unlimited spending rights over a token
        account
      tags:
      - v2-transaction-token-delegation
  /svm/v2/transaction/token/approve/max:
    post:
      consumes:
      - application/json
      description: 'Same as approve, except no amount is read at all: it grants the
        maximum representable base-unit amount directly, mirroring approve-checked/max
        and the standard effectively-unlimited approval pattern. Neither the mint
        nor decimals is named or verified against the account, the same as approve;
        mint is still resolved server-side and reported in the response. token_account_owner
        must be token_account''s owner, never an existing delegate. This is for compatibility
        with the original opcode; approve-checked/max remains the normal public path.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Approve parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ApproveMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ApproveMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grant a delegate the maximum representable amount over a token account
        (original opcode)
      tags:
      - v2-transaction-token-delegation
  /svm/v2/transaction/token/ata/recover-nested:
    post:
      consumes:
      - application/json
      description: 'Moves the balance out of a nested associated token account — one
        created by mistakenly deriving from another associated account as if it were
        a wallet — into wallet''s real associated account for the same mint, and closes
        the nested one. Three addresses are derived internally, none are request fields:
        owner_account is wallet''s associated account for owner_mint (the one mistaken
        for a wallet), nested_account is owner_account''s own associated account for
        nested_mint (the mistake itself, being closed), and destination is wallet''s
        real associated account for nested_mint (where the balance and reclaimed rent
        both end up). wallet signs, since only the real owner may authorize closing
        an account that pays its lamports back there. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Recovery parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ATARecoverNestedRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ATARecoverNestedResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Recover a nested associated token account
      tags:
      - v2-transaction-token-ata
  /svm/v2/transaction/token/burn:
    post:
      consumes:
      - application/json
      description: 'Raw Burn, the original opcode: decimals is neither named nor verified
        against the mint. token_account_authority must be token_account''s owner,
        or its delegate for no more than the delegated amount. This is for compatibility
        with the original opcode; burn-checked remains the normal public path. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Burn parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.BurnRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.BurnResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Destroy supply held by a token account (original opcode)
      tags:
      - v2-transaction-token-supply
  /svm/v2/transaction/token/burn-checked:
    post:
      consumes:
      - application/json
      description: Reduces the mint's total supply and token_account's balance together.
        decimals is checked against the mint rather than filled in from it, catching
        a client that formatted amount against the wrong decimals as a 400 instead
        of an on-chain failure. token_account_authority must be token_account's owner,
        or its delegate for no more than the delegated amount — the mint's own authority
        has no say over what a holder chooses to burn. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Burn parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.BurnCheckedRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.BurnCheckedResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Destroy supply held by a token account
      tags:
      - v2-transaction-token-supply
  /svm/v2/transaction/token/burn-checked/max:
    post:
      consumes:
      - application/json
      description: Same as burn-checked, except the amount destroyed is token_account's
        current balance rather than a caller-given one. decimals is never a request
        field, since there is no client-computed amount to protect against a wrong
        decimals assumption; the mint's own value is used to build the instruction
        directly. Unlike a lamport sweep, token_account carries no rent-exemption
        floor tied to its token balance, so it is always swept all the way to zero.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Burn parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.BurnCheckedMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.BurnCheckedMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Destroy a token account's entire balance
      tags:
      - v2-transaction-token-supply
  /svm/v2/transaction/token/burn/max:
    post:
      consumes:
      - application/json
      description: Same as burn, except the amount destroyed is token_account's current
        balance rather than a caller-given one. decimals is neither named nor verified
        against the mint, the same as burn. Unlike a lamport sweep, token_account
        carries no rent-exemption floor tied to its token balance, so it is always
        swept all the way to zero. This is for compatibility with the original opcode;
        burn-checked/max remains the normal public path. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Burn parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.BurnMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.BurnMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Destroy a token account's entire balance (original opcode)
      tags:
      - v2-transaction-token-supply
  /svm/v2/transaction/token/close-account:
    post:
      consumes:
      - application/json
      description: token_account must already hold no tokens; the balance is not swept,
        it has to be zero. A wrapped SOL account is the exception, since its lamports
        are its balance, and closing it is how SOL is unwrapped. token_account_close_authority
        must be token_account's owner or its close authority. The reclaimed lamports
        go to recipient_account. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Close parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CloseAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CloseAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Close a token account and reclaim its rent
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/create-ata:
    post:
      consumes:
      - application/json
      description: 'Derives the associated token address and creates it. The address
        is not a request field: it follows from owner, mint, and program, so nothing
        generates a keypair for it and nothing has to remember it. The account itself
        does not sign, unlike a keypair token account, because a program derived address
        has no private key. Fails if the account already exists; use create-ata-idempotent
        when that is not known in advance. recent_blockhash is always required and
        is never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Associated account parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CreateATARequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CreateATAResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create the canonical token account for an owner and mint
      tags:
      - v2-transaction-token-ata
  /svm/v2/transaction/token/create-ata-idempotent:
    post:
      consumes:
      - application/json
      description: 'Same as create-ata, except the instruction succeeds rather than
        fails when the account is already there. This is the one to prepend to a transfer:
        checking first and creating only if absent leaves a window in which somebody
        else creates it, and the plain create would then fail the whole transaction
        over an account that exists and is correct. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Associated account parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CreateATAIdempotentRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CreateATAIdempotentResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create the canonical token account, succeeding if it exists
      tags:
      - v2-transaction-token-ata
  /svm/v2/transaction/token/create-kta:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized and owned for a holder account
        but not initialized. This is deliberately the low-level half: nothing stops
        somebody else from initializing the account first, naming their own wallet
        as owner, in between this transaction and the next; a caller who wants that
        race closed should build create+initialize as two instructions in one transaction
        themselves. Initializing is a separate call: initialize-account3, or initialize-account/initialize-account2
        for the original opcodes. This produces a plain keypair account rather than
        an associated one (see create-ata): the address is whatever key was generated
        for it, and nothing can rediscover it from the wallet and mint the way an
        associated token account can be. recent_blockhash is always required and is
        never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Token account parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CreateKTARequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CreateKTAResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a keypair SPL Token holder
        account (KTA)
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/create-mint:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized and owned for a mint but not
        initialized. This is deliberately the low-level half: nothing stops somebody
        else from initializing the account first, in between this transaction and
        the next; a caller who wants that race closed should build create+initialize
        as two instructions in one transaction themselves. Initializing is a separate
        call: initialize-mint2, or initialize-mint for the original opcode. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CreateMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CreateMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for an SPL Token mint
      tags:
      - v2-transaction-token-mint-account
  /svm/v2/transaction/token/create-multisig:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized and owned for a multisig but
        not initialized. This is deliberately the low-level half, the same as create-mint
        and create-kta: nothing stops somebody else from initializing the account
        first, with their own m and signers, in between this transaction and the next;
        a caller who wants that race closed should build create+initialize as two
        instructions in one transaction themselves. Initializing is a separate call:
        initialize-multisig2, or initialize-multisig for the original opcode. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Multisig parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CreateMultisigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CreateMultisigResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a Token multisig
      tags:
      - v2-transaction-token-multisig
  /svm/v2/transaction/token/create-native-mint:
    post:
      consumes:
      - application/json
      description: Creates and initializes Token-2022's own native mint -- the mint
        that stands in for wrapped SOL under that program -- at the address the program
        derives (returned in the response). rent_payer signs and pays the rent. It
        fails if the account already exists, which is the case on every public cluster,
        so this is only useful on a fresh one; this endpoint checks that first. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CreateNativeMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CreateNativeMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create Token-2022's native mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-mint-burn/apply-pending-burn:
    post:
      consumes:
      - application/json
      description: Subtracts the mint's pending burn -- what confidential burns accumulated
        -- from its confidential supply and resets the pending burn to zero. Authorized
        by the mint authority. It does not touch the decryptable supply; update that
        separately with update-decryptable-supply once the new supply is known. No
        zero-knowledge proof is needed, and the instruction carries no data beyond
        its own discriminant. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that mint stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialApplyPendingBurnRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialApplyPendingBurnResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fold the mint's pending burn into its confidential supply
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-mint-burn/burn:
    post:
      consumes:
      - application/json
      description: 'Burns an encrypted amount from account''s available confidential
        balance and adds it to the mint''s pending burn -- the amount never appears
        in plaintext on chain. Authorized by the account''s owner, not the mint authority.
        The burn only reaches the confidential supply after the mint authority runs
        apply-pending-burn, and rotating the supply key is refused while a burn is
        pending. Account must carry ConfidentialTransferAccount and the mint both
        ConfidentialMintBurn and ConfidentialTransferMint. Builds only the Burn instruction:
        the three proofs must already be verified into context-state accounts, whose
        addresses are named here -- build them with tool/prove/confidential-burn,
        create the accounts with zk-elgamal-proof/context-state/create, and verify
        each with context-state/verify. new_decryptable_available_balance and the
        auditor ciphertexts must come from the same tool/prove call as those proofs,
        and the account''s available balance must not change in between. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, mint, owner, context-state accounts, proof outputs,
          and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialBurnRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialBurnResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Burn tokens confidentially from an account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-mint-burn/initialize:
    post:
      consumes:
      - application/json
      description: 'Names the ElGamal public key the mint''s confidential supply is
        encrypted under (supply_elgamal_pubkey, base58-encoded raw 32-byte ElGamal
        public key, not a Solana address) and starts the supply at zero, with supply_ae_key
        encrypting that zero into the decryptable supply the instruction carries (keep
        the key: every later mint and burn needs it). This can only ever run in the
        narrow window every mint extension shares: after the mint account has been
        allocated with room for this extension (see extensions/mint/data-size) and
        before initialize-mint2 locks the extension list forever -- there is no path
        back into an already-initialized mint. Confidential mint and burn also read
        the auditor key from ConfidentialTransferMint, so the mint should carry that
        extension too. No zero-knowledge proof and no signer are needed. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, supply ElGamal key, supply AE key, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeConfidentialMintBurnRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeConfidentialMintBurnResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Attach the ConfidentialMintBurn extension to a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-mint-burn/mint:
    post:
      consumes:
      - application/json
      description: 'Mints an encrypted amount into account''s pending confidential
        balance and adds it to the mint''s confidential supply -- the amount never
        appears in plaintext on chain. Authorized by the mint''s mint authority. Account
        must carry ConfidentialTransferAccount and the mint both ConfidentialMintBurn
        and ConfidentialTransferMint. Builds only the Mint instruction: the three
        proofs (commitment equality, batched grouped 3-handle validity, batched u128
        range) must already be verified into context-state accounts, whose addresses
        are named here -- build them with tool/prove/confidential-mint, create the
        accounts with zk-elgamal-proof/context-state/create, and verify each with
        context-state/verify (or verify-from-account with a compute_unit_limit for
        the range proof). new_decryptable_supply and the auditor ciphertexts must
        come from the same tool/prove call as those proofs. The minted amount lands
        in pending balance, so the account still needs apply-pending-balance before
        it can spend it. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, mint, authority, context-state accounts, proof outputs,
          and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Mint tokens confidentially into an account and the confidential supply
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-mint-burn/rotate-supply-elgamal-pubkey:
    post:
      consumes:
      - application/json
      description: 'Replaces the ElGamal key the mint''s confidential supply is encrypted
        under, and the supply ciphertext itself, with new_supply_elgamal_pubkey. Authorized
        by the mint authority. The mint''s pending burn must be zero (see apply-pending-burn).
        Builds only the instruction: the CiphertextCiphertextEquality proof must already
        be verified into a context-state account, whose address is named here -- build
        it with tool/prove/confidential-rotate-supply-elgamal-pubkey, create the account
        with zk-elgamal-proof/context-state/create/ciphertext-ciphertext-equality,
        and verify it with context-state/verify/ciphertext-ciphertext-equality. The
        proof is bound to the mint''s supply ciphertext and key as they are when it
        is built, so a mint or burn in between makes it fail. Later mint and burn
        proofs must use the new key. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that mint stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialRotateSupplyElGamalPubkeyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialRotateSupplyElGamalPubkeyResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Rotate the ElGamal key the mint's confidential supply is encrypted
        under
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-mint-burn/update-decryptable-supply:
    post:
      consumes:
      - application/json
      description: Overwrites the mint's decryptable supply -- the cheap AE cache
        of its confidential supply -- with new_supply encrypted under supply_ae_key.
        Authorized by the mint authority. The program cannot check the value against
        the confidential supply, so it has to be the value the caller knows the supply
        to be, for example after apply-pending-burn. No zero-knowledge proof is needed.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        mint stores instead, so the transaction never expires, and prepends the advance
        that consumes it; recent_blockhash then only prices the transaction. The response
        reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialUpdateDecryptableSupplyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialUpdateDecryptableSupplyResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Overwrite the mint's decryptable supply
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/apply-pending-balance:
    post:
      consumes:
      - application/json
      description: 'Moves whatever deposit and incoming transfers have accumulated
        in account''s encrypted pending balance into its available balance, the one
        transfer and withdraw actually spend from. Nothing received since account''s
        last apply can be spent until this runs. No zero-knowledge proof is needed:
        the program does the pending-into-available ElGamal addition itself. new_available_balance
        is this endpoint''s own bookkeeping catching up to that addition -- the total
        available balance account should hold once this lands, AE-encrypted here under
        ae_key the same way configure-account''s decryptable_zero_balance was; this
        endpoint never decrypts a confidential balance itself, so the caller supplies
        the total. expected_pending_balance_credit_counter is how many pending-balance
        credits landed since account''s last apply -- the program rejects a mismatched
        count. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, authority, expected credit counter, new balance, AE
          key, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ApplyPendingBalanceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ApplyPendingBalanceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move an account's confidential pending balance into its available balance
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/approve-account:
    post:
      consumes:
      - application/json
      description: 'Flips account''s ConfidentialTransferAccount.approved flag, authorized
        by mint''s ConfidentialTransferMint authority (the role initialize or update
        named), not account''s own owner. Only needed when mint was set up with auto_approve_new_accounts
        false -- against a mint set up with it true, configure-account already leaves
        a usable account and this call has nothing left to flip, though the deployed
        program accepts it regardless. No zero-knowledge proof is needed: unlike configure-account
        this carries no data beyond the two discriminant bytes. recent_blockhash is
        always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, mint, authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ApproveAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ApproveAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Approve a token account for confidential transfers
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/configure-account:
    post:
      consumes:
      - application/json
      description: 'Builds two instructions in one transaction: ConfigureAccount itself,
        and the VerifyPubkeyValidity instruction it depends on as its very next sibling
        -- the program has to be convinced elgamal_pubkey has a known secret key before
        letting account claim it. mint must already carry ConfidentialTransferMint,
        and account must already hold room for this extension (see extensions/confidential-transfer-account/reallocate).
        pubkey_proof is checked locally (the same relationship the deployed verifier
        itself checks) before a transaction is ever built, so a proof built against
        a different key fails as a 400 rather than a wasted fee. decryptable_zero_balance
        is computed here from ae_key, always encrypting zero -- a freshly configured
        account has no balance yet. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, mint, owner, ElGamal pubkey/proof, AE key, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfigureAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfigureAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Attach the ConfidentialTransferAccount extension to a token account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/configure-account-with-registry:
    post:
      consumes:
      - application/json
      description: 'ConfigureAccountWithRegistry: attaches ConfidentialTransferAccount
        to account using the ElGamal public key in its owner''s registry (see elgamal-registry/create)
        instead of a PubkeyValidity proof, so no context-state account is needed.
        No signature from the account''s owner is required -- the program only checks
        that the registry''s owner is the account''s owner -- so anyone can pay for
        it. The registry is found from the account''s owner; it has to exist. Naming
        rent_payer lets the program resize the account itself (room for the confidential
        fee extension too, on a fee mint) and pay any rent shortfall, so the separate
        reallocate step is not needed; rent_payer signs. Without rent_payer the account
        must already have room. The account starts with an all-zero decryptable balance
        and the default pending-credit limit; an all-zero AE ciphertext is not a valid
        encryption of zero, so the owner''s first apply-pending-balance is what makes
        the decryptable balance real. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, optional payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialConfigureAccountWithRegistryRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialConfigureAccountWithRegistryResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Set up a token account for confidential transfers using the owner's
        ElGamal registry
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/deposit:
    post:
      consumes:
      - application/json
      description: 'Moves amount from account''s ordinary public balance into its
        own ConfidentialTransferAccount pending balance, encrypted along the way --
        the entry point into the confidential side from a plain SPL balance. account
        must already carry the extension (see configure-account). No zero-knowledge
        proof is needed: the amount is still public at this instant, leaving a balance
        anyone can already see, so there is nothing to prove about it yet. decimals
        must equal mint''s own, checked here the same as every other *Checked-shaped
        instruction rather than trusting the caller''s figure. recent_blockhash is
        always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, mint, authority, amount, decimals, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.DepositRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.DepositResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move an account's public balance into its confidential pending balance
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/disable-confidential-credits:
    post:
      consumes:
      - application/json
      description: Clears account's allow_confidential_credits flag, so it rejects
        any incoming confidential transfer (ordinary transfers are still governed
        by allow_non_confidential_credits). Authorized by account's owner. No zero-knowledge
        proof is needed, and the instruction carries no data beyond its own discriminant.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.DisableConfidentialCreditsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.DisableConfidentialCreditsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Make a token account reject confidential transfers
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/disable-non-confidential-credits:
    post:
      consumes:
      - application/json
      description: Clears account's ConfidentialTransferAccount.allow_non_confidential_credits
        flag, so it rejects any ordinary (non-confidential) transfer into it -- paired
        with the account still accepting confidential credits (the default), it becomes
        a confidential-only receiver. Authorized by account's owner. No zero-knowledge
        proof is needed, and the instruction carries no data beyond its own discriminant.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.DisableNonConfidentialCreditsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.DisableNonConfidentialCreditsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Make a token account reject non-confidential transfers
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/empty-account:
    post:
      consumes:
      - application/json
      description: 'Resets account''s confidential available balance to all-zero bytes,
        which a confidential token account needs before it can be closed: after a
        withdraw empties the balance it still holds a randomized encryption of zero,
        not zero bytes. Takes a proof that the stored ciphertext encrypts zero, then
        overwrites it. Fails if the available balance is already empty, so it only
        applies to an account that once held a balance. Builds only the EmptyAccount
        instruction: the zero-ciphertext proof must already be verified into a context-state
        account, whose address is named here -- build the proof with tool/prove/confidential-empty-account,
        create the account with zk-elgamal-proof/context-state/create/zero-ciphertext,
        and verify it with context-state/verify/zero-ciphertext. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, zero-ciphertext context-state account, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialEmptyAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialEmptyAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Empty a confidential account's available balance so it can be closed
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/enable-confidential-credits:
    post:
      consumes:
      - application/json
      description: Sets account's allow_confidential_credits flag, so it accepts incoming
        confidential transfers. Authorized by account's owner. No zero-knowledge proof
        is needed, and the instruction carries no data beyond its own discriminant.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.EnableConfidentialCreditsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.EnableConfidentialCreditsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Make a token account accept confidential transfers
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/enable-non-confidential-credits:
    post:
      consumes:
      - application/json
      description: Sets account's allow_non_confidential_credits flag, so it accepts
        ordinary (non-confidential) transfers again. Authorized by account's owner.
        No zero-knowledge proof is needed, and the instruction carries no data beyond
        its own discriminant. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.EnableNonConfidentialCreditsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.EnableNonConfidentialCreditsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Make a token account accept non-confidential transfers
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/reallocate:
    post:
      consumes:
      - application/json
      description: 'Checks whether account already holds enough space for its existing
        extensions plus ConfidentialTransferAccount, and grows it if not. The instruction
        itself only ever needs the one new extension: Reallocate reads account''s
        own existing extensions on chain and unions them with what this sends, so
        a caller never resends what is already there. Getting the resize''s rent right
        is this endpoint''s own job: it reads account''s current extensions and actual
        lamports, asks GetAccountDataSize for the full target size once ConfidentialTransferAccount
        is unioned in, and only then knows rent_payer''s shortfall — the same authority
        Reallocate itself defers to, asked directly rather than recomputed here. Unlike
        ConfidentialTransferMint, ConfidentialTransferAccount is exactly the token-account
        extension Reallocate''s own account list expects: this is the endpoint that
        actually succeeds, preparing an account for confidential-transfer-account/configure-account,
        which otherwise fails as InvalidAccountData the moment the program tries to
        write pending/available balance ciphertexts into an account that never reserved
        the 295 bytes those TLV fields need. recent_blockhash is always required and
        is never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well. On a mint that charges a transfer fee,
        set include_confidential_transfer_fee_amount: configure-account then also
        initializes ConfidentialTransferFeeAmount, and room for both has to be reserved
        in this one call.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, rent payer, owner, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ReallocateConfidentialTransferAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ReallocateConfidentialTransferAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a token account to hold room for ConfidentialTransferAccount
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/transfer:
    post:
      consumes:
      - application/json
      description: 'Moves an amount confidentially from source to destination -- neither
        the amount nor either account''s resulting balance ever appears in plaintext
        on chain. Both accounts must already carry the ConfidentialTransferAccount
        extension. Builds only the Transfer instruction: the three zero-knowledge
        proofs it depends on (equality, ciphertext validity, and a batched range proof)
        must already be verified into context-state accounts, whose addresses are
        named here -- build the proofs with tool/prove/confidential-transfer, create
        the accounts with zk-elgamal-proof/context-state/create, and verify each with
        context-state/verify. new_source_decryptable_available_balance, auditor_ciphertext_lo,
        and auditor_ciphertext_hi must come from that same tool/prove/confidential-transfer
        call, since a later call draws new randomness and no longer matches the verified
        proofs. The source balance must not change between building the proofs and
        this transfer landing. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Source, mint, destination, owner, ElGamal/AE key material, amount,
          and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialTransferRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialTransferResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move tokens confidentially between two accounts
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/transfer-with-fee:
    post:
      consumes:
      - application/json
      description: 'Moves an amount confidentially from source to destination on a
        mint that carries TransferFeeConfig -- neither the amount, the fee, nor either
        account''s resulting balance ever appears in plaintext on chain. Once a mint
        has that extension the plain confidential transfer is refused and only this
        instruction is accepted. Both accounts must already carry the ConfidentialTransferAccount
        extension, and destination must also carry ConfidentialTransferFeeAmount,
        where the withheld fee accumulates. Builds only the TransferWithFee instruction:
        the five zero-knowledge proofs it depends on (equality, transfer amount validity,
        fee percentage-with-cap, fee validity, and a 256-bit range proof) must already
        be verified into context-state accounts, whose addresses are named here --
        build the proofs with tool/prove/confidential-transfer-with-fee, create the
        accounts with zk-elgamal-proof/context-state/create, and verify each with
        context-state/verify. new_source_decryptable_available_balance and auditor_ciphertext_lo/hi
        must come from that same tool call, since a later call draws new randomness
        and no longer matches the verified proofs. The source balance must not change,
        and the epoch must not roll over into a different fee, between building the
        proofs and this transfer landing. recent_blockhash is always required and
        is never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Source, mint, destination, owner, ElGamal/AE key material, amount,
          and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialTransferWithFeeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialTransferWithFeeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move tokens confidentially on a mint that charges a transfer fee
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-account/withdraw:
    post:
      consumes:
      - application/json
      description: 'Moves amount out of account''s confidential available balance
        back into its ordinary public balance -- the reverse of deposit. amount is
        public, but the remaining encrypted balance is not, so the program needs two
        proofs it is what it should be: an equality proof and a 64-bit range proof.
        Builds only the Withdraw instruction: both proofs must already be verified
        into context-state accounts, whose addresses are named here -- build the proofs
        with tool/prove/confidential-withdraw, create the accounts with zk-elgamal-proof/context-state/create,
        and verify each with context-state/verify. new_decryptable_available_balance
        must come from that same tool/prove/confidential-withdraw call, since a later
        call draws new randomness and no longer matches the verified proofs. The account''s
        balance must not change between building the proofs and this withdrawal landing.
        decimals is checked against the mint rather than filled in from it. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, mint, owner, amount, decimals, context-state accounts,
          and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialWithdrawRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialWithdrawResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Withdraw from a confidential balance to the public balance
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-fee-config/disable-harvest-to-mint:
    post:
      consumes:
      - application/json
      description: Clears harvest_to_mint_enabled, so the mint rejects confidential
        fees harvested from token accounts -- the mint's ConfidentialTransferFeeConfig.harvest_to_mint_enabled
        flag. Authorized by the ConfidentialTransferFeeConfig's own authority, not
        the TransferFeeConfig's withdraw withheld authority. No zero-knowledge proof
        is needed, and the instruction carries no data beyond its own discriminant.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        mint stores instead, so the transaction never expires, and prepends the advance
        that consumes it; recent_blockhash then only prices the transaction. The response
        reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.DisableHarvestToMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.DisableHarvestToMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Stop a mint accepting harvested confidential fees
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-fee-config/enable-harvest-to-mint:
    post:
      consumes:
      - application/json
      description: Sets harvest_to_mint_enabled, so the mint accepts confidential
        fees harvested from token accounts -- the mint's ConfidentialTransferFeeConfig.harvest_to_mint_enabled
        flag. Authorized by the ConfidentialTransferFeeConfig's own authority, not
        the TransferFeeConfig's withdraw withheld authority. No zero-knowledge proof
        is needed, and the instruction carries no data beyond its own discriminant.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        mint stores instead, so the transaction never expires, and prepends the advance
        that consumes it; recent_blockhash then only prices the transaction. The response
        reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.EnableHarvestToMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.EnableHarvestToMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Let a mint accept harvested confidential fees
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-fee-config/harvest-withheld-tokens-to-mint:
    post:
      consumes:
      - application/json
      description: 'Moves the confidential fees withheld on each of source_accounts
        into the mint''s own withheld amount (ConfidentialTransferFeeConfig.withheld_amount),
        where the withdraw authority can then collect them all at once with withdraw-withheld-tokens-from-mint.
        It is permissionless: no account signs, so anyone with a fee payer can run
        it. The mint must have harvest_to_mint_enabled set (see enable-harvest-to-mint),
        which this endpoint checks. A source account that does not carry both TransferFeeAmount
        and ConfidentialTransferAccount is skipped by the program rather than rejected,
        so the response lists harvested_sources and skipped_sources; a request where
        nothing would be harvested is rejected. The transaction has to fit the 1232-byte
        limit, which caps how many sources one call can carry; this endpoint checks
        that and says so. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, source accounts, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialHarvestWithheldTokensToMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialHarvestWithheldTokensToMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move withheld confidential fees from token accounts to the mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-fee-config/initialize:
    post:
      consumes:
      - application/json
      description: 'Names who may later change this extension (authority, optional)
        and the ElGamal public key withheld confidential transfer fees are encrypted
        under (withdraw_withheld_authority_elgamal_pubkey, required, base58-encoded
        raw 32-byte ElGamal public key, not a Solana address). A mint that already
        charges an ordinary transfer fee refuses the plain confidential transfer and
        accepts only transfer-with-fee, which needs this extension. This can only
        ever run in the narrow window every mint extension shares: after create-mint
        has allocated the account and before initialize-mint2 locks the extension
        list forever -- there is no path back into an already-initialized mint. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, auto-approve flag, auditor key, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeConfidentialTransferFeeConfigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeConfidentialTransferFeeConfigResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Attach the ConfidentialTransferFeeConfig extension to a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-fee-config/withdraw-withheld-tokens-from-accounts:
    post:
      consumes:
      - application/json
      description: 'Moves the confidential fees withheld on each of source_accounts
        straight into destination''s available balance and zeroes them, without revealing
        the amount and without going through the mint (see withdraw-withheld-tokens-from-mint
        for that route). Authorized by the TransferFeeConfig''s withdraw withheld
        authority. Builds only the instruction: the CiphertextCiphertextEquality proof
        must already be verified into a context-state account, whose address is named
        here -- build the proof and new_decryptable_available_balance with tool/prove/confidential-withdraw-withheld-from-accounts
        using the same source_accounts, create the account with zk-elgamal-proof/context-state/create/ciphertext-ciphertext-equality,
        and verify it with context-state/verify/ciphertext-ciphertext-equality. The
        proof covers the sum of the sources'' withheld ciphertexts as they are when
        it is built, so any transfer into a source in between makes it fail. Every
        source must carry ConfidentialTransferFeeAmount, and the transaction has to
        fit the 1232-byte limit, which caps how many sources one call can carry; this
        endpoint checks both. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, sources, destination, equality context-state account, authority,
          and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialWithdrawWithheldTokensFromAccountsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialWithdrawWithheldTokensFromAccountsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Withdraw confidential fees withheld on token accounts into a token
        account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-fee-config/withdraw-withheld-tokens-from-mint:
    post:
      consumes:
      - application/json
      description: 'Moves the confidential fees gathered on the mint (see harvest-withheld-tokens-to-mint)
        into destination''s available balance and zeroes the mint''s withheld amount,
        without revealing it. Authorized by the TransferFeeConfig''s withdraw withheld
        authority. The destination may be any token account of this mint that carries
        ConfidentialTransferAccount, including the sender''s own. Builds only the
        instruction: the CiphertextCiphertextEquality proof must already be verified
        into a context-state account, whose address is named here -- build the proof
        and new_decryptable_available_balance with tool/prove/confidential-withdraw-withheld-from-mint,
        create the account with zk-elgamal-proof/context-state/create/ciphertext-ciphertext-equality,
        and verify it with context-state/verify/ciphertext-ciphertext-equality. The
        proof is bound to the mint''s withheld ciphertext and to the destination''s
        ElGamal key as they are when it is built, so harvesting more fees or reconfiguring
        the destination in between makes it fail. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, destination, equality context-state account, authority,
          and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ConfidentialWithdrawWithheldTokensFromMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ConfidentialWithdrawWithheldTokensFromMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Withdraw the mint's withheld confidential fees into a token account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-mint/initialize:
    post:
      consumes:
      - application/json
      description: 'Names who may later reconfigure this extension and approve new
        confidential accounts (authority), whether new accounts need that approval
        before use (auto_approve_new_accounts), and an optional auditor key that can
        decrypt any confidential transfer amount (auditor_elgamal_pubkey, base58-encoded
        raw 32-byte ElGamal public key, not a Solana address). This can only ever
        run in the narrow window every mint extension shares: after create-mint has
        allocated the account and before initialize-mint2 locks the extension list
        forever -- there is no path back into an already-initialized mint, no Reallocate
        equivalent exists for mints at all. This endpoint only builds InitializeMint,
        the one sub-instruction of the ConfidentialTransfer family that needs no zero-knowledge
        proof; the other 14 (ConfigureAccount, Deposit, Withdraw, Transfer, and the
        rest) are not built here. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, auto-approve flag, auditor key, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeConfidentialTransferMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeConfidentialTransferMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Attach the ConfidentialTransferMint extension to a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-mint/reallocate:
    post:
      consumes:
      - application/json
      description: 'Checks whether account already holds enough space for its existing
        extensions plus ConfidentialTransferMint, and grows it if not. The instruction
        itself only ever needs the one new extension: Reallocate reads account''s
        own existing extensions on chain and unions them with what this sends, so
        a caller never resends what is already there. Getting the resize''s rent right
        is this endpoint''s own job: it reads account''s current extensions and actual
        lamports, asks GetAccountDataSize for the full target size once ConfidentialTransferMint
        is unioned in, and only then knows rent_payer''s shortfall — the same authority
        Reallocate itself defers to, asked directly rather than recomputed here. ConfidentialTransferMint
        is a mint-side extension in the interface crate''s own numbering, not what
        Reallocate''s own account list expects (always a token account); nothing here
        stops a caller from naming it anyway, and the deployed program is what rejects
        it, not this endpoint. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, rent payer, owner, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ReallocateConfidentialTransferMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ReallocateConfidentialTransferMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a token account to hold room for ConfidentialTransferMint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/confidential-transfer-mint/update:
    post:
      consumes:
      - application/json
      description: 'Replaces auto_approve_new_accounts and auditor_elgamal_pubkey
        with the values given, authorized by authority (the role initialize named),
        not mint''s own mint or freeze authority. Both fields are replaced entirely
        -- passing the current value back is how a caller keeps one unchanged. Unlike
        initialize, authority itself is never reassignable through this endpoint:
        upstream''s UpdateMintData carries no authority field at all, since authority
        is proven by signing as an account rather than named as data. Confirmed against
        the interface crate''s update_mint: no zero-knowledge proof or context-state
        account is needed, the same as initialize. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, new configuration, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.UpdateConfidentialTransferMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.UpdateConfidentialTransferMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Change the ConfidentialTransferMint configuration on a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/cpi-guard/disable:
    post:
      consumes:
      - application/json
      description: Clears the account's CpiGuard.lock_cpi flag, so all token operations
        may happen via CPI as normal. Authorized by the account's owner. If the account
        does not carry the extension yet, the instruction adds it (unlocked), which
        needs room for it already in the account (see cpi-guard/reallocate). recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.DisableCpiGuardRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.DisableCpiGuardResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Turn the CPI guard off for a token account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/cpi-guard/enable:
    post:
      consumes:
      - application/json
      description: Sets the account's CpiGuard.lock_cpi flag. Within a cross-program
        invocation, Transfer and Burn must then go through a delegate, CloseAccount
        can only return lamports to the owner, SetAuthority can only remove a close
        authority, and Approve is disallowed. It cannot itself be enabled or disabled
        via CPI. Authorized by the account's owner. If the account does not carry
        the extension yet, the instruction adds it, which needs room for it already
        in the account (see cpi-guard/reallocate). recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.EnableCpiGuardRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.EnableCpiGuardResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Turn the CPI guard on for a token account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/cpi-guard/reallocate:
    post:
      consumes:
      - application/json
      description: 'Checks whether account already holds enough space for its existing
        extensions plus CpiGuard, and grows it if not -- the account-side extension
        cpi-guard/enable and /disable need room for when the account does not carry
        it yet. The instruction itself only ever needs the one new extension: Reallocate
        reads account''s own existing extensions on chain and unions them with what
        this sends, so a caller never resends what is already there. Getting the resize''s
        rent right is this endpoint''s own job: it reads account''s current extensions
        and actual lamports, asks GetAccountDataSize for the full target size once
        CpiGuard is unioned in, and only then knows rent_payer''s shortfall. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, rent payer, owner, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ReallocateCpiGuardRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ReallocateCpiGuardResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a token account to hold room for CpiGuard
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/memo-transfer/disable:
    post:
      consumes:
      - application/json
      description: Clears the account's MemoTransfer.require_incoming_transfer_memos
        flag, so transfers into it no longer need a Memo. Authorized by the account's
        owner. If the account does not carry the extension yet, the instruction adds
        it (disabled), which needs room for it already in the account (see memo-transfer/reallocate).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.DisableRequiredMemoTransfersRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.DisableRequiredMemoTransfersResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Stop requiring a memo on transfers into a token account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/memo-transfer/enable:
    post:
      consumes:
      - application/json
      description: Sets the account's MemoTransfer.require_incoming_transfer_memos
        flag, so every transfer into it must be preceded by a Memo instruction in
        the same transaction. Authorized by the account's owner. If the account does
        not carry the extension yet, the instruction adds it, which needs room for
        it already in the account (see memo-transfer/reallocate). No zero-knowledge
        proof is needed, and the instruction carries no data beyond its discriminants.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, owner, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.EnableRequiredMemoTransfersRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.EnableRequiredMemoTransfersResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Require a memo on transfers into a token account
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/memo-transfer/reallocate:
    post:
      consumes:
      - application/json
      description: 'Checks whether account already holds enough space for its existing
        extensions plus MemoTransfer, and grows it if not -- the account-side extension
        memo-transfer/enable and /disable need room for when the account does not
        carry it yet. The instruction itself only ever needs the one new extension:
        Reallocate reads account''s own existing extensions on chain and unions them
        with what this sends, so a caller never resends what is already there. Getting
        the resize''s rent right is this endpoint''s own job: it reads account''s
        current extensions and actual lamports, asks GetAccountDataSize for the full
        target size once MemoTransfer is unioned in, and only then knows rent_payer''s
        shortfall. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, rent payer, owner, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ReallocateMemoTransferRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ReallocateMemoTransferResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a token account to hold room for MemoTransfer
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/mint-close-authority/clear:
    post:
      consumes:
      - application/json
      description: 'Clears the MintCloseAuthority extension''s close_authority to
        None. This makes the mint permanently unclosable: the program accepts a None
        close_authority, and once it is None nothing can ever sign close-account against
        this mint again, nor can this endpoint or replace ever restore one. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetCloseMintAuthorityClearRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetCloseMintAuthorityClearResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Remove a mint's MintCloseAuthority close authority permanently
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/mint-close-authority/close:
    post:
      consumes:
      - application/json
      description: Closes mint, reclaiming its rent to recipient_account, authorized
        by the MintCloseAuthority extension's close_authority -- not mint_authority
        or freeze_authority. mint must already have zero supply (the program rejects
        otherwise with MintHasSupply) and must already carry the extension with an
        authority set (see extensions/mint-close-authority/initialize, or set-authority/replace
        if it was skipped at initialization time and later granted). This is the same
        generic CloseAccount instruction (opcode 9) close-account itself sends, since
        upstream's own processor tries a token account first and falls back to a mint
        -- the only reason this is a separate endpoint is that close-account's own
        validation decodes TokenAccount and checks its close_authority field, neither
        of which describes a mint, so mint's own extension has to be decoded and checked
        here instead. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, recipient, close authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.CloseMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.CloseMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Close a Token-2022 mint via its MintCloseAuthority extension
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/mint-close-authority/initialize:
    post:
      consumes:
      - application/json
      description: 'Names who may later close the mint via close-account -- without
        this extension a mint can never be closed at all, since the base layout has
        no close-authority field of its own the way a token account does. This can
        only ever run in the narrow window every mint extension shares: after create-mint
        has allocated the account and before initialize-mint2 locks the extension
        list forever -- there is no path back into an already-initialized mint, no
        Reallocate equivalent exists for mints at all. close_authority may be left
        empty to skip the extension; unlike transfer_fee_config_authority, upstream
        also exposes AuthorityType::CloseMint through the plain set-authority instruction,
        so a close authority can still be granted or replaced later even if none is
        set here. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, close authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeMintCloseAuthorityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeMintCloseAuthorityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Attach the MintCloseAuthority extension to a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/mint-close-authority/reallocate:
    post:
      consumes:
      - application/json
      description: 'Checks whether account already holds enough space for its existing
        extensions plus MintCloseAuthority, and grows it if not. The instruction itself
        only ever needs the one new extension: Reallocate reads account''s own existing
        extensions on chain and unions them with what this sends, so a caller never
        resends what is already there. Getting the resize''s rent right is this endpoint''s
        own job: it reads account''s current extensions and actual lamports, asks
        GetAccountDataSize for the full target size once MintCloseAuthority is unioned
        in, and only then knows rent_payer''s shortfall — the same authority Reallocate
        itself defers to, asked directly rather than recomputed here. MintCloseAuthority
        is a mint-side extension in the interface crate''s own numbering, not what
        Reallocate''s own account list expects (always a token account); nothing here
        stops a caller from naming it anyway, and the deployed program is what rejects
        it, not this endpoint. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, rent payer, owner, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ReallocateMintCloseAuthorityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ReallocateMintCloseAuthorityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a token account to hold room for MintCloseAuthority
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/mint-close-authority/replace:
    post:
      consumes:
      - application/json
      description: Hands the MintCloseAuthority extension's close_authority to new_close_authority.
        close_authority must be the mint's current close authority exactly; there
        is no delegate concept for this role. Unlike mint_authority or freeze_authority,
        this reuses the plain SetAuthority instruction with AuthorityType::CloseMint
        rather than a dedicated sub-instruction. Mint must already carry the extension
        with a close_authority set (see extensions/mint-close-authority/initialize)
        -- there is no granting one here where none exists. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetCloseMintAuthorityReplaceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetCloseMintAuthorityReplaceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Replace a mint's MintCloseAuthority close authority
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/non-transferable/initialize:
    post:
      consumes:
      - application/json
      description: 'Marks the mint as non-transferable: its tokens can be minted and
        burned but never moved between accounts, and no instruction ever makes the
        mint transferable again. Every token account of such a mint gets the NonTransferableAccount
        extension automatically and needs ImmutableOwner. This can only ever run in
        the narrow window every mint extension shares: after the mint account has
        been allocated with room for this extension (see extensions/mint/data-size)
        and before initialize-mint2 locks the extension list forever. No signer beyond
        the fee payer, and no data beyond the instruction''s own discriminant. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeNonTransferableMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeNonTransferableMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Make a mint non-transferable
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/permanent-delegate/initialize:
    post:
      consumes:
      - application/json
      description: 'Names a permanent delegate for the mint -- an authority that can
        transfer or burn any holder''s tokens of this mint, without approval and for
        as long as the mint exists. Required: a mint that should not have one does
        not carry this extension. This can only ever run in the narrow window every
        mint extension shares: after the mint account has been allocated with room
        for this extension (see extensions/mint/data-size) and before initialize-mint2
        locks the extension list forever. recent_blockhash is always required and
        is never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializePermanentDelegateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializePermanentDelegateResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Attach the PermanentDelegate extension to a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-amount/reallocate:
    post:
      consumes:
      - application/json
      description: 'Checks whether account already holds enough space for its existing
        extensions plus TransferFeeAmount, and grows it if not -- the account-side
        extension a destination needs before it can receive a transfer from a fee-charging
        mint; without it, any transfer that computes a non-zero fee fails as InvalidState.
        The instruction itself only ever needs the one new extension: Reallocate reads
        account''s own existing extensions on chain and unions them with what this
        sends, so a caller never resends what is already there. Getting the resize''s
        rent right is this endpoint''s own job: it reads account''s current extensions
        and actual lamports, asks GetAccountDataSize for the full target size once
        TransferFeeAmount is unioned in, and only then knows rent_payer''s shortfall
        — the same authority Reallocate itself defers to, asked directly rather than
        recomputed here. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, rent payer, owner, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ReallocateTransferFeeAmountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ReallocateTransferFeeAmountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a token account to hold room for TransferFeeAmount
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-config/harvest:
    post:
      consumes:
      - application/json
      description: 'Sweeps whatever each of source_token_accounts has withheld in
        its own TransferFeeAmount extension into mint''s TransferFeeConfig withheld_amount.
        This is permissionless: no authority field exists here at all, since moving
        a balance between two places it can already only ever sit -- an account''s
        own withheld fees, or the mint''s -- needs nobody''s permission, only the
        mint they all agree on. Getting a balance out of the mint (or straight out
        of the accounts, bypassing the mint) is withdraw-withheld-tokens-from-mint/from-accounts''s
        job instead, and those do require withdraw_withheld_authority''s signature.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, source token accounts, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.HarvestWithheldTokensToMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.HarvestWithheldTokensToMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Sweep withheld transfer fees from token accounts into the mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-config/initialize:
    post:
      consumes:
      - application/json
      description: 'Fixes the fee rate every transfer-checked-with-fee withholds and
        who may later change it (transfer_fee_config_authority) or withdraw what accumulates
        (withdraw_withheld_authority). This can only ever run in the narrow window
        every mint extension shares: after create-mint has allocated the account and
        before initialize-mint2 locks the extension list forever -- there is no path
        back into an already-initialized mint, no Reallocate equivalent exists for
        mints at all. transfer_fee_basis_points is out of 10,000 and is validated
        here rather than left to come back as an on-chain rejection. Either authority
        may be left empty to permanently forgo that capability; unlike a mint or freeze
        authority there is no later instruction that grants one where none was set.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authorities, fee rate, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeTransferFeeConfigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeTransferFeeConfigResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Attach the TransferFeeConfig extension to a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-config/reallocate:
    post:
      consumes:
      - application/json
      description: 'Checks whether account already holds enough space for its existing
        extensions plus TransferFeeConfig, and grows it if not. The instruction itself
        only ever needs the one new extension: Reallocate reads account''s own existing
        extensions on chain and unions them with what this sends, so a caller never
        resends what is already there. Getting the resize''s rent right is this endpoint''s
        own job: it reads account''s current extensions and actual lamports, asks
        GetAccountDataSize for the full target size once TransferFeeConfig is unioned
        in, and only then knows rent_payer''s shortfall — the same authority Reallocate
        itself defers to, asked directly rather than recomputed here. TransferFeeConfig
        is a mint-side extension in the interface crate''s own numbering, not what
        Reallocate''s own account list expects (always a token account); nothing here
        stops a caller from naming it anyway, and the deployed program is what rejects
        it, not this endpoint. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Account, rent payer, owner, fee payer, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ReallocateTransferFeeConfigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ReallocateTransferFeeConfigResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Grow a token account to hold room for TransferFeeConfig
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-config/set:
    post:
      consumes:
      - application/json
      description: 'Sets a new fee rate and maximum, authorized by transfer_fee_config_authority
        (the role initialize-transfer-fee-config named), not mint''s own mint or freeze
        authority. The change is not immediate: the program keeps an older and a newer
        rate side by side, each stamped with the epoch it takes effect at. What this
        sets becomes the newer rate, effective at the start of the next epoch; whatever
        was already active keeps applying until then, enforced on chain rather than
        by anything this endpoint does. transfer_fee_basis_points is out of 10,000
        and is validated here rather than left to come back as an on-chain rejection.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, authority, new fee rate, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetTransferFeeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetTransferFeeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Change the TransferFeeConfig rate on a mint
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-config/transfer:
    post:
      consumes:
      - application/json
      description: 'TransferChecked plus a fee: source_token_account_authority and
        decimals are checked exactly the same way, and the fee withheld into destination_token_account''s
        TransferFeeAmount extension is the only addition. There is no fee field to
        set -- unlike Reallocate''s own generous tolerance, the deployed program recomputes
        this fee itself from mint''s current TransferFeeConfig rate and requires whatever
        this instruction carries to match byte for byte, failing the whole transfer
        as FeeMismatch over a single base unit of difference. This handler reads mint''s
        TransferFeeConfig and the cluster''s current epoch itself and computes the
        one fee that can ever be correct, reported back as transfer_fee and epoch.
        Building a transaction here and sending it after the epoch rolls over risks
        landing against a stale rate; rebuild first if that much time has passed.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Transfer parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.TransferCheckedWithFeeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.TransferCheckedWithFeeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move tokens between accounts, withholding the mint's transfer fee
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-config/withdraw-from-accounts:
    post:
      consumes:
      - application/json
      description: 'Moves whatever each of source_token_accounts has withheld in its
        own TransferFeeAmount extension straight to destination, bypassing mint''s
        own TransferFeeConfig withheld_amount entirely -- the shortcut harvest-withheld-tokens-to-mint
        does not take. mint itself is read-only: it is named only so the program can
        confirm every source actually belongs to it, never written to. Unlike harvest-withheld-tokens-to-mint
        this does require withdraw_withheld_authority to sign, the same authority
        withdraw-withheld-tokens-from-mint answers to, since real tokens are leaving
        the accounts entirely rather than settling into a shared pool anyone could
        later account for. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, destination, authority, source token accounts, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.WithdrawWithheldTokensFromAccountsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.WithdrawWithheldTokensFromAccountsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Withdraw withheld transfer fees directly from token accounts
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/extensions/transfer-fee-config/withdraw-from-mint:
    post:
      consumes:
      - application/json
      description: 'Moves mint''s own TransferFeeConfig withheld_amount out to destination
        as real tokens, authorized by withdraw_withheld_authority rather than mint''s
        own mint or freeze authority. This is the counterpart harvest-withheld-tokens-to-mint''s
        permissionless sweep feeds: harvesting only ever moves a balance into the
        mint, never out of it, so getting it out from there to somewhere spendable
        is this endpoint''s job alone, and it is the one step in the whole withheld-fee
        lifecycle that actually requires a signature. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint, destination, authority, and program
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.WithdrawWithheldTokensFromMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.WithdrawWithheldTokensFromMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Withdraw a mint's accumulated withheld transfer fees
      tags:
      - v2-transaction-token-extensions
  /svm/v2/transaction/token/freeze-account:
    post:
      consumes:
      - application/json
      description: 'Freezes token_account, so it rejects transfer, burn, and approve
        until thawed. freeze_authority is the mint''s freeze authority, not the account''s
        owner or any delegate: freezing is a mint-level power to suspend any account
        holding it, on a different axis from who may spend a balance. Only works on
        a mint that was initialized with a freeze authority. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Freeze parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.FreezeAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.FreezeAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Suspend a token account
      tags:
      - v2-transaction-token-freeze
  /svm/v2/transaction/token/initialize-account:
    post:
      consumes:
      - application/json
      description: Raw InitializeAccount, the original opcode that passes owner as
        a read-only account and carries the rent sysvar alongside it; the program
        never checks owner against a signer, and neither is read for anything but
        its address. See initialize-account3 for the variant without either. token_account
        must already exist, be owned by program, be at least 165 bytes (Token-2022
        extensions may make it larger), and be uninitialized. recent_blockhash is
        always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-account parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as an SPL Token holder account
        (original opcode)
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/initialize-account2:
    post:
      consumes:
      - application/json
      description: 'Raw InitializeAccount2: owner rides in the instruction data rather
        than as an account, dropping the account InitializeAccount carries; the rent
        sysvar is still read. See initialize-account3 for the variant without it.
        token_account must already exist, be owned by program, be at least 165 bytes
        (Token-2022 extensions may make it larger), and be uninitialized. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-account parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeAccount2Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeAccount2Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as an SPL Token holder account
        (owner in data)
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/initialize-account3:
    post:
      consumes:
      - application/json
      description: 'Raw InitializeAccount3: owner rides in the instruction data and
        the rent sysvar is dropped entirely. This is the natural pairing for create-kta,
        which only creates the account and never initializes it. token_account must
        already exist, be owned by program, be at least 165 bytes (Token-2022 extensions
        may make it larger), and be uninitialized. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-account parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeAccount3Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeAccount3Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as an SPL Token holder account
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/initialize-immutable-owner:
    post:
      consumes:
      - application/json
      description: 'Locks token_account''s owner field against SetAuthority forever.
        This is a Token-2022 extension: it needs extension space appended after the
        classic 165-byte layout, which classic Token accounts never have. On Token-2022
        this attaches the real extension; on classic Token, upstream documents this
        opcode as a no-op, kept only so the Associated Token Account program can call
        it on either program without branching, so program accepts either. token_account
        must already exist, be owned by program, be at least 165 bytes, and — critically
        — still be Uninitialized at the base layout: the program itself requires extensions
        to attach before initialize-account*/initialize-mint* commits the account,
        not after, so this has to run first or it fails on chain. Whether the extension
        space itself is actually reserved is not verified here (Token-2022 extension
        TLV parsing is its own separate undertaking), so that specific mismatch still
        fails on chain rather than as a 400. There is no authority: nothing about
        locking the owner field needs proving. The Associated Token Account program
        calls this automatically on every account it creates, regardless of program,
        before its own initialize-account3 equivalent. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-immutable-owner parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeImmutableOwnerRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeImmutableOwnerResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Permanently lock a token account's owner field
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/initialize-mint:
    post:
      consumes:
      - application/json
      description: Raw InitializeMint, the original opcode that carries the rent sysvar
        as a read-only account alongside mint; the program stopped reading it once
        rent collection was disabled. See initialize-mint2 for the variant without
        it. mint must already exist, be owned by program, be at least 82 bytes (Token-2022
        extensions may make it larger), and be uninitialized. recent_blockhash is
        always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-mint parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeMintRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeMintResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as an SPL Token mint (original
        opcode)
      tags:
      - v2-transaction-token-mint-account
  /svm/v2/transaction/token/initialize-mint2:
    post:
      consumes:
      - application/json
      description: 'Raw InitializeMint2, with no CreateAccount alongside it: mint
        must already exist, be owned by program, be at least 82 bytes (Token-2022
        extensions may make it larger), and be uninitialized. This is the natural
        pairing for create-mint, which only creates the account and never initializes
        it; nothing stops somebody else from initializing it first in between, so
        a caller who wants that race closed has to build create+initialize as two
        instructions in one transaction themselves. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-mint parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeMint2Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeMint2Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as an SPL Token mint
      tags:
      - v2-transaction-token-mint-account
  /svm/v2/transaction/token/initialize-multisig:
    post:
      consumes:
      - application/json
      description: Raw InitializeMultisig, the original opcode that carries the rent
        sysvar as a read-only account alongside multisig; the program never reads
        it for anything else. See initialize-multisig2 for the variant without it.
        multisig_account must already exist, be owned by program, be exactly 355 bytes,
        and be uninitialized. Anywhere the Token Program takes an authority, a multisig
        may stand in for it instead, and the named signers sign in its place. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-multisig parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeMultisigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeMultisigResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as a Token multisig (original
        opcode)
      tags:
      - v2-transaction-token-multisig
  /svm/v2/transaction/token/initialize-multisig2:
    post:
      consumes:
      - application/json
      description: 'Raw InitializeMultisig2: the rent sysvar InitializeMultisig reads
        is dropped. multisig_account must already exist, be owned by program, be exactly
        355 bytes, and be uninitialized. Anywhere the Token Program takes an authority,
        a multisig may stand in for it instead, and the named signers sign in its
        place. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-multisig parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeMultisig2Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeMultisig2Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as a Token multisig
      tags:
      - v2-transaction-token-multisig
  /svm/v2/transaction/token/initialize-wrapped-sol:
    post:
      consumes:
      - application/json
      description: 'Raw InitializeAccount3 with mint fixed to program''s own native
        mint, rather than taken from the request. Classic Token''s native mint is
        the fixed well-known address; Token-2022''s is a separate PDA, resolved here
        rather than hardcoded. There is no create-wrapped-sol or wrap-sol composite:
        this pairs with create-kta/create-ata the same way initialize-account3 does,
        and funding it is a plain system/transfer followed by sync-native. token_account
        must already exist, be owned by program, be at least 165 bytes (Token-2022
        extensions may make it larger), and be uninitialized. recent_blockhash is
        always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Initialize-wrapped-sol parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.InitializeWrappedSolRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.InitializeWrappedSolResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Initialize an already-existing account as a wrapped-SOL holder account
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/mint-to:
    post:
      consumes:
      - application/json
      description: 'Raw MintTo, the original opcode: decimals is neither named nor
        verified against the mint. mint_authority must be mint''s own mint authority.
        This is for compatibility with the original opcode; mint-to-checked remains
        the normal public path. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.MintToRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.MintToResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create new supply into an existing token account (original opcode)
      tags:
      - v2-transaction-token-supply
  /svm/v2/transaction/token/mint-to-checked:
    post:
      consumes:
      - application/json
      description: Creates new supply and credits an existing token account. decimals
        is checked against the mint rather than filled in from it, which is what catches
        a client that formatted amount against the wrong decimals as a 400 instead
        of an on-chain failure. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Mint-to parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.MintToCheckedRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.MintToCheckedResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Mint new supply into a token account
      tags:
      - v2-transaction-token-supply
  /svm/v2/transaction/token/revoke:
    post:
      consumes:
      - application/json
      description: 'Revokes token_account''s delegate and delegated amount, whatever
        they are, without naming either: the program clears what is stored, so there
        is nothing to get wrong by naming it. A token_account with no delegate revokes
        cleanly too. Only token_account_owner may revoke, matching approve-checked''s
        rule that only the owner may grant one. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Revoke parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.RevokeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.RevokeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Clear whatever delegation an account currently has
      tags:
      - v2-transaction-token-delegation
  /svm/v2/transaction/token/set-authority/close/clear:
    post:
      consumes:
      - application/json
      description: 'Clears close_authority to None. This is recoverable: the owner
        never goes away, and close-account already falls back to the owner when no
        close authority is set, so clearing this only reverts to that default. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetCloseAuthorityClearRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetCloseAuthorityClearResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Remove a token account's close authority
      tags:
      - v2-transaction-token-authority
  /svm/v2/transaction/token/set-authority/close/replace:
    post:
      consumes:
      - application/json
      description: 'Hands token_account_close_authority to new_token_account_close_authority.
        token_account_close_authority is whichever one is already recorded: the account''s
        close authority if one is set, otherwise its owner, the same rule close-account
        itself checks. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetCloseAuthorityReplaceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetCloseAuthorityReplaceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Replace a token account's close authority
      tags:
      - v2-transaction-token-authority
  /svm/v2/transaction/token/set-authority/freeze/clear:
    post:
      consumes:
      - application/json
      description: Clears freeze_authority to None. No holder of this mint can ever
        be frozen afterward, and nothing can restore the capability. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetFreezeAuthorityClearRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetFreezeAuthorityClearResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Remove a mint's freeze authority permanently
      tags:
      - v2-transaction-token-authority
  /svm/v2/transaction/token/set-authority/freeze/replace:
    post:
      consumes:
      - application/json
      description: Hands freeze_authority to new_freeze_authority. freeze_authority
        must be the mint's existing freeze_authority exactly. recent_blockhash is
        always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetFreezeAuthorityReplaceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetFreezeAuthorityReplaceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Replace a mint's freeze authority
      tags:
      - v2-transaction-token-authority
  /svm/v2/transaction/token/set-authority/mint/clear:
    post:
      consumes:
      - application/json
      description: 'Clears mint_authority to None. This caps the supply forever: the
        program accepts a None mint_authority, and once it is None nothing can ever
        sign as that authority again to restore it. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetMintAuthorityClearRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetMintAuthorityClearResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Remove a mint's mint authority permanently
      tags:
      - v2-transaction-token-authority
  /svm/v2/transaction/token/set-authority/mint/replace:
    post:
      consumes:
      - application/json
      description: Hands mint_authority to new_mint_authority. mint_authority must
        be the mint's existing mint_authority exactly; there is no delegate concept
        for this role. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetMintAuthorityReplaceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetMintAuthorityReplaceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Replace a mint's mint authority
      tags:
      - v2-transaction-token-authority
  /svm/v2/transaction/token/set-authority/owner/replace:
    post:
      consumes:
      - application/json
      description: 'Hands token_account to new_token_account_owner. token_account_owner
        must be token_account''s existing owner exactly, never a delegate. There is
        no clear variant: the owner field has no None representation on chain, and
        the program rejects one. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Authority change parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SetAccountOwnerReplaceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SetAccountOwnerReplaceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Replace a token account's owner
      tags:
      - v2-transaction-token-authority
  /svm/v2/transaction/token/sync-native:
    post:
      consumes:
      - application/json
      description: 'A wrapped-SOL account''s amount is not the same field as its lamports:
        lamports can change independently, by a plain System transfer landing on the
        account directly, and nothing updates amount when that happens. This is the
        only instruction that reconciles the two, setting amount to lamports minus
        the rent-exempt reserve. token_account must already exist, be owned by program,
        and actually be a wrapped-SOL account — the program rejects one that is not,
        and this endpoint checks the same thing client-side. There is no authority:
        recomputing a derived value from what the account already holds needs nobody''s
        permission. estimated_amount in the response is exactly that — an estimate
        computed from the account''s lamports at read time, not the value the program
        itself will use, which is computed fresh at landing time. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Sync-native parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.SyncNativeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.SyncNativeResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Recompute a wrapped-SOL account's token balance from its lamports
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/thaw-account:
    post:
      consumes:
      - application/json
      description: Reverses FreezeAccount, letting transfer, burn, and approve resume
        against token_account. freeze_authority is the mint's freeze authority, the
        same rule FreezeAccount applies. recent_blockhash is always required and is
        never fetched server-side. Left alone, it also builds the message and expires
        whenever the runtime says it does. Naming durable_nonce_account builds the
        message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Thaw parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.ThawAccountRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ThawAccountResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Resume a suspended token account
      tags:
      - v2-transaction-token-freeze
  /svm/v2/transaction/token/transfer:
    post:
      consumes:
      - application/json
      description: 'Raw Transfer, the original opcode: neither the mint nor its decimals
        is named or verified against the accounts, which is exactly the failure mode
        transfer-checked exists to catch. mint is still resolved server-side from
        source_token_account''s own stored value and reported in the response; destination_token_account
        is still checked against it. source_token_account_authority must be source_token_account''s
        owner, or its delegate for no more than the delegated amount. This is for
        compatibility with the original opcode; transfer-checked remains the normal
        public path. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Transfer parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.TransferRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.TransferResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move a balance between two token accounts (original opcode)
      tags:
      - v2-transaction-token-transfer
  /svm/v2/transaction/token/transfer-checked:
    post:
      consumes:
      - application/json
      description: Transfers between token accounts, never to a wallet address directly.
        decimals is checked against the mint rather than filled in from it, catching
        a client that formatted amount against the wrong decimals as a 400 instead
        of an on-chain failure. source_token_account_authority must be source_token_account's
        owner, or its delegate for no more than the delegated amount. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Transfer parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.TransferCheckedRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.TransferCheckedResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move a balance between two token accounts of the same mint
      tags:
      - v2-transaction-token-transfer
  /svm/v2/transaction/token/transfer-checked/max:
    post:
      consumes:
      - application/json
      description: 'Same as transfer-checked, except the amount moved is source_token_account''s
        current balance rather than a caller-given one. decimals is never a request
        field, since there is no client-computed amount to protect against a wrong
        decimals assumption; the mint''s own value is used to build the instruction
        directly. Unlike a lamport sweep, source_token_account carries no rent-exemption
        floor: a token account can hold zero tokens and still exist, so it is always
        swept all the way to zero, even when it is also fee_payer. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Transfer parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.TransferCheckedMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.TransferCheckedMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Sweep a token account's entire balance to another token account
      tags:
      - v2-transaction-token-transfer
  /svm/v2/transaction/token/transfer-from-ata:
    post:
      consumes:
      - application/json
      description: 'Derives the source''s associated token account from owner and
        mint; destination_token_account is an exact address, keypair or associated,
        exactly as transfer-checked takes it. The source is never created here: an
        account nobody has funded has nothing to send, so a missing one fails rather
        than being created empty. decimals is checked against the mint rather than
        filled in from it, catching a client that formatted amount against the wrong
        decimals as a 400 instead of an on-chain failure. source_token_account_authority
        must be the derived source''s owner, or its delegate for no more than the
        delegated amount. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Transfer parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.TransferFromATARequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.TransferFromATAResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Move a balance from an owner's associated token account to any token
        account
      tags:
      - v2-transaction-token-ata
  /svm/v2/transaction/token/transfer-from-ata/max:
    post:
      consumes:
      - application/json
      description: 'Same as transfer-from-ata, except the amount moved is the derived
        source''s current balance rather than a caller-given one. decimals is never
        a request field, since there is no client-computed amount to protect against
        a wrong decimals assumption; the mint''s own value is used to build the instruction
        directly. Unlike a lamport sweep, the source carries no rent-exemption floor:
        a token account can hold zero tokens and still exist, so it is always swept
        all the way to zero, even when it is also fee_payer. recent_blockhash is always
        required and is never fetched server-side. Left alone, it also builds the
        message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Transfer parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.TransferFromATAMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.TransferFromATAMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Sweep an owner's associated token account balance to any token account
      tags:
      - v2-transaction-token-ata
  /svm/v2/transaction/token/transfer/max:
    post:
      consumes:
      - application/json
      description: 'Same as transfer, except the amount moved is source_token_account''s
        current balance rather than a caller-given one. Neither the mint nor decimals
        is named or verified against the accounts, the same as transfer; mint is still
        resolved server-side and reported in the response. Unlike a lamport sweep,
        source_token_account carries no rent-exemption floor: a token account can
        hold zero tokens and still exist, so it is always swept all the way to zero,
        even when it is also fee_payer. This is for compatibility with the original
        opcode; transfer-checked/max remains the normal public path. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Transfer parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.TransferMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.TransferMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Sweep a token account's entire balance to another token account (original
        opcode)
      tags:
      - v2-transaction-token-transfer
  /svm/v2/transaction/token/unwrap-lamports:
    post:
      consumes:
      - application/json
      description: 'Unlike close-account, source_token_account is never consumed:
        it stays exactly as it was, still rent-exempt and still wrapping whatever
        is left — the partial counterpart to closing a wrapped-SOL account entirely.
        The instruction''s amount is an optional u64 with a one-byte tag, not the
        4-byte COption tag older instructions use; this endpoint always sends it present,
        and unwrap-lamports/max always sends it absent. amount must not exceed the
        wrapped balance. source_token_account_authority must be its owner, or its
        delegate for no more than the delegated amount — spending wrapped SOL out
        as raw lamports is a spend, not a close, the same axis transfer-checked and
        burn-checked use rather than close-account''s close_authority.unwrap_or(owner).
        source_token_account must already exist, be owned by program, and actually
        be a wrapped-SOL account. destination_token_account receives the unwrapped
        lamports directly as SOL, not tokens; it need not be a token account at all.
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Unwrap-lamports parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.UnwrapLamportsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.UnwrapLamportsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Pull exactly amount lamports out of a wrapped-SOL account without closing
        it
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/unwrap-lamports/max:
    post:
      consumes:
      - application/json
      description: Same as unwrap-lamports, except the instruction's amount goes out
        absent, which the program reads as the whole wrapped balance. source_token_account
        is left holding exactly its rent-exempt reserve, still initialized and still
        wrapped SOL, ready to be funded again — which is what separates this from
        close-account. source_token_account_authority must be its owner, or its delegate
        approved for at least the whole balance. estimated_amount in the response
        is the wrapped balance at read time; the program computes the real figure
        when this lands. source_token_account must already exist, be owned by program,
        and actually be a wrapped-SOL account. destination_token_account receives
        the unwrapped lamports directly as SOL, not tokens; it need not be a token
        account at all. recent_blockhash is always required and is never fetched server-side.
        Left alone, it also builds the message and expires whenever the runtime says
        it does. Naming durable_nonce_account builds the message against the value
        that account stores instead, so the transaction never expires, and prepends
        the advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Unwrap-lamports parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.UnwrapLamportsMaxRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.UnwrapLamportsMaxResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Pull a wrapped-SOL account's entire balance out as lamports without
        closing it
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/token/withdraw-excess-lamports:
    post:
      consumes:
      - application/json
      description: 'Recovers whatever lamports account holds beyond its own rent-exemption
        minimum, to destination. Unlike close-account, account is never consumed —
        it stays exactly as it was, rent-exempt and still carrying whatever mint,
        token, or multisig state it held. This is for the ordinary way an account
        ends up overfunded: a plain System transfer landing on it by mistake, since
        System''s own Transfer takes any account regardless of who owns it. Which
        role authority actually has to be depends on what account is (a mint''s close
        authority extension, a token account''s close_authority.unwrap_or(owner),
        or a multisig''s own enrolled signers) and this endpoint has no Token-2022
        extension parser to settle that ahead of time, so a wrong authority fails
        on chain rather than as a 400. estimated_recovered in the response is exactly
        that — an estimate computed from account''s balance and size at read time,
        not the value the program itself will use, which is computed fresh at landing
        time. This opcode is unverified: confirm it exists on the deployed program
        before relying on it. recent_blockhash is always required and is never fetched
        server-side. Left alone, it also builds the message and expires whenever the
        runtime says it does. Naming durable_nonce_account builds the message against
        the value that account stores instead, so the transaction never expires, and
        prepends the advance that consumes it; recent_blockhash then only prices the
        transaction. The response reports nonce_authority in that case, which has
        to sign as well.'
      parameters:
      - description: Cluster name
        in: header
        name: X-Chain-Name
        required: true
        type: string
      - description: Cluster network
        in: header
        name: X-Chain-Network
        required: true
        type: string
      - description: Withdraw-excess-lamports parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/v2.WithdrawExcessLamportsRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.WithdrawExcessLamportsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Recover a Token-owned account's excess lamports above its rent-exemption
        minimum
      tags:
      - v2-transaction-token-token-account
  /svm/v2/transaction/zk-elgamal-proof/context-state/close:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof CloseContextState: closes context_state_account
        and sends its lamports to destination. One endpoint serves every proof type,
        since the instruction takes the same three accounts whatever proof the account
        holds. context_state_account_owner must be the authority recorded in the account
        when it was verified (see context-state/verify), and it signs. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state close request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCloseRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCloseResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Close a context-state account and reclaim its rent
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/batched-grouped-ciphertext-2-handles-validity:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (289 bytes) and owned (by the
        ZkElgamalProof program) for a BatchedGroupedCiphertext2HandlesValidity proof''s
        context, but not yet written to. This is deliberately the low-level half:
        the account must be created in the same transaction as (and strictly before)
        the matching context-state/verify-batched-grouped-ciphertext-2-handles-validity
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-batched-grouped-ciphertext-2-handles-validity
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateBatchedGroupedCiphertext2HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateBatchedGroupedCiphertext2HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a BatchedGroupedCiphertext2HandlesValidity
        proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/batched-grouped-ciphertext-3-handles-validity:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (385 bytes) and owned (by the
        ZkElgamalProof program) for a BatchedGroupedCiphertext3HandlesValidity proof''s
        context, but not yet written to. This is deliberately the low-level half:
        the account must be created in the same transaction as (and strictly before)
        the matching context-state/verify-batched-grouped-ciphertext-3-handles-validity
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-batched-grouped-ciphertext-3-handles-validity
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateBatchedGroupedCiphertext3HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateBatchedGroupedCiphertext3HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a BatchedGroupedCiphertext3HandlesValidity
        proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/batched-range-proof-u64:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (297 bytes) and owned (by the
        ZkElgamalProof program) for a BatchedRangeProofU64 proof''s context, but not
        yet written to. This is deliberately the low-level half: the account must
        be created in the same transaction as (and strictly before) the matching context-state/verify-batched-range-proof-u64
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-batched-range-proof-u64
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateBatchedRangeProofU64Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateBatchedRangeProofU64Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a BatchedRangeProofU64 proof
        context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/batched-range-proof-u128:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (297 bytes) and owned (by the
        ZkElgamalProof program) for a BatchedRangeProofU128 proof''s context, but
        not yet written to. This is deliberately the low-level half: the account must
        be created in the same transaction as (and strictly before) the matching context-state/verify-batched-range-proof-u128
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-batched-range-proof-u128
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateBatchedRangeProofU128Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateBatchedRangeProofU128Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a BatchedRangeProofU128 proof
        context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/batched-range-proof-u256:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (297 bytes) and owned (by the
        ZkElgamalProof program) for a BatchedRangeProofU256 proof''s context, but
        not yet written to. This is deliberately the low-level half: the account must
        be created in the same transaction as (and strictly before) the matching context-state/verify-batched-range-proof-u256
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-batched-range-proof-u256
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateBatchedRangeProofU256Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateBatchedRangeProofU256Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a BatchedRangeProofU256 proof
        context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/ciphertext-ciphertext-equality:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (225 bytes) and owned (by the
        ZkElgamalProof program) for a CiphertextCiphertextEquality proof''s context,
        but not yet written to. This is deliberately the low-level half: the account
        must be created in the same transaction as (and strictly before) the matching
        context-state/verify-ciphertext-ciphertext-equality instruction, or another
        party can claim it first -- build create+verify as two instructions in one
        transaction, calling this endpoint and context-state/verify-ciphertext-ciphertext-equality
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateCiphertextCiphertextEqualityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateCiphertextCiphertextEqualityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a CiphertextCiphertextEquality
        proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/ciphertext-commitment-equality:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (161 bytes) and owned (by the
        ZkElgamalProof program) for a CiphertextCommitmentEquality proof''s context,
        but not yet written to. This is deliberately the low-level half: the account
        must be created in the same transaction as (and strictly before) the matching
        context-state/verify-ciphertext-commitment-equality instruction, or another
        party can claim it first -- build create+verify as two instructions in one
        transaction, calling this endpoint and context-state/verify-ciphertext-commitment-equality
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateCiphertextCommitmentEqualityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateCiphertextCommitmentEqualityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a CiphertextCommitmentEquality
        proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/grouped-ciphertext-2-handles-validity:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (193 bytes) and owned (by the
        ZkElgamalProof program) for a GroupedCiphertext2HandlesValidity proof''s context,
        but not yet written to. This is deliberately the low-level half: the account
        must be created in the same transaction as (and strictly before) the matching
        context-state/verify-grouped-ciphertext-2-handles-validity instruction, or
        another party can claim it first -- build create+verify as two instructions
        in one transaction, calling this endpoint and context-state/verify-grouped-ciphertext-2-handles-validity
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateGroupedCiphertext2HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateGroupedCiphertext2HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a GroupedCiphertext2HandlesValidity
        proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/grouped-ciphertext-3-handles-validity:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (257 bytes) and owned (by the
        ZkElgamalProof program) for a GroupedCiphertext3HandlesValidity proof''s context,
        but not yet written to. This is deliberately the low-level half: the account
        must be created in the same transaction as (and strictly before) the matching
        context-state/verify-grouped-ciphertext-3-handles-validity instruction, or
        another party can claim it first -- build create+verify as two instructions
        in one transaction, calling this endpoint and context-state/verify-grouped-ciphertext-3-handles-validity
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateGroupedCiphertext3HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateGroupedCiphertext3HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a GroupedCiphertext3HandlesValidity
        proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/percentage-with-cap:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (137 bytes) and owned (by the
        ZkElgamalProof program) for a PercentageWithCap proof''s context, but not
        yet written to. This is deliberately the low-level half: the account must
        be created in the same transaction as (and strictly before) the matching context-state/verify-percentage-with-cap
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-percentage-with-cap
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreatePercentageWithCapRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreatePercentageWithCapResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a PercentageWithCap proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/pubkey-validity:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (65 bytes) and owned (by the
        ZkElgamalProof program) for a PubkeyValidity proof''s context, but not yet
        written to. This is deliberately the low-level half: the account must be created
        in the same transaction as (and strictly before) the matching context-state/verify-pubkey-validity
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-pubkey-validity
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreatePubkeyValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreatePubkeyValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a PubkeyValidity proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/create/zero-ciphertext:
    post:
      consumes:
      - application/json
      description: 'System CreateAccount only, sized (129 bytes) and owned (by the
        ZkElgamalProof program) for a ZeroCiphertext proof''s context, but not yet
        written to. This is deliberately the low-level half: the account must be created
        in the same transaction as (and strictly before) the matching context-state/verify-zero-ciphertext
        instruction, or another party can claim it first -- build create+verify as
        two instructions in one transaction, calling this endpoint and context-state/verify-zero-ciphertext
        and merging their instructions yourself. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state create-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateCreateZeroCiphertextRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateCreateZeroCiphertextResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Fund a new account, sized and owned for a ZeroCiphertext proof context-state
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/batched-grouped-ciphertext-2-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedGroupedCiphertext2HandlesValidity,
        proof-in-account and context-state form: reads the proof from proof_account
        at proof_offset -- five bytes of instruction data however large the proof
        is -- and, because context_state_account and context_state_account_owner are
        given, writes the proof''s context into that account. This is the way to verify
        a proof too large to carry in one transaction, such as a 256-bit range proof:
        write it into a record account first (record/create-account, record/initialize,
        record/write), then verify it from there with proof_offset 33. context_state_account
        must already exist, sized and owned for this proof type (see context-state/create/batched-grouped-ciphertext-2-handles-validity).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext2HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext2HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedGroupedCiphertext2HandlesValidity proof from an account
        and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/batched-grouped-ciphertext-3-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedGroupedCiphertext3HandlesValidity,
        proof-in-account and context-state form: reads the proof from proof_account
        at proof_offset -- five bytes of instruction data however large the proof
        is -- and, because context_state_account and context_state_account_owner are
        given, writes the proof''s context into that account. This is the way to verify
        a proof too large to carry in one transaction, such as a 256-bit range proof:
        write it into a record account first (record/create-account, record/initialize,
        record/write), then verify it from there with proof_offset 33. context_state_account
        must already exist, sized and owned for this proof type (see context-state/create/batched-grouped-ciphertext-3-handles-validity).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext3HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedGroupedCiphertext3HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedGroupedCiphertext3HandlesValidity proof from an account
        and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/batched-range-proof-u64:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedRangeProofU64, proof-in-account and
        context-state form: reads the proof from proof_account at proof_offset --
        five bytes of instruction data however large the proof is -- and, because
        context_state_account and context_state_account_owner are given, writes the
        proof''s context into that account. This is the way to verify a proof too
        large to carry in one transaction, such as a 256-bit range proof: write it
        into a record account first (record/create-account, record/initialize, record/write),
        then verify it from there with proof_offset 33. context_state_account must
        already exist, sized and owned for this proof type (see context-state/create/batched-range-proof-u64).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedRangeProofU64Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedRangeProofU64Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedRangeProofU64 proof from an account and persist its
        context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/batched-range-proof-u128:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedRangeProofU128, proof-in-account and
        context-state form: reads the proof from proof_account at proof_offset --
        five bytes of instruction data however large the proof is -- and, because
        context_state_account and context_state_account_owner are given, writes the
        proof''s context into that account. This is the way to verify a proof too
        large to carry in one transaction, such as a 256-bit range proof: write it
        into a record account first (record/create-account, record/initialize, record/write),
        then verify it from there with proof_offset 33. context_state_account must
        already exist, sized and owned for this proof type (see context-state/create/batched-range-proof-u128).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedRangeProofU128Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedRangeProofU128Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedRangeProofU128 proof from an account and persist its
        context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/batched-range-proof-u256:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedRangeProofU256, proof-in-account and
        context-state form: reads the proof from proof_account at proof_offset --
        five bytes of instruction data however large the proof is -- and, because
        context_state_account and context_state_account_owner are given, writes the
        proof''s context into that account. This is the way to verify a proof too
        large to carry in one transaction, such as a 256-bit range proof: write it
        into a record account first (record/create-account, record/initialize, record/write),
        then verify it from there with proof_offset 33. context_state_account must
        already exist, sized and owned for this proof type (see context-state/create/batched-range-proof-u256).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedRangeProofU256Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountBatchedRangeProofU256Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedRangeProofU256 proof from an account and persist its
        context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/ciphertext-ciphertext-equality:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyCiphertextCiphertextEquality, proof-in-account
        and context-state form: reads the proof from proof_account at proof_offset
        -- five bytes of instruction data however large the proof is -- and, because
        context_state_account and context_state_account_owner are given, writes the
        proof''s context into that account. This is the way to verify a proof too
        large to carry in one transaction, such as a 256-bit range proof: write it
        into a record account first (record/create-account, record/initialize, record/write),
        then verify it from there with proof_offset 33. context_state_account must
        already exist, sized and owned for this proof type (see context-state/create/ciphertext-ciphertext-equality).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountCiphertextCiphertextEqualityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountCiphertextCiphertextEqualityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a CiphertextCiphertextEquality proof from an account and persist
        its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/ciphertext-commitment-equality:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyCiphertextCommitmentEquality, proof-in-account
        and context-state form: reads the proof from proof_account at proof_offset
        -- five bytes of instruction data however large the proof is -- and, because
        context_state_account and context_state_account_owner are given, writes the
        proof''s context into that account. This is the way to verify a proof too
        large to carry in one transaction, such as a 256-bit range proof: write it
        into a record account first (record/create-account, record/initialize, record/write),
        then verify it from there with proof_offset 33. context_state_account must
        already exist, sized and owned for this proof type (see context-state/create/ciphertext-commitment-equality).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountCiphertextCommitmentEqualityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountCiphertextCommitmentEqualityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a CiphertextCommitmentEquality proof from an account and persist
        its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/grouped-ciphertext-2-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyGroupedCiphertext2HandlesValidity, proof-in-account
        and context-state form: reads the proof from proof_account at proof_offset
        -- five bytes of instruction data however large the proof is -- and, because
        context_state_account and context_state_account_owner are given, writes the
        proof''s context into that account. This is the way to verify a proof too
        large to carry in one transaction, such as a 256-bit range proof: write it
        into a record account first (record/create-account, record/initialize, record/write),
        then verify it from there with proof_offset 33. context_state_account must
        already exist, sized and owned for this proof type (see context-state/create/grouped-ciphertext-2-handles-validity).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountGroupedCiphertext2HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountGroupedCiphertext2HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a GroupedCiphertext2HandlesValidity proof from an account and
        persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/grouped-ciphertext-3-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyGroupedCiphertext3HandlesValidity, proof-in-account
        and context-state form: reads the proof from proof_account at proof_offset
        -- five bytes of instruction data however large the proof is -- and, because
        context_state_account and context_state_account_owner are given, writes the
        proof''s context into that account. This is the way to verify a proof too
        large to carry in one transaction, such as a 256-bit range proof: write it
        into a record account first (record/create-account, record/initialize, record/write),
        then verify it from there with proof_offset 33. context_state_account must
        already exist, sized and owned for this proof type (see context-state/create/grouped-ciphertext-3-handles-validity).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountGroupedCiphertext3HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountGroupedCiphertext3HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a GroupedCiphertext3HandlesValidity proof from an account and
        persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/percentage-with-cap:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyPercentageWithCap, proof-in-account and context-state
        form: reads the proof from proof_account at proof_offset -- five bytes of
        instruction data however large the proof is -- and, because context_state_account
        and context_state_account_owner are given, writes the proof''s context into
        that account. This is the way to verify a proof too large to carry in one
        transaction, such as a 256-bit range proof: write it into a record account
        first (record/create-account, record/initialize, record/write), then verify
        it from there with proof_offset 33. context_state_account must already exist,
        sized and owned for this proof type (see context-state/create/percentage-with-cap).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountPercentageWithCapRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountPercentageWithCapResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a PercentageWithCap proof from an account and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/pubkey-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyPubkeyValidity, proof-in-account and context-state
        form: reads the proof from proof_account at proof_offset -- five bytes of
        instruction data however large the proof is -- and, because context_state_account
        and context_state_account_owner are given, writes the proof''s context into
        that account. This is the way to verify a proof too large to carry in one
        transaction, such as a 256-bit range proof: write it into a record account
        first (record/create-account, record/initialize, record/write), then verify
        it from there with proof_offset 33. context_state_account must already exist,
        sized and owned for this proof type (see context-state/create/pubkey-validity).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountPubkeyValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountPubkeyValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a PubkeyValidity proof from an account and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify-from-account/zero-ciphertext:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyZeroCiphertext, proof-in-account and context-state
        form: reads the proof from proof_account at proof_offset -- five bytes of
        instruction data however large the proof is -- and, because context_state_account
        and context_state_account_owner are given, writes the proof''s context into
        that account. This is the way to verify a proof too large to carry in one
        transaction, such as a 256-bit range proof: write it into a record account
        first (record/create-account, record/initialize, record/write), then verify
        it from there with proof_offset 33. context_state_account must already exist,
        sized and owned for this proof type (see context-state/create/zero-ciphertext).
        recent_blockhash is always required and is never fetched server-side. Left
        alone, it also builds the message and expires whenever the runtime says it
        does. Naming durable_nonce_account builds the message against the value that
        account stores instead, so the transaction never expires, and prepends the
        advance that consumes it; recent_blockhash then only prices the transaction.
        The response reports nonce_authority in that case, which has to sign as well.
        A ComputeBudget SetComputeUnitLimit instruction is placed ahead of the verify,
        because the program charges a fixed compute cost per proof type (range u128
        costs the whole default 200,000 and u256 costs 368,000): compute_unit_limit
        sets it, and left empty it is this proof type''s own cost plus a margin.'
      parameters:
      - description: Context-state verify-from-account request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyFromAccountZeroCiphertextRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyFromAccountZeroCiphertextResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a ZeroCiphertext proof from an account and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/batched-grouped-ciphertext-2-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedGroupedCiphertext2HandlesValidity,
        context-state form: verifies proof_data and, because context_state_account
        and context_state_account_owner are given, writes the proof''s context into
        that account instead of only checking it. context_state_account must already
        exist, sized and owned for this proof type (see context-state/create/batched-grouped-ciphertext-2-handles-validity)
        -- created in an earlier transaction, or (to close the front-running window
        the interface crate''s own docs describe) merged into this same transaction
        by combining this endpoint''s instruction with that create endpoint''s. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyBatchedGroupedCiphertext2HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyBatchedGroupedCiphertext2HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedGroupedCiphertext2HandlesValidity proof and persist
        its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/batched-grouped-ciphertext-3-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedGroupedCiphertext3HandlesValidity,
        context-state form: verifies proof_data and, because context_state_account
        and context_state_account_owner are given, writes the proof''s context into
        that account instead of only checking it. context_state_account must already
        exist, sized and owned for this proof type (see context-state/create/batched-grouped-ciphertext-3-handles-validity)
        -- created in an earlier transaction, or (to close the front-running window
        the interface crate''s own docs describe) merged into this same transaction
        by combining this endpoint''s instruction with that create endpoint''s. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyBatchedGroupedCiphertext3HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyBatchedGroupedCiphertext3HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedGroupedCiphertext3HandlesValidity proof and persist
        its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/batched-range-proof-u64:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedRangeProofU64, context-state form:
        verifies proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/batched-range-proof-u64) -- created in an earlier
        transaction, or (to close the front-running window the interface crate''s
        own docs describe) merged into this same transaction by combining this endpoint''s
        instruction with that create endpoint''s. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyBatchedRangeProofU64Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyBatchedRangeProofU64Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedRangeProofU64 proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/batched-range-proof-u128:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedRangeProofU128, context-state form:
        verifies proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/batched-range-proof-u128) -- created in an
        earlier transaction, or (to close the front-running window the interface crate''s
        own docs describe) merged into this same transaction by combining this endpoint''s
        instruction with that create endpoint''s. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyBatchedRangeProofU128Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyBatchedRangeProofU128Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedRangeProofU128 proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/batched-range-proof-u256:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyBatchedRangeProofU256, context-state form:
        verifies proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/batched-range-proof-u256) -- created in an
        earlier transaction, or (to close the front-running window the interface crate''s
        own docs describe) merged into this same transaction by combining this endpoint''s
        instruction with that create endpoint''s. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyBatchedRangeProofU256Request'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyBatchedRangeProofU256Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a BatchedRangeProofU256 proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/ciphertext-ciphertext-equality:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyCiphertextCiphertextEquality, context-state
        form: verifies proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/ciphertext-ciphertext-equality) -- created
        in an earlier transaction, or (to close the front-running window the interface
        crate''s own docs describe) merged into this same transaction by combining
        this endpoint''s instruction with that create endpoint''s. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyCiphertextCiphertextEqualityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyCiphertextCiphertextEqualityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a CiphertextCiphertextEquality proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/ciphertext-commitment-equality:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyCiphertextCommitmentEquality, context-state
        form: verifies proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/ciphertext-commitment-equality) -- created
        in an earlier transaction, or (to close the front-running window the interface
        crate''s own docs describe) merged into this same transaction by combining
        this endpoint''s instruction with that create endpoint''s. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyCiphertextCommitmentEqualityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyCiphertextCommitmentEqualityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a CiphertextCommitmentEquality proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/grouped-ciphertext-2-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyGroupedCiphertext2HandlesValidity, context-state
        form: verifies proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/grouped-ciphertext-2-handles-validity) -- created
        in an earlier transaction, or (to close the front-running window the interface
        crate''s own docs describe) merged into this same transaction by combining
        this endpoint''s instruction with that create endpoint''s. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyGroupedCiphertext2HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyGroupedCiphertext2HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a GroupedCiphertext2HandlesValidity proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/grouped-ciphertext-3-handles-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyGroupedCiphertext3HandlesValidity, context-state
        form: verifies proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/grouped-ciphertext-3-handles-validity) -- created
        in an earlier transaction, or (to close the front-running window the interface
        crate''s own docs describe) merged into this same transaction by combining
        this endpoint''s instruction with that create endpoint''s. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyGroupedCiphertext3HandlesValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyGroupedCiphertext3HandlesValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a GroupedCiphertext3HandlesValidity proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/percentage-with-cap:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyPercentageWithCap, context-state form: verifies
        proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/percentage-with-cap) -- created in an earlier
        transaction, or (to close the front-running window the interface crate''s
        own docs describe) merged into this same transaction by combining this endpoint''s
        instruction with that create endpoint''s. recent_blockhash is always required
        and is never fetched server-side. Left alone, it also builds the message and
        expires whenever the runtime says it does. Naming durable_nonce_account builds
        the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyPercentageWithCapRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyPercentageWithCapResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a PercentageWithCap proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/pubkey-validity:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyPubkeyValidity, context-state form: verifies
        pubkey_proof against elgamal_pubkey and, because context_state_account and
        context_state_account_owner are given, writes the proof''s context (the ElGamal
        pubkey alone) into that account instead of only checking it. context_state_account
        must already exist, sized and owned for this proof type (see context-state/create/pubkey-validity)
        -- created in an earlier transaction, or (to close the front-running window
        the interface crate''s own docs describe) merged into this same transaction
        by combining this endpoint''s instruction with that create endpoint''s. recent_blockhash
        is always required and is never fetched server-side. Left alone, it also builds
        the message and expires whenever the runtime says it does. Naming durable_nonce_account
        builds the message against the value that account stores instead, so the transaction
        never expires, and prepends the advance that consumes it; recent_blockhash
        then only prices the transaction. The response reports nonce_authority in
        that case, which has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyPubkeyValidityRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyPubkeyValidityResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a PubkeyValidity proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
  /svm/v2/transaction/zk-elgamal-proof/context-state/verify/zero-ciphertext:
    post:
      consumes:
      - application/json
      description: 'ZkElgamalProof VerifyZeroCiphertext, context-state form: verifies
        proof_data and, because context_state_account and context_state_account_owner
        are given, writes the proof''s context into that account instead of only checking
        it. context_state_account must already exist, sized and owned for this proof
        type (see context-state/create/zero-ciphertext) -- created in an earlier transaction,
        or (to close the front-running window the interface crate''s own docs describe)
        merged into this same transaction by combining this endpoint''s instruction
        with that create endpoint''s. recent_blockhash is always required and is never
        fetched server-side. Left alone, it also builds the message and expires whenever
        the runtime says it does. Naming durable_nonce_account builds the message
        against the value that account stores instead, so the transaction never expires,
        and prepends the advance that consumes it; recent_blockhash then only prices
        the transaction. The response reports nonce_authority in that case, which
        has to sign as well.'
      parameters:
      - description: Context-state verify request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/v2.ContextStateVerifyZeroCiphertextRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v2.ContextStateVerifyZeroCiphertextResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "502":
          description: Bad Gateway
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Verify a ZeroCiphertext proof and persist its context
      tags:
      - v2-transaction-zk-elgamal-proof-context-state
swagger: "2.0"
